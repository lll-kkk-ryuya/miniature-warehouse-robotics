# warehouse_orchestrator — KPI 計測・Langfuse score・分析

- **担当トラック / ブランチ**: wo / `feat/wo-metrics`（epic #6。slice 1=#69 audit→KPI / slice 2=#73 Langfuse v4 / slice 3=#6 node-wiring）
- **Phase**: 0.5→4（slice 1 #69 = audit→KPI 土台 / slice 2 #73 = Langfuse v4 実配線。WO統合本番は Phase 4: doc06:239,265-268）
- **ビルド**: ament_python
- **ノード / CLI**: `kpi_collector`（rclpy ノード）/ `kpi_report`（オフライン分析 CLI）
- **編集境界**: このパッケージ配下のみ。共有契約 `warehouse_interfaces` は変更不可（`.claude/rules/parallel-workflow.md` §4）。`warehouse_mcp_server` 等の他トラック内部は import しない（audit.jsonl は**ファイルとして**消費）。
- **依存**: `warehouse_interfaces.paths.audit_log_path`（凍結パス）・rclpy・`nav_msgs`（odom 購読）。`langfuse`（**v4: `>=4.9,<5` を想定**）は**遅延・任意 import**（fail-open / pip extra）＝ハード依存にしない（package.xml に追加せず、未インストールでも build/test 可）。
- **テスト**: 偽 audit.jsonl（`tmp_path` + `WAREHOUSE_AUDIT_LOG_PATH`）+ 偽 Langfuse client + 注入 `create_fn` で独立検証（doc16 §11 / doc20:36）。rclpy/langfuse 非依存のコア（`kpi.py`/`audit_reader.py`/`langfuse_sink.py`/`trace_id.py`）を単体検証。Ruff(py312/line100/double-quote) + pytest 緑を維持（host py3.7 不可＝`python3.12`）。
- **設計**: docs/architecture/08-llm-bridge-common.md（§Langfuse: v4 `create_score`/`flush` 356-362, §比較指標, **§比較計測の追加設計（#88: 追加スコア/Grok cost/集計）**, cancelled 除外 252）, 13-hermes-setup.md（**§7.5 trace_id 契約 512-520**: 32hex no-dash 516 / Bridge-owned 517 / `create_trace_id(seed)` 516,519 / **Grok cost :520②**）, 14-character-llm-negotiation.md（**§交渉スコア #88**）, 15-mcp-platform.md（§Command Audit Log 344-360）, 06（Phase 265-268, **交通モード軸 275**）, 16 §4（共有パス）, 09:79（odom）, **20 §8（Langfuse 観測 taxonomy・#88）**。

## 提供 (produce)
- **CLI** `kpi_report [path] [--include-cancelled] [--json]` — audit.jsonl から result KPI を集計し出力。
- **node** `kpi_collector` — `report_interval_sec`(=30) ごとに audit.jsonl を読み KPI を log 出力、`/bot{n}/odom` を購読し移動距離を積算、Langfuse v4 score を best-effort 送信。param: `exclude_cancelled`(=True) / `audit_log_path`(空=凍結パス) / `robot_names`(=[bot1,bot2]) / `run_id`(空=`WAREHOUSE_RUN_ID` env) / `mode`(score metadata 用 A/B/C) / `provider`(空=`WAREHOUSE_PROVIDER` env、score metadata 用。doc08:367) / **`motion_buffer_samples`(=4096・#432 odom Tier-1 の ring buffer 深さ。メモリ境界であって domain しきい値ではない。非正値は既定へ fallback＋warning)**。
- **lane-internal 型/関数**（**凍結契約ではない**。`warehouse_interfaces` には置かない）: `kpi.{KpiReport,ResultTally,CompletionStats,CompletionRecord,DistanceAccumulator,distance_traveled,compute_efficiency,latest_gen_id,robot_load_fairness,INTERVENTION_TOOL}`（**#432 Tier-1 追加**: `KpiReport.{intervention_count,command_decisions,intervention_rate,command_rejection_rate,fairness_jain}` / `CompletionStats.{makespan,throughput}`＝report フィールドのみ・**新 score 送信なし**）、**`motion.{MotionSample,MotionAccumulator,MotionInputs,SmoothnessStats,sample_rate_hz,smoothness_stats,detour_factors,DEFAULT_MOTION_BUFFER_SAMPLES}`（#432 odom 由来 Tier-1・`kpi` から再 export・`KpiReport.{distance_traveled,detour_factors,smoothness}` 追加＝report フィールドのみ・**新 score 送信なし**）**、`audit_reader.AuditEntry`、`conversation_metrics.{ConversationBenchMetrics,compute_conversation_metrics,read_conversation_event_rows}`、`trace_id.{normalize_trace_id,derive_trace_id,seed_for,trace_id_for}`、`langfuse_sink.LangfuseScoreSink`＋予約 `SCORE_*`（Phase3-4 score 名・inert）、`tags.{TAG_KEYS,TAG_KEY_RUN_ID/MODE/PROVIDER/GEN_ID/ROBOT,provider_tags}`（Langfuse 観測 taxonomy 単一ソース・doc20 §8・`provider_tags` は trace tag 形 `[provider,mode]` の reference で live emit は #4＝inert）、`score_send.{resolve_provider,resolve_pattern_d,build_score_metadata,send_scores}`（`resolve_pattern_d`=Option D scorer 脚の owner switch・#360）、`grok_cost.{grok_cost,resolve_grok_price,grok_cost_for_model,GrokPrice,GROK_PRICE_TABLE_2026_06_04,GROK_PRICE_TABLE_2026_06_12}`（Grok オフライン cost 純関数・#133。`_2026_06_12` は #88 wo-metrics の **current-public 価格表**（additive・注入優先・default 不変））。`KpiReport.to_dict()` / `ConversationBenchMetrics.to_dict()` はローカル出力用でトラック跨ぎ契約ではない（KPI 出力契約は未定 → voids）。

## 消費 (consume)
- 契約: `warehouse_interfaces.paths.audit_log_path()`（**凍結**、doc16 §4 / paths.py:48）→ `/tmp/warehouse/audit.jsonl`（dev、`WAREHOUSE_AUDIT_LOG_PATH` 上書き可）。
- env: **`WAREHOUSE_RUN_ID`**（per-run、#4 と同一値を読む。#73 / doc13:519）。
- env: **`WAREHOUSE_LANGFUSE_OWNER`**（Option D scorer 脚・additive・既定 `bridge`＝Pattern A 不変）。`kpi_collector` が `score_send.resolve_pattern_d(load_config())` で解決し `send_scores(pattern_d=…)` へ渡す＝**#4 Bridge が読む同一 knob を読んで両脚を1 config で flip**（`hermes_plugin`→Pattern D で `eval_sdk.seed.derive_plugin_trace_id` 再導出・不明値/blank は安全側で Pattern A）。**Bridge 定数を import しない**（一方向依存・値 `bridge`/`hermes_plugin` を mirror）。`hermes.langfuse_owner` config も env 欠如時に読む（`warehouse_interfaces.config.load_config`・fail-open）。
- file: Command Audit Log（JSON Lines）。**レコード形は凍結契約ではない**。実プロデューサ `warehouse_mcp_server/audit.py:34-43` = `{timestamp, tool, result∈{executed,rejected,error}, detail, robot}`（doc15 例示の `traffic_mode` は実コード未記録＝コードが正）→ **防御的にパース**。
- file: Mode A conversation event log（JSON Lines）`conversation_events.jsonl`。既定は bridge 側 `runtime_dir()/conversation_events.jsonl`、実験では `WAREHOUSE_CONVERSATION_EVENT_LOG_PATH`。凍結 contract ではないため `conversation_metrics.py` が防御的にパースする。
- topic: `/bot{n}/odom`（`nav_msgs/Odometry`、efficiency=総移動距離。doc09:79）。
- **pkg `eval_sdk`（doc21 Phase 1・一方向依存・`package.xml` exec_depend）**: domain 非依存コアを import。`trace_id`←`eval_sdk.seed.{seed_for,normalize_trace_id,derive_trace_id}`（`seed_for` は #4 `tracing.trace_seed` と統合した**唯一の join key**）／`langfuse_sink.LangfuseScoreSink`←`eval_sdk.sink.FailOpenScoreSink`＋`DATA_TYPE_*`（KPI score 名・robot-name fallback・HERMES env は wo 側に残置）／`kpi.{_percentile,distance_traveled,compute_efficiency,DistanceAccumulator}`←`eval_sdk.stats`（`compute_efficiency`=`path_lengths` の wo 別名）／`grok_cost.{grok_cost,resolve_grok_price,grok_cost_for_model,GrokPrice}`←`eval_sdk.cost`（価格表 `GROK_PRICE_TABLE_*` は provenance 付きで wo に残置＝注入）。**外部 import 面は不変**（再 export）。

## 実装済み（slice 2 / #73）
- `langfuse_sink.py` — **v2 `.score()` → v4 `create_score(trace_id,name,value,data_type,metadata)` + `flush()`**（`get_client()` 経由、doc08:356-365）。`result`=CATEGORICAL / `task_completion_time`・`efficiency`=NUMERIC。robot/mode/run_id を metadata に、**未対応版は score 名に robot 埋め込み（`result_bot1`）でフォールバック**（doc08:367）。trace_id を 32hex-no-dash 正規化（doc13:516）。fail-open + 遅延 import 維持。
- `trace_id.py` — `trace_id = create_trace_id(seed=f"{WAREHOUSE_RUN_ID}:{gen_id}")`（#73 / doc13:516,519、**両脚決定的同一 id**）。`normalize_trace_id`/`seed_for`/`derive_trace_id`(create_fn 注入可)/`trace_id_for`。**凍結契約変更なし**（trace_id は Langfuse/Audit 突合キー）。
- `kpi.py` — `distance_traveled`/`compute_efficiency`/`DistanceAccumulator`（efficiency=総移動距離, doc08 §比較指標 / odom doc09:79）。result KPI 群・cancelled 除外・task_completion_time scaffold は slice 1 のまま。
- `audit_reader.py` — `AuditEntry.gen_id`（**`detail.gen_id` のみ**読み、無ければ None。`received_gen`＝stale reject の*却下された*古い世代には **fallback しない**＝doc13:519 は executed gen を join key とする。下記 voids 2 / `tests/unit/test_wo_audit_reader.py:148-162`）。
- `kpi_collector.py` — odom 購読（inert）+ v4 sink + trace_id 導出 + shutdown flush。**live 送信はゲート**（creds + `WAREHOUSE_RUN_ID` + gen_id が揃うまで no-op）。

## 実装済み（slice 3 / #6 部分: provider+gen_id score metadata）
- `score_send.py`（**新規・rclpy 非依存の pure helper**）— node の score 送信ロジックを抽出（node は薄い shell、doc16 §11）。`resolve_provider(param)`＝param→`WAREHOUSE_PROVIDER` env（空/空白は unset、doc08:367）／`build_score_metadata(run_id,mode,provider,gen_id)`＝doc08:360,363 の `{robot,mode,provider,gen_id}` を組立（`robot` は efficiency leg が per-leg 付与、`run_id` は seed 半分の additive extra・#73）／`send_scores(sink,report,entries,distances,*,run_id,mode,provider,create_fn,pattern_d=False)`＝ゲート（sink無効 / run_id 未設定 / trace 導出不可 → `(0,None)` fail-open）+ task_completion_time + per-robot efficiency 送信 → `(#sent, trace|None)`。`create_fn` 注入で SDK 無しに unit 可能。`pattern_d=False`（既定 Pattern A）は `trace_id_for`（Bridge 所有 trace 再導出）／`True`（Option D）は `derive_plugin_trace_id`（plugin `H::H` 再導出）＝blank/whitespace run_id は両脚とも `None`（Pattern A の strip と対称・#360 review #3）。`resolve_pattern_d(cfg,env)`＝`WAREHOUSE_LANGFUSE_OWNER` env→`hermes.langfuse_owner` config→既定 `bridge`(False)・`hermes_plugin` のみ True・不明値は安全側 False＋**`log.warning`**（#4 `resolve_langfuse_owner` の bool 鏡像・`isinstance(...,Mapping)` 型チェックも一致・Bridge import せず＝`"bridge"`/`"hermes_plugin"` literal は cross-lane 文字列契約として `tests/unit/test_hermes_client_option_d.py` の cross-check で同値 pin・#360 round-2 D）。
- `kpi_collector.py` — `provider` param 追加（doc08:367）。`_send_scores` は `score_send.send_scores` へ委譲し、trace 導出時のみ flush + log（送信ゲート/metadata 組立はすべて pure helper 側）。**送信 score の metadata に `provider`+`gen_id` を追加**（従来 `{run_id,mode}` のみ → `{run_id,mode,provider,gen_id}`、doc08:369）。**Option D scorer 脚（#360）**: `__init__` で `load_config()`→`resolve_pattern_d(cfg)` を一度解決し（fail-open）`send_scores(pattern_d=self._pattern_d)` へ渡す＝`WAREHOUSE_LANGFUSE_OWNER` で #4 Bridge と両脚を flip（既定 Pattern A 不変）。startup ログに `langfuse_owner` を表示。
- `tests/unit/test_wo_kpi_collector.py`（新規）— fake audit.jsonl（`tmp_path`+`WAREHOUSE_AUDIT_LOG_PATH`）→ `read_audit_log`→`compute_kpis`→`send_scores`（fake v4 client + 注入 `create_fn`）の実チェーンで **gate matrix**（run_id×gen_id×creds→送信数）+ `provider`/`gen_id` metadata + trace 決定性を検証。

## 実装済（#133 / Part of #88: Grok オフライン cost + 予約 SCORE_*・実機不要分）
- `grok_cost.py`（**新規・rclpy/langfuse/xAI SDK 非依存の pure helper**, doc08:500-508）— `grok_cost(usage_details, price)`＝`tokens×per-token 価格→USD`（0-token 境界=0.0、token キーは `input`/`input_tokens`/`prompt_tokens` 等を**防御パース**＝live 形未確定 doc08:508）／`resolve_grok_price(model, table, *, default)`＝`xai/` prefix 除去・case 無視・**最長 prefix** マッチ、未知 model→`default`（既定 `None`=unpriceable）／`grok_cost_for_model(...)`＝解決+計算、未知 model は `None`（0.0 と区別＝fail-open）。`GROK_PRICE_TABLE_2026_06_04`＝**versioned 静的 xAI 価格表**だが**値は UNVERIFIED PLACEHOLDER**（`# TODO(#88 Phase3)`＋出典 URL/取得日）＝**構造のみ凍結**、テストは表注入（doc08:508 の literal model 文字列・単価・価格フィールド形は live 確認まで推測固定しない）。
- **`GROK_PRICE_TABLE_2026_06_12`（#88 wo-metrics・additive・current-public 価格表）** — 公開 xAI 価格を再取得し date-stamp（出典 `https://docs.x.ai/developers/models` / 取得 2026-06-12）。grok-4.3 = \$1.25/\$2.50・grok-4.20\*（family prefix）= \$1.25/\$2.50・grok-build-0.1 = \$1.00/\$2.00（USD/1M ÷1e6）。**placeholder ではなく実 public 値**だが ①literal model 文字列 ②`usage_details` キー形 ③v4 価格フィールド形 が **live 未確認＝「検証済み」と称さない**（doc08:510）。current page に無い旧 model（grok-4-0709/grok-3）は **意図的に無行**＝`resolve_grok_price`→`default`(None)＝推測価格を発明しない（doc08:510）。既存 placeholder 表は**削除せず併存**・`resolve_grok_price` の default 引数は不変（注入優先）。
- `langfuse_sink.py` — Phase3-4 score 名を**予約**（`SCORE_COLLISION_FREE`/`SCORE_REPLANS`/`SCORE_MEAN_DECISION_LATENCY`/`SCORE_DEADLOCK`＝NUMERIC doc08:496 + Mode A `SCORE_NEGOTIATION_ROUNDS`/`SCORE_AGREEMENT_REACHED`、`DATA_TYPE_BOOLEAN` 追加）。**docs凍結名一致・live send 経路なし（inert）**＝producer（Guardian/Nav2/deadlock 検出/交渉）未配線のため emit は Phase3-4（#88）。
- `tests/unit/test_wo_grok_cost.py`（新規, R-26・live SDK 不使用）— token×price / 0境界 / alias token キー / bool 除外 / prefix・`xai/`・case マッチ / 未知 model fallback / 最長 prefix / static 表 structure。**#88 wo-metrics で additive 3 case**: `_2026_06_12` の structure + 公開 USD/1M ラウンドトリップ（grok-4.3=\$3.75・grok-build-0.1=\$3.00／1M+1M）/ 出典 URL+取得日 provenance（`inspect.getsource`・doc08:506）/ family-prefix 解決 + 旧 model 無行→None。`test_wo_langfuse_sink.py` に予約 SCORE_* 名 assert ＋ live-send 経路非存在 assert（inert 担保）。

## 実装済（#88 / wo-metrics: Langfuse 観測 tag taxonomy + score metadata 定数化・実機不要分）
- `tags.py`（**新規・pure leaf・rclpy/langfuse 非依存**, doc20 §8 / doc08:377,489）— Phase4 比較（4 provider × 3 交通モード）の弁別子語彙を**単一ソース**化。`TAG_KEYS=(run_id,mode,provider,gen_id)`＝score metadata キー集合（doc08:489）／`TAG_KEY_ROBOT`＝per-leg キー（doc08:369、`TAG_KEYS` 非包含）／`provider_tags(provider,mode)→[provider,mode]`＝trace tag リスト形（doc08:377）の reference（blank 除外）。**`provider_tags` の live emit は `eval_sdk.tracer.LangfuseTracer.turn`（`ws/src/eval_sdk/eval_sdk/tracer.py:220` `tags=[self._provider, self._mode, *self._extra_tags]`・記述は同 :59-60）**＝cross-lane import 禁止のため #4 は本 module を import せず、wo 側は inert（予約 `SCORE_*` と同型）。wo が消費するのは `TAG_KEYS`。**re-pin（#432）**: 旧引用 `tracing.py:171`/`:96` は #273 の eval_sdk 抽出後に無効（`warehouse_llm_bridge/tracing.py` は現在 **70 行の re-export shim**＝`from eval_sdk.tracer import LangfuseTracer, NoopTracer, Tracer` のみで、:96/:171 は範囲外）。
- `score_send.build_score_metadata` — キーを `TAG_KEY_*` 参照に（**additive・挙動不変**、off-taxonomy キーを発明しない）。`langfuse_sink._name_with_robot` も `TAG_KEY_ROBOT` 参照に。
- `docs/architecture/20-dev-quality-and-testing.md` §8 — 弁別子→格納先→owner 一覧（trace name/tags/metadata vs score metadata/name）＋ Phase4 比較クエリ（doc08:514-520）＋未決（§8.4: `mode`=Mode-letter vs traffic_mode 未確定／metadata group-by 可否／実機実トレース＝Phase3）。**doc08/doc13 が正・doc20 は地図**。
- `tests/unit/test_wo_tags.py`（新規, R-26・live SDK 不使用）— `TAG_KEYS` 集合 / `provider_tags` 順序・blank 除外 / **`build_score_metadata` のキー = `TAG_KEYS`（ドリフトガード）**。
- `tests/unit/test_wo_kpi.py`（#88 wo-metrics で property-style 追補, R-26・live SDK 不使用・`hypothesis` 非依存）— offline KPI 算術コアの不変条件を **fixed-seed 決定的ループ**で pin（既存の example test に追加）: `_percentile`=百分位の単調性・データ域境界・順序非依存・グリッド点 `q=100k/(n-1)` での順序統計量一致（`kpi._percentile` = `eval_sdk.stats.percentile`）／`cancelled_task_ids`=executed cancel のみ抽出を独立オラクルと突合（`kpi.cancelled_task_ids`）／`pair_completion_times`=非負所要時間・task_id ごと ≤1・最早 dispatch・supplied∩executed・exclude_cancelled 部分集合（`kpi.pair_completion_times`）／`completion_stats`=`min≤p50≤p95≤p99≤max` と空入力全 None（`kpi.completion_stats`）。**テストのみ・本体コード/契約は不変**。**#432 review 反映**: これら 4 件の引用は元々 `kpi.py:79-91` 等の行番号だったが origin/main 時点で既に stale（#273 の `_percentile` 抽出・その後の追記でズレ）＋本 slice の +109 行で更にズレたため、**行番号を捨ててシンボル参照に切替**（同じ理由で `tests/unit/test_wo_kpi.py` 内の 8 件も再ピン）。
- **docs-first 同期（citation hygiene・#88 review 反映）**: doc 成長で生じた stale 行参照を本 slice で touch した wo lane の全ファイル（`tags/score_send/langfuse_sink/本CLAUDE.md/test_wo_tags/test_wo_kpi_collector`）で補正: doc13 §7.5（現 `512-520`）・doc08 robot 採用実装（`343`→`369`）・score metadata 例示 dict（`341-346`→`360,363`、score 名+型→`358-362`）・no-tags / robot-name fallback（`350`・`365`→`367`＝code fence でなく実文）・gen_id 定義（`182`→`183`＝blank 行）・trace tag emit（`tracing.py:96`記述→`:171`emit。**#432 で再 re-pin → `eval_sdk/tracer.py:220` emit / :59-60 記述**＝#273 抽出で `tracing.py` が 70 行 shim 化したため）。doc08 本体は #54 所有＝read-only・未編集（doc08 内部の `:365` 自己参照は #54 の課題）。**残 follow-up**（本 slice 対象外）: 未編集関数の `doc08:341`単独（`langfuse_sink` create_score/result 名・本 CLAUDE.md result 値マッピング）、`doc13:506`（12構成 KPI 集計）、他レーンの module stale。
- **残課題（Phase 3）**: 実 Langfuse 4.9.x での trace 1本集約・二重 generation 無し・trace_id 突合・Grok cost（doc13:520）。**Grok cost の exact live-verify 項目（①literal model 文字列 ②`usage_details` キー形 ③v4 価格フィールド形 ④cost≠0）は §前提・未確定 item 8 に一元化**。score `mode` 値域（Mode-letter vs traffic_mode）は #4 と確定。

## 実装済（#432 / Part of #430: Tier-1 KPI 拡張 — audit 由来のみ・additive）
- `kpi.py` — doc21 §14 Step1.5b（`docs/architecture/21-eval-sdk-extraction.md:407`）/ §6 表（`:180-189`）/ §13.2（`:310`）の **audit 由来 Tier-1** を additive 実装。層分担は doc21:178（**数学=`eval_sdk.stats` / ドメイン合成=wo**）。**新 producer ゼロ・新トピックゼロ・新 score 送信ゼロ**（score 名符号化は #434 所有）。
  - `KpiReport.intervention_count` = **executed** `escalation_response` 行数（doc21:184 の producer＝MCP tool6・`warehouse_mcp_server/tools.py:443` が executed 行を `robot=new_robot` で記帳＝executed 分岐は同 `:435-444`。rejected 行は「介入していない」ので除外＝全数は `by_tool["escalation_response"]` に残す）。`COMMAND_TOOLS` 所属とは独立に判定し、その独立性は `COMMAND_TOOLS` を実際に再スコープする unit（`tests/unit/test_wo_tier1_kpi.py::test_intervention_count_survives_a_rescoped_command_tool_set`）で赤く落ちる形に pin。
  - `KpiReport.intervention_rate` = `intervention_count / command_decisions`。**分母は暫定**（doc21:184 は分子のみ規定＝下記 voids 11）。`command_decisions`（=`acceptance_rate` と同分母）を露出して再導出可能にする。
  - `KpiReport.command_rejection_rate` = `1 − acceptance_rate`（doc21:310「command rejection 率」）。**error/other 行も含む補数**である旨を docstring と本節に明記（voids 11）。
  - `KpiReport.fairness_jain` = `robot_load_fairness(by_robot)` = **Jain 指数**（doc21:310）を **per-robot executed 負荷**に適用（doc21:186 は `by_robot` を指定するが「どの tally が負荷か」は未規定＝executed を採用・voids 11）。
  - `CompletionStats.{makespan,throughput}` = 完了記録の span（最初の dispatch→最後の completion）と `count/makespan`。**live completion 源は Phase 3a のため既定は inert（`completions` 未指定＝`None`）**。観測窓は doc21:185 未規定＝暫定（voids 11）。**失敗モード追加（#432 review）**: `completion_stats` は `completion_ts < dispatch_ts` の `CompletionRecord` を**直接**渡されると `eval_sdk.stats.makespan` 由来の `ValueError` を送出する（fail-loud）。`compute_kpis` 経由では `pair_completion_times` が反転を事前に落とすので **live 経路（`kpi_collector`→`compute_kpis`）は挙動不変・到達不能**。docstring に明記。
  - `format_report` / `to_dict()` に上記を追記（**既存キーは不変＝additive**）。
- `eval_sdk/stats.py`（同レーンで追記・domain 非依存）— `rate` / `jain_fairness_index`（Jain, Chiu & Hawe 1984 DEC-TR-301・自前実装＋帰属コメント）/ `makespan` / `throughput`。**しきい値・ドメイン語彙を持たない**（doc21:178）。
- **odom 由来 Tier-1 は本 slice では未実装**（当時: detour factor / jerk・SPARC 合成 / 速度予算消化率 / idle 率）: `kpi_collector` は odom を `DistanceAccumulator.add(robot,x,y)` に流すだけで **pose 列も速度も保持しない**（`eval_sdk.stats.DistanceAccumulator` は `_totals` と直前1点のみ）→ 保持無しでは算出不能。**→ 後続 slice（下記「#432 odom 由来」）で detour factor と 軌道平滑性のみ実装**（`motion.MotionAccumulator` が**既存 subscription 内で**列を保持＝新トピック/新ノード/新契約なし）。**idle 率 / 速度予算消化率 は定義が docs に無いため引き続き未実装**（voids 15）。
- `tests/unit/test_wo_tier1_kpi.py`（新規, live SDK 不使用）— 偽 `audit.jsonl`（`tmp_path`+`WAREHOUSE_AUDIT_LOG_PATH`）→ `read_audit_log`→`compute_kpis` の実チェーン。期待値は**独立オラクル**（手集計 / Jain の閉形式端点 1.0・1/n / 手計算 makespan）。mutation 確認済（fairness `executed→total` / 介入の executed フィルタ除去 / makespan→最大単体時間 いずれも赤）。`tests/unit/test_eval_sdk_stats.py` に汎用側の property/端点テストを追補（`rate` 0除算・Jain scale/order 不変・[1/n,1] 包絡・負値 ValueError・makespan/throughput ガード）。

## 実装済（#432 / Part of #430: Tier-1 KPI 拡張 — odom 由来・additive）
- `motion.py`（**新規・rclpy/nav_msgs 非依存の pure helper**）— doc21 §13.2（`docs/architecture/21-eval-sdk-extraction.md:310`）の **odom 由来 Tier-1** を additive 実装。層分担は doc21:178（数学=`eval_sdk.stats` / 指標定義・データ producer=wo）。**新トピック・新契約・新ノード・新 score 送信ゼロ**（`_send_scores` 不変）。
  - `MotionAccumulator` — **既存の `/bot{n}/odom` subscription 内**で `(t,x,y,v)` を robot ごとに ring buffer 保持。`DistanceAccumulator`（全走行の総距離・O(1)）の対。**防御パース**: 非有限フィールドを含む sample と **stamp が進まない sample**（重複 publish / sim time reset / bag loop）は drop（`add` が採否 bool を返す）。`max_samples`(=4096) は**メモリ境界であって domain しきい値ではない**＝要約した窓は毎回 `SmoothnessStats.{samples,window_start,window_end}` で開示。
  - **`v` の出所** = odom メッセージ自身の符号付き線速度 `twist.twist.linear.x`（doc12:340 が `/bot{n}/odom` 由来の `velocity{linear,angular}` を per-robot 線速度の正準とする。`warehouse_state/state_cache.py:108-109` が同一フィールドを読む先例だが **cross-lane import はせず**、documented field を独立に読む＝`kpi.py` の tool 名集合と同じ流儀）。pose 差分の再微分ではない（N_MU は符号反転数＝符号無し speed では常に 0 になる）。
  - `SmoothnessStats` — doc21:306 が名指しする **3 指標のみ**（`sparc` 推奨 / `ldlj` / `n_movement_units`）。**jerk のスカラー化は発明しない**（doc21 に reduction の定義が無く `eval_sdk.stats.jerk` は系列を返す）。spectral 2 指標は |v| profile、N_MU は**符号付き**系列に与える。`sample_rate_hz` = `(n−1)/(t_last−t_first)`（窓の平均サンプリング率＝`eval_sdk.stats.throughput` を再利用して非正 duration ガードを継承）。全 None は「算出不能」であって「測定値 0」ではない（`rate`/`percentile` の慣行）。**fail-open**: numpy 不在（`sparc` の lazy import）や退化窓の raise は `None` へ落とし、KPI がノードを落とさない。
  - `detour_factors(travelled, optimal)` = **pᵢ/lᵢ**（doc21:310 が式を inline で規定）。pᵢ = `DistanceAccumulator.totals()`（全走行）、lᵢ = **外部供給**（doc21:301（データ源 (c)）/ doc21:304 の KNOWN_LOCATIONS+planner oracle は **Phase 3a まで producer 不在**＝doc21:409）→ dev では常に空（`completions` scaffold と同型）。`lᵢ≤0` は事前フィルタ（doc21:304 が同じ lᵢ に課すガード）。**SPL の `max(pᵢ,lᵢ)` クランプは適用しない**（<1.0 は oracle か odom の異常を示す情報であり、クランプは異常を隠す）＝docstring に明記。
  - `kpi.compute_kpis(..., motion=MotionInputs|None)` — `motion=None`（オフライン `kpi_report` CLI の既定）で `KpiReport.{distance_traveled,detour_factors,smoothness}` は空＝**audit 専用呼び出しの挙動・出力は完全に不変**。`to_dict()`/`format_report` も供給時のみ追記（additive）。
  - `kpi_collector` — 既存 odom callback が `MotionAccumulator.add(...)` も呼び、`_report` が `MotionInputs(samples=…, distances=…)` を渡す。`optimal_distances` は**渡さない**（lᵢ producer 不在＝推測しない）。`motion_buffer_samples` param は非正値で既定へ fallback＋`log.warning`（`resolve_pattern_d` の「不明値→安全側＋warning」先例）。
- `tests/unit/test_wo_motion_kpi.py`（新規, live SDK 不使用・`hypothesis` 非依存）— ring buffer（新しい方を残す / stamp 非前進 drop / 非有限 drop）・`sample_rate_hz`（11 sample×0.1s→10Hz の手計算）・|v| 統計・N_MU の符号性・**SPARC の振幅不変性**（doc21:306 の文献不変量＝独立オラクル）・jittery<smooth の順序・全零窓は `None`（0.0 ではない）・fail-open（`sparc` を raise 化）・detour（1.5 の手計算 / <1.0 非クランプ / lᵢ 欠落・非正・非有限のフィルタ / **scale 不変・factor×lᵢ=pᵢ の fixed-seed 反復チェック**）・`compute_kpis` 合成と additive 性。numpy 依存の本数は `importorskip`（既存 `test_eval_sdk_stats.py` の SPARC 3 本と同じ挙動＝CI では skip）。
  - **detour の 2 本の「property」は低情報**（#616 で表現を是正）: `scale 不変` と `factor×lᵢ=pᵢ` は**除算の代数恒等式**であり、300 反復しても 1 反復と検出力は変わらない。実際に落とせる変異は **比の反転（lᵢ/pᵢ）** と **SPL クランプ `max(pᵢ,lᵢ)` の適用**の 2 種で、それ以上を保証するものではない（無差別 property ではない＝「実装非依存だが弱い」と読む）。

## 実装済（#616 / #613 の post-merge 敵対レビュー是正 — 観測面のみ・契約変更なし）
- `eval_sdk.stats.low_pass(signal, window=5)`（**additive・既存関数の挙動/シグネチャ不変**）— `jerk()` が doc21:306 のために内部で使っていた centered moving average（'valid' モード）に公開名を与えただけ。ドメイン側が**同じ**前置フィルタを別の平滑性指標へ掛けられるようにするための seam（数学=eval_sdk の層則 doc21:178 を維持）。
- `motion.smoothness_stats` が **doc21:306「3階微分前に low-pass 必須」を SPARC/LDLJ にも適用**（#613 では未適用＝規範句の取りこぼし）。|v| profile を `low_pass` してから両指標へ渡す。**N_MU は生の符号付き系列のまま**（微分を含まず、平滑は数えるべき反転を消すため）。**値は変わる**（9 sample のジッタ窓で LDLJ −8.34 → −3.06）。幅 `DEFAULT_SMOOTHING_WINDOW=5` は**工学的チューニング値**（doc21 は幅を規定しない）。
- `SmoothnessStats.{smooth_window,filtered_samples}` を additive 追加（`to_dict()` も additive。KPI 出力契約は未凍結＝下記 voids 9）。`filtered_samples` は **spectral 指標に実際に渡った平滑後サンプル数**、`None` = 渡っていない（幅未満／`_MIN_SPECTRAL_SAMPLES` 未満／peak 0）。これにより **numpy 不在の CI でも床の除去変異が赤くなる**（numpy 不在では `sparc`→`ImportError`・`ldlj`→`ValueError` でどちらも `None` になり、値だけでは床の有無を区別できなかった）。
- `motion.resolve_motion_buffer_samples(value) -> (depth, warning|None)`（純関数）— `motion_buffer_samples` param の fallback を `KpiCollector.__init__` から抽出。**live node 無しで unit 到達可能**になり、非正・非整数・非数値・`bool` を既定へ落として warning 文字列を返す（node は log するだけ・fail-open）。
- `sample_rate_hz` docstring の**事実誤りを訂正**: LDLJ は fs で値が動かない（`dur³`·Σ`jerk²`·`dt` が fs を相殺＝fs=1/10/30 で 15 桁一致）。**fs で動くのは SPARC のみ**（`fc`=10Hz が絶対 Hz。同一 profile が fs=10 で −2.55・fs=30 で −1.41）。テストは production 相当 **fs=30** で fs 実効性を pin する（旧 fixture は全て dt=0.1＝fs が効かない領域だった）。
- 引用 re-pin: lᵢ oracle の出所は **doc21:301（データ源 (c)）/ doc21:304**（`doc21:303` は SR の行＝誤引用）。`motion.py` / `kpi_collector.py` / 本書の 4 箇所。
- **doc21 §16 追補（`docs/architecture/21-eval-sdk-extraction.md` は wo 所有）**: ①「新 producer ゼロ」(:189) の読み替え（新**ノード**不要／既存 subscriber 内の bounded 列保持は要る）②velocity 源 = signed twist 採用の明文化 ③low-pass 適用の記録 ④fs の非対称性 ⑤未定義2指標は定義しない。§6 の producer セルは **symbol 名アンカ**へ re-pin（行番号ピンは churn する＝`.claude/rules/session-orchestration.md` §8）。**行ズレ回避のため §6 の 4 行は in-place 置換・新節は末尾 append**（doc21 への inbound pin は :409 まで＝末尾追記は無影響）。

## 前提・未確定 (TODO / 設計の空白＝発明しない・予告して確定)
docs-first.md に従い、未定義は**コードで発明せず**予告 → docs/contract で確定（implementation-and-dependencies §3）:
1. **audit レコードの凍結スキーマが無い** → 防御パースで凌ぐ。`AuditEntry`/`KpiRecord` 凍結は将来 `contract` PR（skeleton #1 / bridge #4）。# TODO(contract)
2. **【#4 接点・要予告】audit 行に `gen_id` が無い**（実プロデューサは executed 行に未記録、stale reject の `received_gen` のみ）。**per-task の live trace 連結には mcp_server が audit 行へ `gen_id` を追加する必要**（#73 合意 point3 が前提とする）。`AuditEntry.gen_id` は **`detail.gen_id` のみ**読む（stale reject の `received_gen`=却下された古い世代は trace seed に**使わない**。doc13:519 は executed gen を join key とする）→ 追加まで `gen_id`=None → trace_id None → 送信 no-op（graceful）。# TODO(coordinate #4/#73)
3. **task_completion_time の live 完了源**（Nav2 goal-reached）= Phase 3（nav-traffic #8 / bridge #4）。# TODO(Phase 3)
4. ✅ **新規 score 名 docs 凍結済（#88 wo-metrics docs PR）**: `collision_free`(BOOLEAN)/`replans`(NUMERIC)/`mean_decision_latency`(NUMERIC) = doc08 §比較計測の追加設計、Mode A `negotiation_rounds`(NUMERIC)/`agreement_reached`(BOOLEAN) = doc14 §交渉スコア（演出専用・Phase4 比較対象外）。`deadlock`(NUMERIC, 検出回数/run) は信号源 **#55/#128 で確定**（doc08a:271-281＝2台 idle+`current_task`+距離<0.4m+heading>2.5rad、:281 で再基礎付け）。`collision_free`/`replans`/`negotiation_rounds`/`deadlock` は Phase依存・暫定（live 計測は実機/sim）。**live 送信実装は Phase 3-4**。# TODO(Phase 3-4 実装)
5. **result score の値マッピング**（audit `executed/rejected/error` → score `"success"` 等）が未定義（doc08:341 例示）。node は `result` を自動送信しない（値語彙が docs 未定）。# TODO(docs)
6. **cancelled 除外の正準定義**が audit 単位で未定（doc08:250 は Langfuse trace status）。本実装は解釈。# TODO(docs)
7. **efficiency の live 値**は robots/sim 稼働が要る（Phase 3）。積算器+送信路は実装済・inert。# TODO(Phase 3)
8. ✅ **Grok オフライン cost = 実装済**（#133 `grok_cost.py` pure helper + #88 wo-metrics で current-public 価格表 `GROK_PRICE_TABLE_2026_06_12` を additive 追加）＝`usage_details`×versioned 静的価格→USD（live SDK 非依存）。**Phase 3 で実機確認する exact 項目（wo 側・ここに一元化。正本 doc13 §7.5② :520 / doc08:508,510）**:
   - **① literal `model` 文字列** — Hermes が Grok に転送する実文字列（`grok-4` / `grok-4-0709` / `grok-4.3` / `grok-4.20-*` のどれか）。`resolve_grok_price` の prefix キー解決の正否を決める。**推測で固定しない**（doc08:510）。
   - **② `usage_details` キーの実形** — input/output トークン数キーが `grok_cost._INPUT/_OUTPUT_TOKEN_KEYS`（`input`/`input_tokens`/`prompt_tokens` 等）の防御パース対象のどれで届くか（doc08:508）。
   - **③ Langfuse v4 価格フィールド形** — `prices:{input,output}` ネスト vs flat `input_price`/`output_price`（doc08:510）。カスタムモデル価格登録（UI/API・doc08:506）時の form。
   - **④ `grok-*` の `generation.cost_details.total > 0`** — 4社とも cost≠0・比較可能を実 Langfuse 4.9.x で assert（doc08:508 / doc13:520②）。
   検証手段＝実 Langfuse 4.9.x + Hermes + 4provider キー（課金）= **human gate**（harness `spike/langfuse/`）。`GROK_PRICE_TABLE_2026_06_12` は current-public 値だが ①〜③ 未確認のため **「検証済み」とは称さない**（注入優先・`resolve_grok_price` default 不変）。Langfuse カスタムモデル価格**登録**（doc08:506）= Phase 3。**Metrics API + Datasets/Experiments**（12構成×KPI、doc13:506 / doc08:510-518）= **Phase 4**。`SCORE_*` 予約は実装済・inert（producer 未配線＝live send は Phase 3-4）。# TODO(Phase 3 live verify ①〜④ / Phase 4 集計)
9. **KPI 出力契約が無い**（Langfuse 以外の出力先/形）。`to_dict()` は lane-internal。# TODO(docs/contract)
10. ✅ **doc06 KPI リスト矛盾 解消済（#88）**: 06:275 交通モード軸が `deadlock`/`collision_free`/`replans` を明示し doc08 §比較計測の追加設計 を正本参照、LLM 公平性比較軸（06:276）と軸分離を明記。
11. **Tier-1 の定義が doc21 で未規定な 3 点（#432 で暫定採用・docs 追記を follow-up 起票）**: ① **介入率の分母** — `docs/architecture/21-eval-sdk-extraction.md:184` は分子（`escalation_response` 行）のみ規定。暫定 = 決定された COMMAND_TOOLS 行数（`acceptance_rate` と同分母）。② **command rejection 率に error を含めるか** — doc21:310 は名前のみ。暫定 = `1−acceptance_rate`（error/other を含む補数）。③ **fairness の負荷量と throughput の観測窓** — doc21:186 は `by_robot` を、:185 は「完了 task」を指すだけで、どの tally / どの窓かは未規定。暫定 = executed 件数 / 完了記録の span。**いずれも report フィールドのみで、score 送信も契約凍結もしていない**（差し替えは非破壊）。# TODO(docs: doc21 追記)
12. **doc21 §6 の producer 行が現コードとドリフト（#432 で発見・doc 修正は別 PR）**: `21-eval-sdk-extraction.md:187` は jerk の producer を `kpi.py:97-140` とするが、当該範囲に odom コードは**1行も無い**（現 `kpi.py` の同域は `ResultTally`/`CompletionRecord`）。`:186` の fairness `kpi.py:315-316` も同様にドリフト（現 `by_robot` 集計は `compute_kpis` 内）。**#432 review 追補**: 本 slice が最も依存する `:184` 自身も同様で、「executed 集合 `kpi.py:62`」は origin/main の `kpi.py:62` が `# The 7 MCP tool names (…)` というコメント見出し（`COMMAND_TOOLS` 定義は :63-70・executed 判定は `compute_kpis` 内）、同行の producer `tools.py:354-388` も executed 記帳行（`:408`）を含まない範囲＝**表の 4 行すべてが要 re-pin**。実体として odom 系 Tier-1 は **pose/velocity バッファという新 producer が要る**ため、doc21:189「新 producer ゼロ」は **audit 系にのみ成立**。**【#432 odom slice 追補】** 保持側は `motion.MotionAccumulator` で解決した（**新ノード/新トピック/新契約ではなく、既存 subscription 内の bounded buffer**）ので doc21:189 との矛盾は「新 *producer node* は不要／ただし既存 consumer に列保持の追加が要る」と読み替えれば解消する。✅ **#616 で解消**: doc21 §6 の producer セルを **symbol 名アンカ**へ re-pin（`tools.py` tool6 の executed 記帳 / `kpi.py` の `COMMAND_TOOLS` / `compute_kpis`+`robot_load_fairness` / `motion.py` の `MotionAccumulator`+`smoothness_stats`）し、:189 を「**新ノード不要／既存 subscriber 内の列保持は要る**」へ改め、経緯と読み替えを **doc21 §16 ①** に記録した（行ズレ回避のため in-place 置換＋末尾 append）。
13. **本 slice が下流の doc 行参照を割った（境界外＝本レーンでは直せない）**: `docs/architecture/22-web-observability.md:400` が `kpi.py:185,199,307` を pin していたが、本 slice の追記で `class KpiReport`=:220 / `acceptance_rate` フィールド=:234 / `acceptance = rate(...)` 計算=:384 へ移動（新規 `robot_load_fairness`=:300）＝**行番号ではなくシンボルで再ピンするのが望ましい**。`docs/**` は編集境界外。`scripts/check_consistency.py` の `B4-doc-line-ref` は **blank 行しか見ない**ため、行が非 blank のまま意味だけズレるこの型（#165 型）は **機械ゲートが緑のまま素通しする**。# TODO(docs: doc22 §参照表 re-pin ＋ check_consistency の symbol-drift 検出は別途検討)
14. **fairness の入力 `by_robot` は tool 非フィルタ**: `compute_kpis` は `robot` フィールドを持つ**全行**を `by_robot` に積む（`COMMAND_TOOLS` で絞らない）。現状は producer 側で `robot` を書くのが command 系だけなので結果は一致するが、audit のレコード形は**凍結契約ではない**（`audit_reader.py:3-8`）ため、将来 read-only tool が `robot` を書き始めると fairness に無言で混入する。docstring に caveat を明記（command 限定 tally の追加は本 slice の additive 範囲外）。# TODO(follow-up: command 限定 by_robot が要るか)

15. **doc21:310 の Tier-1 のうち 2 項目は「名前しか無い」＝実装しない（#432 odom slice で確認）**: **`idle 率`** と **`速度予算消化率`** は `docs/architecture/21-eval-sdk-extraction.md:310` に列挙されるだけで、`docs/` 全体を grep しても分子・分母・しきい値・観測窓のいずれも定義が無い（`grep -rn "速度予算\|消化率\|detour" docs/` のヒットは :310 のみ）。idle は「停止」の判定に task lifecycle 源（audit は**発行のみ**記録）か速度 ε（`warehouse_state.aggregator.derive_status` = 別トラック所有の定数）を要し、速度予算消化率は平均速/上限・上限滞在時間比・∫|v|dt/(v_max·T) のどれとも読める。**推測で式を置かず未実装**とし、代わりに測定済みの材料（`SmoothnessStats.{mean_speed,max_speed}`・`sample_rate_hz`・窓境界）を report に出して、doc21 で定義が確定した後に**スキーマ変更なしで**導出できるようにした。# TODO(docs: doc21 §13.2 に idle 率 / 速度予算消化率 の分子・分母・窓を追記)
16. **odom Tier-1 の観測窓は「直近 `motion_buffer_samples` 件」であり run 全体ではない（#432 odom slice）**: `distance_traveled`(pᵢ) と `detour_factors` は `DistanceAccumulator` 由来＝**run 全体**、`smoothness` は ring buffer 由来＝**直近の窓**。同じ report 内で 2 つの時間スコープが同居する。doc21 は Tier-1 の観測窓を規定していない（voids 11 ③ の throughput 窓と同じ空白）ため、窓境界を `SmoothnessStats` に出して開示するに留めた。加えて `sparc`/`ldlj` は**一様サンプリングを仮定**するが odom は jitter するため、窓の平均レートを `fs` としている（残差＝doc21 に「非一様 odom の扱い」が無い）。# TODO(docs: Tier-1 の観測窓と非一様サンプリングの扱いを doc21 に追記)

> slice 1 (#69) が `main()` スタブを実装で置換、slice 2 (#73) が Langfuse v4 実配線 + trace_id 導出 + efficiency を追加。
