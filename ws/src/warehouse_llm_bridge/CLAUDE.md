# warehouse_llm_bridge — 司令官LLMサイクル・排他制御(A+B-3+C)・キャラLLM

- **担当トラック / ブランチ**: bridge / `feat/llm-bridge`
- **Phase**: 0.5→3
- **ビルド**: ament_python
- **ノード**: llm_bridge, character_llm（Slice 2 キャラLLM交渉レイヤ, doc14）
- **編集境界**: このパッケージ配下のみ。共有契約 `warehouse_interfaces` は変更不可（`.claude/rules/parallel-workflow.md` §4）。**`warehouse_mcp_server` は同一トラック（doc16 §9 = `16-...:183-192`: `feat/llm-bridge` が両 pkg を所有）なので import 可**。CI cross-import check は track-aware（#81）＝他トラック内部のみ禁止。`main()` が実 `WarehouseTools().dispatch` を executor seam に注入する（S2-PR2 HALF B）。`warehouse_nav2_bridge`（同トラック）は REST 越し（httpx）に呼ぶのみ＝Python import しない。
- **Mode X-ER 設計提案 / XER1 進捗**: docs/mode-x-er では本パッケージの責務を **Robotics Bridge** として拡張する方針。Gemini Robotics-ER の音声/画像/state 入力を `RoboticsPlan draft` に正規化し、Validator / Visual Resolver / Task Graph Executor / Command Compiler を経て既存 `Command`→`action_map`→MCP 経路へ落とす。**XER1/G0（#337）で L4 `robotics/` ＋ L3 `robotics_planning_core/` の offline seam を着地**（ER adapter seam + L4 入力 hygiene + L4→L3 transport 等価正規化 + L3 Handoff 受理ゲート L3H-G0/G1 + observation-only enum）。Validator 以降（XER2-5）と config / ROS topic / `warehouse_interfaces` schema は依然 **未実装・未凍結**で発明しない（昇格は doc06 §1 のゲート後）。ER は Jetson/Nav2/ROS endpoint を直接呼ばない。X-lite は Nav2 Bridge REST、X-rmf は Open-RMF Task API/Fleet Adapter へ分岐する設計候補（正本: `docs/mode-x-er/README.md`, `docs/mode-x-er/01-architecture-and-flow.md`, `docs/mode-x-er/02-l3-planning-core.md`, `docs/mode-x-er/03-er-adapter-skeleton.md`, `docs/mode-x-er/06-unfrozen-contract-resolutions.md`）。
- **Mode X-ER docs 構成**: `docs/mode-x-er/README.md`=位置づけ・正本 file map・未凍結事項 / `01-architecture-and-flow.md`=L4→L3→L2→L1/L0 data flow と X-lite/X-rmf / `02-l3-planning-core.md`=Validator・Visual Resolver・Task Graph Executor・Command Compiler / `03-er-adapter-skeleton.md`=Gemini Robotics-ER 単体 adapter seam・integration gates。旧 `docs/mode-x/` は互換参照で、ER-only の新規判断は `docs/mode-x-er/` を正本にする。
- **Mode X-ER-VLA 設計提案**: Gemini Robotics-ER と OpenVLA などを統合し、VLA が L3 の一部を補助・代行する設計は `docs/mode-x-er-vla/` を正本にする。VLA でも direct actuation は不可で、MCP / Policy Gate / Nav2 / Open-RMF / Layer0 safety を bypass しない。現時点では調査・spike 用 docs であり、config / topic / schema を発明しない。

## モジュール構成（S1 = #4 司令官サイクル / S2-PR1 = Langfuse trace 所有 + Mode C）
- `llm_client.py` — `LLMClient`（ABC, `async decide(situation)->dict`）+ `LLMUnavailableError`（接続/HTTP 障害→Nav2-only; doc08:288-292）。
- `hermes_client.py` — `HermesClient(LLMClient)`：**`from langfuse.openai import AsyncOpenAI`**（base_url=Hermes/v1, doc13§5.1/doc15:30-44, langfuse+openai 遅延 import）で generation を Bridge 所有 trace 下にネスト。`parse_command_content`（content→Command JSON, 純）/ `parse_command`（dict 形）, 不正は ValueError=doc08:293。provider 切替は Hermes 側 `active_provider`（model="hermes-agent" 固定, doc13:171）。**`build_system_prompt(mode)`（#181, mode-aware）**: Mode A/B（none/simple）は base＋`MODE_A_RULES`（per-bot タスク割当＋doc08a:316-334 のデッドロック3条件+yield解消手順。閾値 0.4m/2.5rad は **(b) docs 例示** doc08a:278-279＝config 未搭載・凍結契約でない）、Mode C（open-rmf）は **standalone `MODE_C_PROMPT`**（doc08c:138-180 忠実＝戦略判断のみ・3段バッテリ doc08c:155-158・`traffic.escalation` gate＋`escalation.id` doc08c:160・robot 選択はアロケーター委譲 doc08c:154「robot 指定なし」・アクション `navigate|stop|charge` 限定 doc08c:136,176。base とは action 集合/衝突回避の役割が矛盾するため append でなく**置換**（battery は base/Mode A/C とも 3段＝doc08a:246-249/doc08c:155-158。base の旧2段→3段 reconcile 済）。action 制限は凍結 `CommandAction` の **strict subset**＝プロンプトのみで制限・schema 不変。座標は逐語コピーせず situation.warehouse.layout 参照＝drift 防止）。**base は mode-neutral**＝出力契約＋バッテリー＋gen_id B-3 注記のみ（gen_id は action_map が注入＝model-b なので advisory, #41/#54）。**per-bot 割当を base に置くと Mode C と矛盾するため `MODE_A_RULES` 側へ**。**直書き定数＝Langfuse Prompt Management の「障害時フォールバック生成元」へ役割降格（`prompts.py` 参照）**＝`llm_bridge` は `resolve_commander_prompt(mode,cfg)` 経由で system prompt を注入。`HermesClient` は optional `langfuse_prompt=` を受け取り `decide()` で generation に紐付ける（fail-open）。
- `prompts.py` — **Langfuse Prompt Management provider（doc08 §Langfuse Prompt Management 方針）**。`resolve_commander_prompt(mode,cfg)→ResolvedPrompt(text,name,langfuse_prompt,version,is_fallback)`（`name`/`version` は trace タグ用＝どの prompt を使ったか・mode を内包）：`hermes.prompts.source` が `langfuse` なら `get_client().get_prompt(name,label,fallback,cache_ttl).prompt` を**起動時1回**取得（実送信単位 2本＝`warehouse-commander-mode-ab`/`-mode-c`・版更新は**ノード再起動**で反映）、`code` or 取得失敗（langfuse 未導入/not-found/auth/network/malformed config いずれも）で `commander_fallback_text(mode)=build_system_prompt(mode)` へ **fail-open 縮退**（doc08:333・**never raise**＝`_prompts_config`/`names` は isinstance ガードで非dict config も安全）。langfuse は `_get_client()` 越しに lazy（pip extra・tests は patch）。`langfuse_prompt` は generation 紐付け用（fallback 時 None）。
- `seed_prompts.py` — **Langfuse へ司令官 prompt を upsert（`python -m warehouse_llm_bridge.seed_prompts`・既定 `--dry-run`・`--commit` で実 upsert）**。本文は `commander_fallback_text`（=doc08a/08c 由来 fallback 定数）から生成＝seed とコード fallback が同一出所で drift しない。`labels=["production"]`（公平性 pin）・`config={model,temperature,traffic_modes}`（**self-describing**: mode-ab→[none,simple] / mode-c→[open-rmf]＝prompt 単体でどの mode 用か分かる）。live upsert は `LANGFUSE_*` 認証＝人間ゲート（Phase3 #88）。
- `tracing.py` — **S2-PR1: Bridge-owned Langfuse trace（Pattern A, doc08:354-356）**。`Tracer`(ABC)/`NoopTracer`(既定・tests)/`LangfuseTracer`(lazy・**fail-open**)。`build_session_id`=`run_{mode}_{provider}_{scenario}_{ts}`（=Langfuse **session id**＝表示ラベル）・`trace_seed(run_id,gen_id)`=`f"{run_id}:{gen_id}"`（決定的→#6 が `create_trace_id(seed)` で同一 id 導出 doc13:519(b)）・**`resolve_run_id`=trace seed の run_id は `WAREHOUSE_RUN_ID` env 優先（#6 と同一ソース）・blank 時のみ session_id に fallback（#108）**。v4 OTEL API は **Phase 3 実機 verify**（doc13:520）。
- `situation.py` — `SituationBuilder`（**mode-aware**）：`StateSnapshot`(state.json)→`Situation`。Mode A/B は `predicted_position_3s`(**CTRV: 等速・等旋回, `velocity.angular` 使用・ω≈0 で CV 縮退**, doc08a:97-111, #101)+`obstacle_ahead`(`<emergency_min_distance`=0.3, doc08a:95)含む全フィールド。**Mode C(open-rmf) は velocity/heading/predicted/obstacle_* を構築せず `model_dump(by_alias=True, exclude_unset=True)` で省略**（~200tok, doc08c:88,108。`=None` 代入では落ちない＝未構築が要。`by_alias` は `PendingTask.from` 用＝唯一の凍結 alias なので他フィールドは不変）。**`current_task` は両モードで Bridge 所有の追跡値**（`build(current_tasks=bot→destination)`、scheduler が充填、snapshot 非搭載 doc12:337 / 08a:62,73,466。値は **destination 単体**＝doc 例の起点→終点は illustrative。未マップ bot=None=idle）。`pending_tasks`（`{id,from,to}`＝凍結 `PendingTask`）は **デモ用シード `WAREHOUSE_TASKS` env → scheduler キュー経由で供給**（恒久 producer は未確定・将来 #6。08a:468/#181。未設定＝`[]`＝非デモ run 不変）。
- `action_map.py` — Command→ToolCall（`gen_id` 注入 + per-call `idempotency_key` mint。既存・#27/#41）。
- `executor.py` — `ToolExecutor`(ABC) / `DispatchToolExecutor`(注入された `async dispatch(name,args)` をラップ) / `RecordingToolExecutor`(fake)。**MCP 依存をここで遮断**。
- `scheduler.py` — `BridgeScheduler`（pure async, 単一グローバルループ doc08:250-252）：gen++ → `gen_store.set`（B-3 publish, cycle 先頭）→ situation → **`async with tracer.turn(gen)`** で `wait_for(decide, 2.5s)`（A client-side cancel, doc08:140）→ action_map→**`tracer.tool_span` 下で** executor dispatch。fallback: timeout=前回継続 / 連続=Nav2-only（doc08:286,141）。**#192**: `decide()` 由来の非JSON/散文ラップ `ValueError` / `TypeError` も `Command.model_validate` 由来の schema 不正と同じ `_on_invalid_response` に流し、cycle ignore（0 forward・loop 生存・Nav2-only 不遷移）とする。**短期記憶（#102）**: 受理 dispatch(`status=="ok"`)から **`current_task`**(bot→destination, **set-on-accept/clear-on-stop 方針**＝navigate/yield/charge で set・stop で clear・wait は据置。格納値=destination=`_dropoffs` 相当で `active_tasks` の task_id とは別物・1:1 mirror ではない)+**`history`**(有界 `deque(maxlen=5)`, `<bot> <action> <target>`/result, 08a:82-85)を保持し次 situation へ供給。deadlock pattern-2(同一 navigate 反復 × idle 持続, 08a:296-305)/pattern-1(idle×goal保持×近接×対向, 08a:277-279)の判定に必要な history+current_task 配管を供給する（#55 で `result:"blocked"` 依存は撤廃＝dispatch 戻り値は ok/rejected/error のみ・非進捗は `status=="idle"` で判断）。**タスク注入（#181）**: `parse_seed_tasks(WAREHOUSE_TASKS)` の検証済みシードで `pending_tasks` キューを保持→毎 build に供給→受理 navigate（`to`==destination）で `_consume_pending_task` が消費。これが current_task chicken-and-egg を解消し head-on デッドロックの前提（両 bot `current_task!=null`）を作る（08a:277/468）。**サイクル長（config 駆動）**: `resolve_cycle_wait(cfg,mode)`（純）が `cfg["cycle"].{mode_a_seconds,mode_c_seconds}`＝総サイクル長を `cycle_wait_sec=総−応答(EXPECTED_RESPONSE_SEC=2.0)` に変換（doc08:125-128・欠落/不正/非正は `CYCLE_WAIT_SEC` code 既定 1.0/3.0s へ fail-open）。dev は Mode A=120→待機118s（~2分スパン）。**待機は2段**: `run_forever` が `run_cycle` の bool（生産=command dispatch 成立）で選び、生産 cycle 後のみ長 cadence、非生産（no-snapshot/timeout/outage/invalid）は短い `RETRY_WAIT_SEC=1s` で再試行＝cold-start 起動・outage 検出(5s窓)を長 cadence で遅らせない（PR#297 review 2 MAJOR 修正）。**outage 判定は時間基準**: `OUTAGE_NO_RESPONSE_SEC=5.0` を `__init__` で `ceil(5/cycle_timeout)=2` 連続タイムアウトに変換＝`cycle_wait` 非依存（旧 `OUTAGE_AFTER_CONSECUTIVE` の暗黙 1s 待機前提を撤廃）。`nav2_only` は可観測フラグ。
- `fairness.py` — **Phase 4 比較公平性ガード（#103, doc08 §比較の公平性）**：`resolve_memory_policy(cfg)`→`MemoryPolicy(memory_enabled,skills_enabled,comparison_run)`（`hermes.*`、既定 OFF）/ `assert_fairness`（`comparison_run` 下で memory/skills の ON 宣言を**独立**チェック=#44 流→`FairnessViolationError` で**起動 abort**）/ `fairness_log_line`（公平性ガードのログ行）。**純・unit テスト済**（ROS/Hermes 非依存）。**intent ガードであって enforcement ではない**: Bridge↔Hermes はステートレスで Hermes の memory 状態を制御・確認不可。実 OFF は Hermes config 側が権威（doc13 §OFF 機構）。
- `persona.py` / `negotiation.py` — **キャラLLM交渉オフラインコア（Slice 1, doc14 §交渉モード）**。`persona.py`=`build_character_prompt`（doc14:148-155）+`parse_turn`（raw→`TurnResult`・JSON lenient=非JSON/非objは speech-only・doc08:293 spirit）+`Persona` Protocol（`async speak(bot_id,prompt)->str`）。`negotiation.py`=`NegotiationEngine.run(ctx,persona,*,abort,abort_event)`：バトンパス・starter 先手・厳密交互・各bot≤4/計≤8（doc14:60）・8秒timeout（doc14:89、`persona.speak` 中も deadline guard）・abort 即中断＋proposal 破棄（doc14:90,141、turn 境界/発話生成中とも late output を parse しない）・合意時のみ `AgreedAction.model_validate`→`Proposal` 組立（gen_id を `/negotiation/start` から刻印 doc14:70,142）。`accept_proposal`=司令官側 gen±2 受理ゲート（doc14:142・純関数）。**純 async・ROS/network/LLM 非依存**（`scheduler.py` 同方針）・**executor/action_map/nav2_client を import しない＝構造的 no-actuation**（doc14:136・#4 精神・unit で固定）。閾値（8/4/8s/±2）は注入（doc14 **例示値**＝凍結契約でない・config 化は Slice 2）。1ノード2ペルソナ=doc14:188（net-zero 注記）。**Slice 2 追加（additive）**: `negotiation.py` に `on_turn(line,turn_no,next)` live publish フック（doc14:76,79）/ `persona.py` に `ScriptedPersona`/`default_offline_script`（Hermes 不在時の決定論 offline fallback・live persona は Slice 3 doc14:173）。
- `negotiation_messages.py`（Slice 2・純）— `/negotiation/{start,turn,proposal,abort}`・`/character/speech`（doc14:200-211）の JSON 封筒 encode/decode（lenient）。`Proposal` のみ凍結契約（schemas.py:190-195）・他封筒は wire 形（凍結契約外）。`decode_snapshot_bots`=`/state_cache/snapshot`→`bot→state`。mcp_server→llm_bridge 循環回避で start 封筒は dict 受け渡し。
- `conversation_events.py` — Mode A v1/v1.5 の内部 event vocabulary。`ConversationEvent` / `CandidateAction` / `TaskLifecycleEvent` / `ConversationEventLog` を持ち、`conversation_events.jsonl` を読み書きする。凍結 `warehouse_interfaces` でも ROS topic でもない。
- `self_action_gate.py` — Mode A v1 Self-Action Gate。`wait_self`→既存 `dispatch_task(action="wait")`、`yield_to_retreat_A/B`→既存 `dispatch_task(action="yield", dropoff=retreat_A/B)`、`release_route_lock`→owner 確認付き内部 lock 解放。任意 destination / raw 座標 / 他 bot target は reject。`wait_self` 初期上限は 5s。
- `character_session.py`（Slice 2 core・rclpy 非依存）— `run_negotiation_session(...)`＝`engine.run` 駆動→`on_turn` で `/character/speech`+`/negotiation/turn` 逐次 publish→合意時のみ `/negotiation/proposal`（doc14:65-93）。両ペルソナ不在 snapshot は clean NO_AGREEMENT。**no-actuation**。host unit で全 episode 検証。
- `character_node.py`（rclpy ノード `character_llm`・Slice 2）— `/negotiation/start`・`/negotiation/abort`・`/state_cache/snapshot`・`/llm/reasoning` subscribe→start 毎に asyncio ループで session 駆動。**1 episode/trigger**（overlapping start drop）・**per-episode `threading.Event` abort**（後続 start が走行中 abort を un-set しない doc14:141,239-247）。**publish-only=no-actuation**。offline=`ScriptedPersona`。
- `llm_bridge.py` — rclpy ノード（薄い adapter）：`/llm/reasoning`・`/llm/command` publish、session_id+`LangfuseTracer` を構築、mode を builder に注入、scheduler を asyncio スレッドで駆動。**起動時に `assert_fairness`（#103 公平性ガード）を実行**（`comparison_run` × memory/skills ON 宣言なら fail-closed で起動失敗）。**tool dispatch = 実 `WarehouseTools().dispatch`**（同一 `gen_store`/`state_store` を共有 → B-3 と Policy Gate が end-to-end で効く）。Mode A/B（`traffic_mode` none/simple）は受理された motion tool を **Nav2 Bridge REST（`:8645`）へ forward**（`Nav2RestForwarder` を注入）。Mode C（open-rmf）は Open-RMF 経由なので forwarder=None（doc15:211-219）。S2-PR2 HALF B。**キャラLLM配線（Slice 2）**: `/negotiation/start` publisher を `WarehouseTools(negotiation_starter=…)` に注入＋`/negotiation/proposal` subscribe→`scheduler.set_negotiation_proposal`。**司令官 scheduler（Slice 2）**: `Command.start_negotiation`（additive 契約）→`_maybe_start_negotiation`→`action_map.start_negotiation_tool_call`→executor→`tools.start_negotiation`→`/negotiation/start`（5モーションaction だけだと到達不能だった穴を解消）。proposal 取込: `set_negotiation_proposal`→次 cycle `accept_proposal`(gen±2)通過時のみ situation dict に `negotiation_proposal` 注入（凍結 `Situation` 不変・extra="ignore"）。
- **Mode X-ER XER1/G0（#337）= L4/L3 を別ディレクトリに分離**（doc01:99 で L4=llm_bridge 拡張のため両者を本 pkg 内に置くが、**L3 box 名を中身と一致**させる）。依存は一方向 **L4 `robotics/` → L3 `robotics_planning_core/`**（L3 は L4 を import しない＝独立再利用可・productization/01:5）。両者 ROS 非依存・凍結 `warehouse_interfaces` のみ consume。
  - **`robotics/`（L4 Robotics Bridge）**: `er_task.py`=`ErTaskRequest`（**ER に送る入力 bundle**・03:37-53。validator は**L4 入力 hygiene**＝`known_locations`⊂`KNOWN_LOCATIONS`/`allowed_actions`⊂`CommandAction`/`output_contract`∈`SUPPORTED_PLAN_VERSIONS`。**L3 Validator〔出力検証・XER2〕とは別物**・productization/06:162-164）。`adapters/`=`GeminiErAdapter`(03:24-33・offline seam のみ・live transport は #344 PROBE まで `NotImplementedError`)＋`enums.py`(`ProviderType`/`Transport`＝**observation-only**・実行分岐に使わない doc03:75/doc06 §2)。
  - **`robotics/transport.py`（L4 ER 音声 transport 選択・純・offline）**: `resolve_audio_transport(er_gateway_cfg)→Transport`＝`config.robotics.er_gateway.{base_url,audio_input_audio_supported}` から音声レグの wire を解決。`base_url` 非空 **かつ** `audio_input_audio_supported is True` の時だけ `Transport.HERMES`（forked `input_audio` gateway 経由）・それ以外（None/欠落/非 Mapping/空 base_url/flag false/truthy 非 bool）は全て `Transport.DIRECT`＝**恒久 fail-safe fallback**（PR #355 / doc06 §5 補遺:269 / `deploy/hermes/er-audio-fork/TRANSPORT-FLIP-PLAN.md` §2.1,§3.1）。既定 config 両キー OFF ゆえ挙動不変。`Transport` は **observation-only 監査タグ**（doc03:75）＝選択のみで dispatch/socket/分岐 key ではない。**live 音声 transport 本体は #344 未実装**（`gemini_er.propose_plan` の live 経路は `NotImplementedError`）＝本 module は intent 決定のみ（live seam と Langfuse タグが同一出所を参照するため）。
  - **`robotics/adapter_factory.py`（L4 ER adapter 生産 wiring・純構築・offline）**: `build_er_adapter(cfg, *, sender=None, load_blob=None, env=None)→GeminiErAdapter`＝main に足りなかった「config→稼働可能な adapter」の組立 seam（`resolve_audio_transport` は wire 選択のみ・`GeminiErAdapter` は送信のみで、両者を実 HTTP sender と束ねる production 経路が不在だった）。`cfg["robotics"]["er_gateway"]`（欠落/非 Mapping は各段で fail-safe）を `resolve_audio_transport` に渡し、解決 transport で `GeminiErAdapter(transport=…, sender=HttpErTransportSender(…), load_blob=…)` を構築。**HERMES 解決時のみ** sender に config の `hermes_base_url`（:8644）＋ gateway bearer を配線、**DIRECT 解決時**は gemini_key のみ（fallback 用）。secrets は **env からのみ**（`GEMINI_API_KEY`/`GOOGLE_API_KEY`＝live test:46・`HERMES_API_KEY`/`API_SERVER_KEY`＝llm_bridge.py:123／.env.example:5-7・commit 厳禁 safety.md）。**構築のみ・I/O/課金呼び出しなし**（実 send は `HttpErTransportSender.send` の `WAREHOUSE_LIVE_ER=1` cost gate 越しのみ）。`sender`/`load_blob`/`env` は **fake-first 注入可**＝network/filesystem 無しで unit テスト可。**Transport 昇格は enum 既定を変えず config でフリップ**（enums.py 既定 `DIRECT` 不変・doc03:75）。**稼働 ROS ノード配線＋実 filesystem `BlobLoader` 注入は別 XER6 cycle**＝今 round は constructible factory + offline unit のみ（`load_blob` 既定 None＝blob part 省略）。
  - **`robotics/composition/`（productization S2・run manifest 構成 seam・純 offline）**: `manifest.py`=bridge-local `run_manifest.v1` pydantic schema（doc09:42-135 の proposal 例を v1 昇格・未知 `schema_version`/未知キー/重複 plugin id/emitter 矛盾/不正 run_id を **fail-closed reject**・`profile_version`/`profile_content_hash` は S3 レーン契約の optional 欄＝hash 算出は S3 所有）。`loader.py`=YAML→`RunManifest`（validation error は握り潰さず raise）。`preflight.py`=**fail-closed composition preflight**（`preflight_composition(manifest, registry)`：宣言 plugin 集合 == 登録 hookimpl 集合でなければ `CompositionError` で起動拒否＝「pluggy hook が登録ゼロでも [] を返し plugin 不在が fail-open」穴を封鎖。registry は最小 Protocol `PluginRegistryView.registered_plugin_ids()->set[str]`＝S4 pluggy レーン非依存・S4 は `pm.register(impl, name=<manifest plugin id>)` で満たす契約）。`record.py`=**effective-composition witness**（`build_effective_composition` が構築済みオブジェクト実体の `type()` から class/module 名を導出し manifest/preflight と突合＝recorded==ran を構造保証、`write_run_artifacts` が `out/runs/<run_id>/{manifest.yaml,effective_composition.json}` を書く。doc09:48,370・out/runs/ は .gitignore 済）。`fixtures.py`=Mode A expressibility fixture（Q4 probe・表現可を実証・taxonomy gap F1-F4 を記録）。凍結契約不変（doc09:8）・actuation/network なし。
  - **`robotics_planning_core/`（L3 Planning Core・provider 非依存）**: `models/boundary.py`=`RawModelOutput`（L3 入力境界契約）＋`SUPPORTED_PLAN_VERSIONS`。`models/robotics_plan_draft.py`=`RoboticsPlanDraft`/`Detection`/`TaskNode`/`InputRefs`（`extra="ignore"`・**warehouse_interfaces 昇格しない**＝doc06 §1。`schema_version` のみ pin〔unknown_schema_version・06:158〕・`TaskNode.action` は free `str`＝未知 action を XER2 Validator の `UNKNOWN_ACTION` まで通す doc02:77,103）。`handoff.py`=`to_robotics_plan_draft(RawModelOutput)`＝**L3 Handoff seam**: ①**transport 等価正規化**(hermes〔OpenAI choices〕/direct〔Gemini candidates〕→同一 `RoboticsPlanDraft`・README:86/01:167) ②**fail-closed 受理ゲート L3H-G0/G1**(productization/06:155,158,160): endpoint=`forbidden_endpoint`/velocity・motor=`low_level_action_present`(**drop でなく reject**)/座標 goal=`coordinate_goal_unfrozen`/版欠落不一致=`missing_required_field`・`unknown_schema_version` を**全て reject**(`extra="ignore"` で危険語彙を黙殺しない)。**この受理ゲートは dict の KEY 名のみを再帰走査する構造ゲート**＝危険 intent を benign key の VALUE に埋めた場合(例 `target="0.4,0.2"`・`action="set_velocity"`)は通過する。VALUE 側の意味検証(`target`∈`detections[].id`|known location)は **XER2 L3 Validator(`docs/mode-x-er/02-l3-planning-core.md:78`)の責務＝landed**(`validator/`・#338 CLOSED)。Handoff の下流に実行経路が無く 0 actuation なので安全＝凍結 XER2 設計に先行して VALUE 検証を実装しない(docs-first)。`fixtures/`=red/blue 順序プラン(01:134-151)＋各ゲート fixture。Validator/Visual Resolver/Task Graph/Command Compiler の**4 stage は bridge-local offline core landed＝XER2-5**（`pipeline.py`/ROS 未配線＝XER6・`command_compiler/`=#341）。**no-actuation**: Command Compiler は凍結 `Command` オブジェクトを生成するが dispatch は下流（Bridge→action_map→MCP）＝L3 自体は motion/tool を一切 emit しない純データ変換。

## 提供 (produce)
- topic: `/llm/reasoning` (std_msgs/String, 表示用) / `/llm/command` (std_msgs/String JSON, ログ用)（doc08:430-431, doc16§3）
- file/state: `GenStore.set(current_gen)`（B-3, cycle 毎。MCP と共有 `/tmp/warehouse/gen_store`）
- per-call `idempotency_key`（C, action_map が mint。MCP が `check_and_add` で replay reject）
- **commander system prompt（Langfuse Prompt Management・`prompts.resolve_commander_prompt`）**: 実送信単位 2 prompt＝`warehouse-commander-mode-ab`（Mode A/B・base+`MODE_A_RULES` 合成・deadlock 検出+yield 解消 doc08a:316-334）/ `warehouse-commander-mode-c`（Mode C open-rmf standalone・doc08c:138-180 忠実＝3段バッテリ・`traffic.escalation` gate＋`escalation.id`・robot 委譲・action `navigate|stop|charge` 限定）。**Langfuse 取得失敗時はコード定数 `build_system_prompt(mode)` へ fail-open**（doc08:333）。seed は `seed_prompts`。閾値 0.4m/2.5rad は (b) docs 例示（doc08a:278-279・config 未搭載）。Mode C action 制限は凍結 `CommandAction`（schemas.py:135-140）の **strict subset**＝プロンプトのみで制限し parser/schema は不変（Mode A の wait/yield は引き続き valid）
- **Langfuse 管理 prompt（seed）**: `warehouse-commander-mode-ab` / `-mode-c`（`type=text`・`labels=["production"]`・`config={model:hermes-agent,temperature:0.3,traffic_modes}`＝self-describing: mode-ab→[none,simple]/mode-c→[open-rmf]）。`seed_prompts --commit` が `create_prompt` で upsert（本文＝コード fallback と同一）
- **situation `pending_tasks`（#181）**: デモ用シード `WAREHOUSE_TASKS` から投入した `{id,from,to}`（凍結 `PendingTask`・wire key は `from`）。司令官の task 割当原資（消費＝受理 navigate `to` 一致）
- **situation top-level `emergency` forward**: State Cache が `state.json` top-level に持つ emergency review context を Mode A/B/C 共通で situation top-level に保持する。凍結 `Situation` field 追加ではなく extra top-level で、Mode C slim robot fields でも落とさない（doc08c:91 / doc12:397-405）。
- **Langfuse trace（Bridge 所有, S2-PR1）**: 1 trace/turn・`trace_id`=`create_trace_id(seed=f"{run_id}:{gen_id}")`（32hex 決定的）・`session_id`=`run_{mode}_{provider}_{scenario}_{ts}`・tags`[provider,mode,"prompt:<name>",env=<v>]`+metadata`{gen_id,prompt_name,prompt_version,prompt_source,mode_label}`（**prompt/env タグ＝どの prompt/mode/env かを trace でフィルタ可能**・`env=<v>` は `warehouse_env()` 由来で trace-only・`mode_label`=bare mode の人間可読版「Mode A (LLM単独交通管理)」等, eval_sdk `LangfuseTracer` の `extra_tags`/`extra_metadata` 経由・doc08 §Langfuse Prompt Management 方針 / doc20 §8）。**#6(wo) との突合契約 = この seed**（**run_id=`WAREHOUSE_RUN_ID` env**＝#6 と同一ソース・session_id は表示ラベル/未設定時 fallback。#108 で session_id 直結＝不一致を修正。凍結契約フィールドは増やさない doc13:519）。
- **Option D（plugin-ON, OPT-IN・既定 OFF）**: `WAREHOUSE_LANGFUSE_OWNER=hermes_plugin`（or `hermes.langfuse_owner`）で **Hermes Langfuse プラグインに trace/generation 所有を委譲**（doc13:517 の Pattern A を反転）。`hermes_client._decide_plugin_owned` が **plain `openai.AsyncOpenAI`**（langfuse.openai ラッパ無し＝二重 generation 無し）で呼び、**`X-Hermes-Session-Id: H`（H=`seed_for(run_id,gen_id)`）** ヘッダを送る→プラグインが `create_trace_id(seed="H::H")` で trace を mint（plugin `__init__.py:544`）。`langfuse_prompt=` は **送らない**（プラグインに `prompt=` 経路無し＝managed-prompt link 喪失＝[`spike/langfuse-plugin-d/MANAGED-PROMPT-DECISION.md`](../../../spike/langfuse-plugin-d/MANAGED-PROMPT-DECISION.md) で name/version を metadata/tag 伝播の方針）。**drift-detect**: echo された session id（`X-Hermes-Session-Id` **応答ヘッダ**＝`/v1/chat/completions` は body でなくヘッダに echo・`with_raw_response` で読む）≠H なら「当 cycle の score join は orphan する」と **log のみ**（scorer は別プロセス＝抑制経路は無い・**fail-open・cycle に raise しない**, R-26）。node は Option D 下で per-turn tracer を **`NoopTracer`** に切替（Bridge 側 trace の二重計上回避）。**#6(wo) 側も同一 `WAREHOUSE_LANGFUSE_OWNER` を読んで両脚を1 knob で flip**＝`kpi_collector` が `resolve_pattern_d(cfg)` で解決し `score_send.send_scores(pattern_d=…)` へ渡す→Pattern D は `eval_sdk.seed.derive_plugin_trace_id` で同 id 再導出（既定 `bridge`＝Pattern A 不変）。**既定 `bridge`＝Pattern A 不変／Option D は live audio D-verify PASS が前提**（[`FORK-DISTINCTION.md`](../../../spike/langfuse-plugin-d/FORK-DISTINCTION.md)：input_audio fork=KEPT / Langfuse-plugin fork=AVOIDED）。
- **公平性ガード（#103, intent-only）**: 起動時 `assert_fairness`＝`comparison_run` 下で `hermes.{memory_enabled,skills_enabled}` の ON 宣言を fail-closed で abort＋「公平性ガード / fairness guard」ログ行。**実 OFF の権威は Hermes config 側**（`memory.memory_enabled:false`＋`user_profile_enabled:false`＋`memory`/`skills`/`session_search` toolset 除外。doc13 §「Memory/Skills/session_search — OFF 機構」）。倉庫 intent→実 Hermes OFF の配線は Phase 4 起動ハーネスへ defer。

- **キャラLLM交渉 offline コア（Slice 1, #4/doc14）**: `NegotiationEngine`→`warehouse_interfaces.Proposal`（合意時のみ・advisory＝司令官承認前提 doc14:14,38）/ `accept_proposal`（司令官側 gen±2 受理ゲート doc14:142）。timeout/abort は in-flight guard で late output を破棄する。凍結契約は不変（`Proposal`/`AgreedAction`/`TranscriptLine` は既存・拡張なし）。
- **キャラLLM交渉 ROS 配線（Slice 2, #4/doc14・実装済）**: topic publish `/character/speech`・`/negotiation/{turn,proposal}`（`character_llm`）/ `/negotiation/start`（`start_negotiation` ツール経由・`negotiation_starter` 注入時）。node `character_llm`（Mode A/B＝`traffic_mode in {none,simple}`∧`llm:=true` で bringup compose, #7）。司令官発火 = `Command.start_negotiation`（additive optional 契約 schemas.py・PR#287 review fix で到達経路を解錠）→ scheduler dispatch。proposal 取込（gen±2→situation 注入・凍結 `Situation` 不変）。**契約変更**: `Command.start_negotiation`（additive optional）＝`contract` ラベル。offline=`ScriptedPersona`／live persona は Slice 3。
- **Mode A v1/v1.5 内部 event log**: `conversation_events.jsonl`（既定 `runtime_dir()/conversation_events.jsonl`、`WAREHOUSE_CONVERSATION_EVENT_LOG_PATH` で上書き）。`record_type` は `conversation_event` / `task_lifecycle` / `self_action_result` / eval producer 用 rows。MCP command audit には混ぜない。
- **ER 音声 transport 選択（`robotics.resolve_audio_transport`・純・offline）**: `config.robotics.er_gateway` から音声レグの `Transport`（`hermes`⇔`direct`）を解決する L4 seam。既定 `direct`（恒久 fallback）・forked gateway 設定時のみ `hermes`。**選択（intent + 監査タグ）を produce するだけ**で actuation/dispatch は行わない（live 音声 wire 本体は #344・別）。`Transport` 昇格 contract は将来の別 PR（doc06 §5:152,208）＝本 seam は `contract` ラベル不要。
- **ER adapter 生産 factory（`robotics.build_er_adapter`・純構築・offline）**: 上記 transport 選択と `GeminiErAdapter`/`HttpErTransportSender` を束ね、`config.robotics.er_gateway` から**稼働可能な `GeminiErAdapter`（解決 transport＋実 HTTP sender＋任意 blob loader）を produce**する L4 wiring seam。**構築のみ**で actuation/network/課金 send は行わない（実 send は `WAREHOUSE_LIVE_ER=1` gate 越しのみ）。secrets は env のみ・凍結契約不変（`Transport` は observation-only のまま・additive）＝本 seam も `contract` ラベル不要。**稼働 ROS ノードからの呼び出し＋実 `BlobLoader` 注入は別 XER6 cycle**（今 round は factory + offline unit のみ）。
- **L3 `compile_raw_output` executor/store 注入 seam（store-seam-pierce・offline・additive）**: `pipeline.compile_raw_output(..., executor: TaskGraphExecutor | None = None, store: TaskGraphStore | None = None)`（相互排他・両方指定は `ValueError`）。既定＝非注入は従来どおり毎呼び出し fresh in-memory store の one-shot（挙動不変・後方互換）。長命 `executor=`（将来 x_er_bridge node が cycle 跨ぎで保持）または durable `store=`（file/Redis/DB＝Store Plugin, docs/productization/03-l3-planning-core-box.md:132,135・doc02:198）を注入すると `after` 進行と duplicate-dispatch guard が cycle（呼び出し）を跨いで効く。所有権: store=cross-cycle 状態 / executor=stateless lifecycle driver / caller loop=進行（`mark_running`/`mark_succeeded`＝commit point, executor.py:94-99）。R-26: 非 accept plan は注入 store を読みも書きもしない（0 dispatch）。unit=`tests/unit/test_l3_pipeline_store_injection.py`（13 本・cross-cycle 進行/二重 dispatch guard/stale-handle hazard 実測/R-26 spy store）。凍結契約不変・bridge-local。

- **run manifest 構成 seam（`robotics.composition`・productization S2・純 offline）**: `run_manifest.v1` schema＋loader＋fail-closed preflight（`CompositionError` 起動拒否）＋effective-composition witness を produce。artifact = `out/runs/<run_id>/{manifest.yaml,effective_composition.json}`（repo-relative・gitignored・書き手は稼働 node/launch harness＝doc09:48,370。preflight 通過＋box 構築直後に 1 run 1 回書く）。S3 レーンとの契約=`profile_version`/`profile_content_hash` optional 欄（hash 算出は S3）。S4 レーンとの契約=`PluginRegistryView.registered_plugin_ids()->set[str]`（pluggy `pm.register(impl, name=<manifest plugin id>)` を adapter で包む）。凍結契約不変・`contract` ラベル不要（bridge-local artifact schema＝doc09:8）。

## 消費 (consume)
- 契約: `warehouse_interfaces.schemas`（Situation/Command/StateSnapshot/RobotSnapshot、**+ Proposal/AgreedAction/TranscriptLine/CommandAction=キャラLLM交渉 Slice 1**）、`stores`（StateStore/GenStore/IdempotencyStore IF）、`config.load_config`（traffic_mode / safety.emergency_min_distance / hermes.base_url / **nav2_bridge.base_url** / **hermes.{memory_enabled,skills_enabled,comparison_run}**=#103 公平性 intent / **hermes.prompts.{source,label,cache_ttl_seconds,names.{mode_ab,mode_c}}**=Langfuse Prompt Management 取得設定・additive・既定で挙動互換 / **cycle.{mode_a_seconds,mode_c_seconds}**=総サイクル長→`scheduler.resolve_cycle_wait` が待機=総−応答(~2s)に変換・欠落/不正は code 既定 1.0/3.0s へ fail-open・dev は Mode A=120 で ~2分スパン / **robotics.er_gateway.{base_url,audio_input_audio_supported}**=Mode X-ER 音声 transport 選択（`robotics.resolve_audio_transport`）・additive・既定 両キー OFF で挙動互換（音声=direct 恒久 fallback・doc06 §5 補遺:269）・warehouse_interfaces には入れない bridge-local）
- 同一トラック（in-process, doc16 §9 / #81）: `warehouse_mcp_server.tools.WarehouseTools().dispatch`（実 tool 実行）、`gen_check.GenChecker`（共有 gen_store/idempotency_store で wire）、`nav2_client.Nav2RestForwarder`（Mode A/B の REST forwarder を注入）。
- net: Nav2 Bridge REST `POST /api/v1/{navigate,wait,stop}`（`:8645`, #86 確定契約 = `docs/mode-a/12a-integration-mode-a.md:200-300`）。受理された `dispatch_task`/`cancel_task`/`send_to_charging` のみ発火（mapping は `docs/mode-a/08a-llm-bridge-mode-a.md:164-173`）。`dropoff`→`destination` の凍結ドリフトは `nav2_client.plan_nav2_request` が明示変換（どちらの凍結フィールドも改名しない）。
- env: `HERMES_API_KEY`/`API_SERVER_KEY`（secret）、`WAREHOUSE_PROVIDER`（Hermes active_provider を反映, 既定 default）/`WAREHOUSE_SCENARIO`（既定 demo）= trace ラベル。**`WAREHOUSE_TASKS`（#181, デモ用 pending_tasks シード・JSON 配列 `[{id,from,to}]`・未設定=[]・malformed は fail-open で warn+[]）**。**`WAREHOUSE_LANGFUSE_OWNER`（Option D, additive・既定 `bridge`＝Pattern A 不変・`hermes_plugin` で plugin-ON 経路・不明値は安全側で `bridge` へ fallback＋log・env > `hermes.langfuse_owner` config・`isinstance(...,Mapping)` 型チェックは scorer 鏡像と一致・`"bridge"`/`"hermes_plugin"` literal は cross-lane 文字列契約として `tests/unit/test_hermes_client_option_d.py` の cross-check で wo 側と同値 pin・#360 round-2 D）**。`LANGFUSE_*`（trace 送信先・deploy 申し送り）。
- file: `state.json`（State Cache=#5 が書く `StateSnapshot`。Bridge は読むだけ・センサ topic は購読しない doc08a:20-22/doc12:169）
- net: Hermes Gateway `/v1/chat/completions`（OpenAI 互換, langfuse.openai 経由）
- pip: `langfuse>=4.9,<5` + `openai>=1.0,<2`（lazy・setup.py 宣言。rosdep でない＝CI pytest は fake で非依存）
- **pkg `eval_sdk`（doc21 Phase 1・一方向依存・`package.xml` exec_depend）**: `tracing.py` が domain 非依存の Tracer seam を import — `Tracer`/`NoopTracer`/`LangfuseTracer`←`eval_sdk.tracer`、`resolve_run_id`/`seed_for`←`eval_sdk.seed`。`trace_seed(run_id,gen_id)` は `eval_sdk.seed.seed_for` へ委譲（#6 `trace_id.seed_for` と統合した**唯一の join key**＝重複 f-string を 1 本化）。`build_session_id`（session ラベル形）は Bridge 固有で本 module に残置。**外部 import 面は不変**（再 export）。

## 排他3層（A+B-3+C, doc08:161-179 / doc15§2）
- **A** = `wait_for(2.5s)` の client-side cancel のみ。明示 `/v1/runs/{id}/stop` は **#54 で撤回**（stateless chat/completions に run_id 無し＋ tool dispatch は Bridge in-process でサーバー側 tool 実行が無く /stop 不要。R-35A 解消, doc08:173-179）。安全主担保は B-3+C。
- **B-3** = `gen_store.set` で世代公開 → action_map が `gen_id` を全 ToolCall に注入 → MCP が stale reject。
- **C** = action_map が per-call UUID mint → MCP `GenChecker.check`→`check_and_add` で replay reject（gen_check.py:83-86 で enforce 確認済）。

## テスト
- `tests/unit/test_situation_builder.py`（Situation 組立・CTRV 予測(直進=CV/旋回=円弧/閾値連続)・obstacle_ahead・**Mode C 省略 / Mode A 保持**・**current_task 充填(両モード)**・**head-on pattern-1（静止）判断材料の充足(#181)**・**pattern-3 接近予兆の材料充足（両 bot 接近→`predicted_position_3s` 収束）＋静止対向の非収束境界（doc08a:307-314,334）**・**pending_tasks `from` wire key(by_alias, #181)**）/ `test_hermes_client_parse.py`（`parse_command`+`parse_command_content`・**`build_system_prompt` mode-aware: Mode A/B=deadlock ルール(#181) / Mode C=`MODE_C_PROMPT` 忠実化（3段バッテリ・escalation gate・robot 委譲）＋action `navigate|stop|charge` が凍結 `CommandAction` の strict subset（schema 不変）**）/ `test_tracing.py`（`build_session_id`・`trace_seed` 決定性・`NoopTracer` no-op）/ `test_bridge_scheduler.py`（gen publish・dispatch・timeout/outage fallback・**malformed `decide()` response ignore (#192)**・**実 WarehouseTools で B-3 stale / C replay の end-to-end**・**current_task 追跡(navigate/stop/rejected)・history 有界+pattern-2 idle 持続**・**pending_tasks seed→situation→受理 navigate 消費 / `parse_seed_tasks` 正常・malformed(#181)**）。
- **Mode C no-actuation contract（#4・forwarder=None で受理 tool でも 0 actuation／goal_pose 非発行）= `tests/unit/test_modec_noactuation.py`**（R-26 `@pytest.mark.safety`・host-runnable・ROS 不要・`test_nav2_forward.py` の Mode A/B=forwarder 注入と対）: ① `forwarder=None`＋受理 navigate/charge/stop（**Mode C 全3アクション navigate|stop|charge を網羅**＝doc08c:136,176。stop=`cancel_task`→`/api/v1/stop`(nav2_client.py:99-103) は安全アクションかつ Mode A/B では実 actuating forward） → `status=="ok"` だが forward seam(`tools.py:153`)未到達を sentinel(`plan_nav2_request` を呼んだら fail)で固定 ② `NAV2_BRIDGE_MODES`(`llm_bridge.py:75`)＝`{none,simple}` のみ・`open-rmf` 除外（`llm_bridge.py` は rclpy 依存で CI に ROS 無し＝AST で源から定数を読む・import 副作用ゼロ）→ `llm_bridge.py:135` 三項は Mode C で None 枝 ③ Bridge publisher は `/llm/reasoning`+`/llm/command` のみ＝`/bot{n}/goal_pose` 非発行を AST で固定（doc03:97/doc08:464 の凍結トポロジ）。実 Open-RMF 転送 seam（doc08:169/doc15:211-219/08c:264）は別レーン #180/PR#219 所有＝tracking issue #228（blocked）。
- **Mode X-ER L4 ER wiring offline unit**: `tests/unit/test_er_transport_resolver.py`（`resolve_audio_transport` 真理値表）/ `test_gemini_er_adapter.py`（`GeminiErAdapter` seam・`build_provider_request` 凍結 shape・`HttpErTransportSender` の secret 配置＋`WAREHOUSE_LIVE_ER` gate）/ **`test_er_adapter_factory.py`（`build_er_adapter`：gateway ON→HERMES adapter（sender が config の :8644 base_url＋env bearer 保持）/ OFF・欠落・malformed→DIRECT adapter・factory↔resolver 一致・env 鍵優先順・注入 sender/load_blob bypass・鍵無しでも offline 構築・propose_plan end-to-end（fake sender）＝network/課金なし・mutation-sensitive）**。全て host-runnable・ROS/network 非依存。
- 偽 LLM / 偽 state.json / `NoopTracer` で独立検証（doc16§11, langfuse 非依存）。`tests/unit/test_negotiation_engine.py` は offline 交渉 engine の format/timeout/abort/gen gate/no-actuation を固定し、timeout/abort は persona 発話生成中の cancel + late proposal 破棄まで見る。tests/ は ws/src 間 import 可（governance 非対象）なので end-to-end は実 `warehouse_mcp_server` を使用。安全機構は `@pytest.mark.safety`。Ruff(py312/line100/double-quote) + pytest 緑を維持。
- `tests/live/test_hermes_gateway_live.py` は opt-in Hermes smoke（`WAREHOUSE_LIVE_HERMES=1`）であり、通常 CI では skip。`API_SERVER_KEY`/`HERMES_API_KEY` があれば authenticated `/v1/models`、`WAREHOUSE_LIVE_HERMES_CHAT=1` なら課金 provider call の `/v1/chat/completions` まで確認する。Langfuse 実トレース（#88）は別 human gate。

## 前提・未確定 (TODO / seam)
- **(済) tool dispatch transport**: S2-PR2 HALF B で **in-process `WarehouseTools().dispatch`** を採用（subprocess/stdio の `StdioMcpToolExecutor` 案は撤回 ＝ #81 が同一トラック import を解錠したため不要）。stdio server.py は Hermes 外部接続用に存続。end-to-end の安全は `test_bridge_scheduler.py` / `test_nav2_forward.py` で実 tools 検証済。
- **(済) MCP→nav2_bridge REST 配線**: S2-PR2 HALF B（`warehouse_mcp_server.nav2_client`）。受理 motion tool→`POST /api/v1/{navigate,wait,stop}`。nav2_bridge 本体（REST→BasicNavigator, #86）は編集せず消費。
- **REST forward は fail-open**: Nav2 Bridge outage は log のみ（cycle を落とさない）。実機での到達確認・retry/backoff は後続（Phase 2/3）。Open-RMF（Mode C）forward 経路は未実装（forwarder=None）＝**no-actuation contract を `tests/unit/test_modec_noactuation.py`(#4) で R-26 固定済**（受理 tool でも 0 actuation・goal_pose 非発行）。実 Open-RMF 転送 seam（doc08:169/doc15:211-219）は #180/PR#219 所有・tracking issue #228（blocked）。
- **Langfuse v4 API 実機 verify（Phase 3, doc13:520）**: trace seam は `eval_sdk.tracer` に移管済みで、v4.9 API（`client.create_trace_id` / `start_as_current_observation` / `propagate_attributes`）に pin。実 SDK call surface は CI `langfuse-api-contract` が鍵なしで検査する。`langfuse.openai.AsyncOpenAI` / cost / managed-prompt の live 確認は #88 継続。通常 unit は lazy+fail-open で Langfuse 非依存。
- **deploy 申し送り**: ① Hermes 内蔵 Langfuse プラグイン **無効化**（二重計上回避, doc13:517）② bridge プロセスに `LANGFUSE_*` env 投入（`config/<env>/.env`、現 `.env.example` は `HERMES_LANGFUSE_*` のみ＝要追記）。
- **docs 反映は #73 へ**: doc08:356/361・doc13:516（trace_id=`uuid7().hex`/session=`demo_...` の記述）を **seed 派生 + `run_*` session** に更新するのは Langfuse doc 所有の **#73**（branch `docs/langfuse-v4-provider-decision`）。本 PR は #73/#6 にコメントで seed 契約を予告（cross-lane 衝突回避）。
- **(済) doc-drift #71 MERGED**: Bridge-mediated dispatch の doc08/doc15 reconcile は #71 で main 入り。
- **(済) Mode A system prompt + task 注入経路 = #181**: situation builder が mode-aware（#66 Optional 化を消費）。`build_system_prompt`：**Mode A/B** は `MODE_A_RULES`（per-bot 割当＋deadlock 3条件+yield 解消手順 doc08a:316-334）を base に付与＝**実装済**。**task 注入経路**（`WAREHOUSE_TASKS` シード→`pending_tasks` キュー→消費）も #181 で実装済。
- **(済) Mode C system prompt の doc08c:138-180 忠実化 = 本スライス（`feat/mode-c-prompt`, track #4）**: Mode C（open-rmf）は standalone `MODE_C_PROMPT` を返す（doc08c:138-180 忠実＝戦略判断のみ・3段バッテリ doc08c:155-158・`traffic.escalation` gate＋`escalation.id` doc08c:160・robot 選択委譲 doc08c:154・action 制限 `navigate|stop|charge` doc08c:136,176）。**裁定（step-3）**: action 集合は凍結 `CommandAction`（schemas.py:135-140）＝Mode C の `navigate|stop|charge` はその **strict subset**。プロンプトのみで USAGE を制限し parser/Command schema は**狭めない**（Mode A の wait/yield は引き続き valid・contract 非変更）。**docs 例示からの意図的逸脱**: doc08c:147-151 のレイアウト座標は illustrative なので逐語コピーせず `situation.warehouse.layout` 参照に置換（KNOWN_LOCATIONS/config が正本＝drift 防止）。base とは action 集合/衝突回避の役割が矛盾するため append でなく**置換**（battery は base/Mode A/C とも 3段＝doc08a:246-249/doc08c:155-158。base の旧2段→3段 reconcile 済）。`start_negotiation`/`negotiation_proposal` は doc08c:164-165 の前方参照（キャラLLM交渉・advisory・任意）。#137 escalation 契約・#180 Phase3（escalation native sub）と整合。
- **(裁定 2026-06-12) 閾値 0.4m/2.5rad の config 化可否 = 現状維持（config 化しない）**: deadlock 判定値 0.4m/2.5rad は (b) docs 例示（doc08a:278-279）で `MODE_A_RULES`（hermes_client.py:93-94）のプロンプト文中に保持する。**config 化しない裁定の根拠**: ① これらは LLM の推論ヒント（プロンプト文＝予兆/解消手順の入力）であって安全しきい値ではない。実際の安全境界は Emergency Guardian の 0.3m（`config.safety.emergency_min_distance`＝`situation.py:48` DEFAULT 0.3 で別途強制 / doc08a:278「Emergency Guardian は 0.3m で緊急停止」）＝**プロンプト数値が緩んでも安全機構は独立に効く**。よって config キー化・凍結契約化の必要が無い。② `config/warehouse.base.yaml` 所有は bringup/skeleton（parallel-workflow.md §7.1）＝**本トラック編集境界外**。**config 化に倒す判断が出た場合のみ**所有トラック（epic #4 経由）へ予告（post は orchestrator 承認後）。**本スライスでは config キーを追加しない**。
- **(済) /stop（#54）= 解決(撤回)**: 採用トランスポート（stateless chat/completions + Bridge in-process dispatch）にはサーバー側 tool 実行が無いため明示 Hermes `/v1/runs/{id}/stop` は不要＝撤回。Layer A は client-side cancel（`wait_for`）のみ・安全主担保は B-3+C。`scheduler._stop_hermes_run` の no-op stub は削除（dead code）。unit: `test_stale_call_rejected_when_stop_noop_54`（R-26）。
- **公平性ガード #103 = intent-only / 一部 defer**: Bridge は倉庫 intent の自己矛盾を起動時 abort するのみ＝**Hermes の実 memory 状態を保証しない**（stateless path・別 config・起動順）。**defer**: ① 倉庫 intent→実 Hermes OFF の配線（env interpolation / 生成 config / toolset 差し替え）= Phase 4 起動ハーネス責務（`deploy/hermes` owner 未割当→要予告）／② MCP token-cost **実測**（現状は config 由来概算 ~800+~500tok）= Phase 3→4／③ doc06:88「3秒ループ動作検証」= runtime 必要。docs 正本: doc13 §「Memory/Skills/session_search — OFF 機構」（PR #150）。
- **(本スライス 2026-06-12 = offline hardening の固定/残し)**: 新機能でなく既 land 機能の境界固定。① **step1**: pattern-3 接近予兆の判断材料 unit（`test_situation_builder.py` 末尾 append・両 bot 接近で `predicted_position_3s` 収束＋静止対向の非収束境界 doc08a:307-314,334）。② **step2**: 0.4m/2.5rad config 化＝**現状維持の裁定**（上記）。③ **step3 fairness OFF 配線 defer 境界（再確認）**: intent ガード（`fairness.py`）は完成・`test_fairness.py` 緑。**実 Hermes OFF の配線（env interpolation / 生成 config / toolset 差替）は Phase 4 起動ハーネス責務＝`deploy/hermes` owner 未割当で未配線（doc13:267, 13:266 = Bridge は intent/consistency ガードのみ）**＝本スライスで配線しない。④ **step4 #228 no-actuation seam（review-only 再確認）**: `test_modec_noactuation.py`（R-26・3角・forwarder=None で 0 actuation / `NAV2_BRIDGE_MODES`＝`{none,simple}` / goal_pose 非発行）が host 緑。**#228 は本スライスで解錠しない**（実 Open-RMF 転送 seam＝#180/PR#219 必須・live-gated）。⑤ **step5 forward gap（offline 表面化のみ）**: **Langfuse Phase 3 実トレース検証＝#88**（live Hermes/Langfuse 必須・track:llm-bridge+wo+docs）で詰める—**(済・offline) mode 別 system prompt の保存先＝Langfuse Prompt Management 採用・実装**（`prompts.py`/`seed_prompts.py`・コード定数は fail-open フォールバックへ降格。doc08 §Langfuse Prompt Management 方針。**live seed・`langfuse_prompt=` 紐付けは #88 Phase3**）・session ラベル（`run_{mode}_{provider}_{scenario}_{ts}`＝tracing.py）・tag taxonomy（`[provider,mode]`+gen_id metadata）の段階移管。**恒久 `pending_tasks` producer 未確定（将来 Warehouse Orchestrator #6 / `get_task_queue` doc15 想定。当面は `WAREHOUSE_TASKS` env シード＝#181, 08a:466-468）**。いずれも live API は呼ばず docs 表面化のみ。

- **(済) キャラLLM交渉 Slice 2（ROS 配線・offline）= 実装済（PR#287）**: `character_llm` ノード（`character_node.py`・`setup.py` entry_point・`bringup.launch.py` #7 Mode A/B compose）/ `start_negotiation`→`/negotiation/start` publish（`negotiation_starter` 注入）/ 司令官発火 `Command.start_negotiation`→scheduler dispatch（PR#287 review: 5モーションaction限定で到達不能だった穴を additive 契約で解錠）/ `/negotiation/proposal`→`set_negotiation_proposal`→gen±2→situation 注入。**`agreed_action.to` 解決**: free-form 表示名（doc14:121「退避地点B」）→ commander が承認時に `KNOWN_LOCATIONS`（retreat_A/retreat_B）へ解決（`MODE_A_RULES` に明示・doc08a:387）してから `CommandItem` に載せる（未解決は validator reject＝LLM 責務）。**Emergency abort producer**: `emergency_guardian` が estop 時 `/negotiation/abort` publish（`gl.build_abort`{reason,bot,event_id}・doc03:108/doc14:241-247・recovery では発行しない）。**契約変更**: `Command.start_negotiation`（additive optional・`contract` ラベル）。`negotiation_proposal` 注入は `extra="ignore"`（凍結 `Situation` 不変）。**残（doc 追従・任意）**: 封筒 shape の doc03/doc14 トピック表登録 / `config` の `character.*` personality。
- **キャラLLM交渉 Slice 3（ライブ・人間ゲート・未実装 = #288）**=実 Hermes キャラ persona（`hermes_client.py` にキャラ system_prompt 経路・max_tokens≈60 doc14:173・現状 `ScriptedPersona` を差替）＋ Langfuse `agent` span/`turn_index`（doc14:283-292）。テンポ実測待ち。キャラLLM は Phase 4 比較対象外＝演出専用（doc14:255）。Phase 3 実機（doc14:254）。

## 設計ドキュメント
- docs/architecture/08（共通サイクル/フォールバック/同時発火制御）・mode-a/08a（Situation/action map/prompt）・mode-x-er/README・mode-x-er/01/02/03（Mode X-ER Robotics Bridge / L3 Planning Core / ER-only adapter 設計提案）・mode-x-er-vla/README・mode-x-er-vla/01/02/03（ER+VLA 統合 / OpenVLA 調査 / safety gates 設計提案）・15（MCP/競合状態）・13（Hermes）・12（State Cache/Emergency）・03（topics）・16§3,§11・17§6。

## Mode X-ER `x_er_bridge` node（XER6・docs/mode-x-er/08）— produce/consume 追記
- **ノード追加**: `x_er_bridge`（`x_er_bridge.py` `main()`・console_script `x_er_bridge`・doc mode-x-er/08 §2）。bringup は **`mode_x_er.enabled == true` のときのみ** compose し、そのとき Mode A commander `llm_bridge` は起動しない（**相互排他**＝司令 node は常に 1 本・gen 発番 B-3 の一意 owner）。起動時 `x_er_composition.build_x_er_runtime(cfg)`（composition §4 の fail-closed 起動列）＋ `robotics.build_er_adapter(cfg)`（adapter_factory.py:77・DIRECT fail-safe）。**どの startup raise でも `main()` は spin せず nonzero exit（doc mode-x-er/08 §6・0 cycle・0 dispatch）**。cycle は llm_bridge.py:254-297 と同型の background thread + 専用 asyncio loop で `x_er_cycle.run_x_er_cycle` を回す。
- **提供 (produce)**: dispatch 済 tool call の記帳（in-process `WarehouseTools.dispatch` 経由。**offline Slice A は `nav2_forwarder=None` 固定＝受理・記帳のみ 0 actuation**, tools.py:92-115。sim flip は **#421 で `mode_x_er.dispatch.forward_to_nav2` key 凍結・#423 で `resolve_nav2_forwarder` 配線済み＝config-driven**（safe-OFF 既定 `None`＝0 actuation・`true`→`Nav2RestForwarder`・base_url 空は fail-closed））/ `FileGenStore` への gen 発番（llm_bridge.py:152 と同型・ER 出力非依存＝mode-x-er/01:184-197）/ `out/runs/<run_id>/` witness artifact（runtime 構築時・record.py 経由・gitignored）。ROS topic は本 slice では publish しない。
- **消費 (consume)**: config `mode_x_er.{enabled,execution_profile,calibration_id,visual.snap_radius_m,run_manifest,plugin_manifests,site_profile.{base_dir,customer,site}}`（doc mode-x-er/08 §3 凍結形・base は安全側 OFF/空で追記済。`execution_profile` は起動 gate `x_er_composition._execution_profile` で検証＝`x_lite` のみ許可・`x_rmf` は `NotImplementedError` 起動拒否〔doc mode-x-er/08 §2/#346〕・未知値/欠落は `XErCompositionError`）＋ `locations`（location_coords 導出）＋ `robotics.er_gateway`（adapter transport）。**ROS topic（Slice B）**: `/nav2_bridge/goal_result`（`std_msgs/String` JSON `{robot,task_id,result}`＝doc03:110 / doc12a:384-392）を **subscribe**（完了信号→進行）。契約: 凍結 `warehouse_interfaces`（stores/config/locations/Command）のみ・同一トラック `warehouse_mcp_server`（tools/gen_check）。lane 内 frozen IF: `x_er_composition.build_x_er_runtime`→`XErRuntime` / `x_er_cycle.run_x_er_cycle`→`XErCycleOutcome`（**additive: `committed`〔(robot,node_id) tuple〕+`plan_id`**＝by-robot 相関の材料）/ `x_er_completion.{parse_goal_result,apply_goal_result}`（純・ROS-free・完了信号→ fail-closed lifecycle 遷移）。
- **完了信号変換（Slice B・実装済）**: `/nav2_bridge/goal_result`→`mark_succeeded`/`mark_failed`→次 cycle 再駆動（doc mode-x-er/08 §5 step7）。**配線（race-safe）**: subscription callback（rclpy spin thread）は `parse_goal_result`→**deque へ enqueue のみ**＋loop wake（`call_soon_threadsafe(_cycle_trigger.set)`）。cycle loop は毎反復冒頭で `apply_pending_completions` を **cycle と cycle の間で drain**（executor handle が生きていない時のみ apply）。**理由**: cycle 途中（`await tool_executor.execute` の suspend 中＝forwarder ON の httpx I/O）に apply すると 2 本目の live handle が開き stale-handle persist が遷移を握り潰す（executor.py:157-163 single-live-handle 契約違反＝敵対レビュー blocking を修正）。**相関は robot 経由**（navigate REST body=`{robot,destination}` のみで dispatch/Policy-Gate task_id も node id も nav2 へ round-trip せず＝nav2 は自前 `nav_NNN`）。**同一 robot 同時 2 task は by-robot で曖昧**（after-gate は ≤1/robot を保証せず・offline は 0.5s rate limit で 2nd dispatch 却下）→ `fold_inflight` が **overwrite せず refuse＋loud log**（誤 node mark を防ぐ safe degrade・敵対レビュー major）。fail-closed: 未知 robot=無視 / running 以外=idempotent no-op（二重・順序前後）/ failed=dependents 据置（retrigger せず）/ malformed=drop。richer な task_id 相関は dispatch→nav2→goal_result の id round-trip 契約追記が別途必要（doc08 §9 residual・#421 開示済）。**ROS glue の非被覆**（`create_subscription`/`call_soon_threadsafe`/`_cycle_trigger.set` の wire）は rclpy-gated＝container/G5 検証（doc16 §11）。決定ロジックは純関数 `x_er_completion.{parse_goal_result,apply_goal_result,apply_pending_completions,fold_inflight}` に隔離し R-26 unit 済。
- **dev-only 暫定（residual・隠さない）**: `mode_x_er.request_fixture`＝v0 の入力源（`ErTaskRequest` フィールドの JSON ファイル path・**設定時のみ消費**・doc mode-x-er/08 §3 凍結 key 外の provisional ゆえ base.yaml には載せない。malformed は起動拒否＝fail-closed）。実 request producer（mic capture / 事前録音 ref 配線）は後続 slice。
- **後続 slice（→ #418/#421/#423 で解消済み）**: sim flip 配線＝`mode_x_er.dispatch.forward_to_nav2`（#421 key 凍結・#423 `resolve_nav2_forwarder` 配線・safe-OFF 既定）／llm gating＝#423 の launch co-gate（`llm ∧ mode_x_er.enabled`）で解消。残は実 mic 入力と G5 sim human gate（doc mode-x-er/07）。
- **テスト**: `tests/unit/test_x_er_bridge_helpers.py`（純 helper・rclpy 不要＝import guard の検証を兼ねる）／`test_x_er_completion.py`（`parse_goal_result`/`apply_goal_result` の R-26＝succeeded/failed 遷移・未知 robot 無視・二重/順序前後 idempotent・malformed drop・相関は robot＝nav2 task_id 非依存）／`test_x_er_autonomous_e2e.py`（goal_result 単独で red→blue を手動 mark 無しで駆動・誤 robot は blue を gate・二重完了は非再 dispatch）。node 全体の offline R-26 は doc mode-x-er/08 §8 ①層＝composition/cycle/completion レーンの test が担う。

## Visual Resolver object-class snap（doc02:150 conjunction 完了・2026-07-11）— produce/consume 追記
- **公開挙動の変更（consumer-visible・stricter snap）**: `VisualTaskResolver._resolve_one` の snap gate が doc02:150「snap は known location への距離と object class で判定する」の **conjunction を完全実装**（従来は距離節のみ・object-class 節は defer＝旧 `# TODO(object-class)` resolver.py:192-194）。距離基準（`snap_radius_m`・最近傍選択）は**無変更**。追加された class 基準: detection 側 class ＝ `Detection.color`（draft 唯一の class 保持 field・doc01:142-143,229・doc07:26,67 の記録済 proxy）と、**距離で選ばれた最近傍 location** に登録された期待 class（新規注入 `VisualPolicy.location_classes`）の**完全一致**。不一致（両側 present かつ相違）は snap 拒否＝`unresolved`／新 reason `object_class_mismatch`（`UnresolvedReason` へ additive・bridge-local）・**より遠い一致 location への redirect はしない**（doc02:150 に探索規則の記載なし＝fail-closed veto）。
- **絶対保存（absent-class 挙動）**: class 情報が**どちらか欠け**れば（`color=None` または location 未登録）基準は評価不能＝**従来どおり距離のみで snap**。根拠 = doc02 自身の実例（入力 :117 は class field 無し → 出力 :126-133 で shelf_1 へ snap）。`location_classes` 既定は**空 map**＝既存呼び出し（`x_er_composition.py:239-241` は `location_coords`+`snap_radius_m` のみ注入）への**挙動変化ゼロ**（additive・optional field・default_factory=dict）。
- **提供 (produce)**: `VisualPolicy.location_classes: Mapping[str, str]`（KNOWN_LOCATIONS 名→期待 object class・注入 site snap rule＝`location_coords` と同じ consume-gap パターン。productization/03:31,39「snap rule — 現場ごと」）／ `UnresolvedReason.OBJECT_CLASS_MISMATCH = "object_class_mismatch"`（audit 用 stable reason・doc02:99,107）。
- **消費 (consume)**: 変更なし（凍結 `KNOWN_LOCATIONS`・landed `Calibration`・`RoboticsPlanDraft.Detection.color`＝既存 field 消費開始）。凍結契約 `warehouse_interfaces` は無編集。
- **テスト**: `tests/unit/test_visual_resolver.py` に conjunction 7 本追加（26 本・oracle は手計算 fixture: red_box→shelf_1 dist 0／far_box→shelf_3 dist ~0.55 > 0.25）。mutation canary: AND→OR flip で distance-ok-class-mismatch / class-ok-distance-fail の 2 本が赤。
- **残（doc07 re-pin lane 所有）**: docs/mode-x-er/07-implementation-status.md:26,67 の Visual Resolver PARTIAL 行・残作業 bullet は本変更 land 後に stale（object-class defer 解消）＝doc07 側 re-pin で更新。
