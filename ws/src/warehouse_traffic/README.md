# warehouse_traffic

- **ビルド**: ament_python
- **責務**: TrafficManager IF（NoTrafficManager=none / SimpleTrafficManager=simple）+ VirtualScan（Multi-Robot Costmap Layer）。RMFTrafficManager(open-rmf) は Phase 3
- **主担当**: ros2 / **Phase**: 0.5→3

> doc16 §2 準拠。Mode 切替は `config/warehouse.yaml` の `traffic_mode`。
