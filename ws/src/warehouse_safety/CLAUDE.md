# warehouse_safety — Emergency Guardian（50ms周期・LLM非経由の安全監視）+ twist_mux 設定

- **担当トラック / ブランチ**: bridge / `feat/safety-state`
- **Phase**: 0.5
- **ビルド**: ament_python
- **ノード**: emergency_guardian
- **編集境界**: このパッケージ配下のみ。共有契約 `warehouse_interfaces` は変更不可（`.claude/rules/parallel-workflow.md` §4）。
- **依存**: warehouse_interfaces（契約は warehouse_interfaces のみ経由・他トラック内部を import しない）
- **テスト**: 偽入力で独立検証（doc16 §11）。安全機構はユニットテスト必須（R-26）。Ruff(py312/line100) + pytest 緑を維持。
- **設計**: docs/architecture/03・12（Layer1 95-151 / event 141-150）・15（twist_mux 383-399）・16・17。

## 提供 (produce)
- topic: `/negotiation/abort`（std_msgs/String JSON `{reason, bot, event_id}`, doc03:108 / doc14:241-247 R2）。**estop 時のみ**（物理緊急停止＝`_emergency_stop` の rising edge）`/emergency/event` と同 `event_id` で発行＝走行中のキャラLLM交渉を即中断・proposal 破棄。**recovery（blocked-timeout 等 low-harm）では発行しない**（交渉はデッドロック解消手段で、recovery はそのフォールバック＝中断すると本末転倒, doc08a:363-372）。payload は `gl.build_abort`（pure・test 済）。PR#287 review fix。
- topic: `/emergency/event`（std_msgs/String JSON, doc12:141-150 コア形: `event_id/robot/type/severity/action_taken/timestamp/requires_llm_review` [+任意 `detail`]）。State Cache が `state['emergency']` に取り込む。**edge-trigger（#126）**: `(bot, reason)` の立ち上がり時のみ発行（持続条件で 20Hz 連発しない／解消→再発で再発火）。コア形は不変。`type` 値: `near_collision`/`battery_critical`/`blocked_timeout`/**`pose_stale`（#126 freshness, additive な新値）**/**`operator_stop_request`（mode-m1/05 §5 操作者非常停止要求, additive な新値）**。新 `type` 値の追加はコアキー不変＝後方互換（State Cache は ring に積むだけ）。`pose_stale` は `/emergency/event` の `type` であって `RobotSnapshot.status` 値ではない（status は moving/idle のみ・doc12:254）。 **【2026-09-16】`type` 新値 `scan_stale`（additive・コア形不変・`feat/guardian-scan-stale`）。** **【2026-09-18】`type` 新値 `terrain_health`（additive・コア形不変・`OQ-OD95` = A 裁定＝[docs/mode-outdoor/04 追補 ⑬](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)。既定 OFF ＝ config `perception.terrain.enabled`）。**
- topic: `/bot{n}/cmd_vel/emergency`（geometry_msgs/Twist 全ゼロ停止, twist_mux priority 100）+ bot 毎 Nav2 goal cancel（action_msgs/CancelGoal を `/{bot}/navigate_to_pose/_action/cancel_goal` へ）。
- topic: `/bot{n}/stop_state`（std_msgs/String JSON `{"stop_requested": bool, "valid_until": float}`, **mode-m1/05 §4-1 producer**・doc03:114）。L0' 停止上乗せ（m1_driver consumer・`stop_overlay_enabled:true` 時のみ購読）への**周期 feed**: `_check_safety` が 50ms tick ごとに同一 tick の `decisions` から `stop_requested = gl.stop_requested_for(decisions, bot)` を導出し全 bot へ publish（停止要求の有無に関わらず毎 tick＝§4-1「常時流れる設計」）。`valid_until = now + DEFAULT_STOP_STATE_VALID_WINDOW_S`（0.5s＝consumer `DEFAULT_STOP_STATE_MAX_VALIDITY_S` / 凍結 twist_mux timeout の借用・`# TODO(Phase 1 実測)`）で、`now` は tick 冒頭の `time.monotonic()` を使い回す＝CLOCK_MONOTONIC 絶対期限・単調非減少。**専用 QoS = RELIABLE/KEEP_LAST/depth1/VOLATILE**（estop 用 `reliable_qos` depth10 とは別プロファイル・`transient_local` は使わない＝後着購読者への古い許可再配信=fail-open を避ける）。**既存 estop/level/event/operator latch 経路は不変更の additive**。R-26: `tests/unit/test_guardian_stop_state.py`（pure `stop_requested_for` 極性＋配線層 AST pin〔topic 式・型・QoS 4 値・毎 tick publish・now 使い回し・estop publish 非回帰〕）。
- ~~file : `config/twist_mux.yaml`~~ → **`warehouse_bringup/config/twist_mux.yaml` へ移設済（#40 / nav-traffic, doc16 §5）**。値（emergency=100 / nav2=10, timeout 0.5, `/bot{n}/cmd_vel/{emergency,nav2}`）は不変で移設。本パッケージは twist_mux 設定を保持しない。

## 消費 (consume)
- 契約: `warehouse_interfaces.safety`（`MAX_LINEAR_VELOCITY`/`BATTERY_CRITICAL_PCT`/`battery_is_critical`/`clamp_velocity`/`normalize_battery_percent`（#44））、`config.load_config`、**`compat.UTC`（#563 py3.10 互換・py3.11+ では `datetime.UTC` 同一 singleton＝挙動不変。`from datetime import UTC` 直 import は `tests/unit/test_py310_compat.py` source-scan で禁止）**。
- config: `safety.emergency_min_distance`（既存・2台間距離。速度cap とは別概念）, `safety.blocked_timeout`（← **このトラックで `config/warehouse.base.yaml` に追加: 10.0**）, `safety.battery_percentage_scale`（← **#44 で追加: `percent` 既定=fail-safe**）, `safety.pose_freshness_timeout`（← **#126 で追加: 1.0s 暫定**。amcl_pose 鮮度窓。`config/warehouse.base.yaml` 所有=bringup/skeleton へ PR で予告）, **変位ゲート3キー（← doc23 A-5③ で追加。同じく `config/warehouse.base.yaml` 所有=bringup/skeleton へ PR で予告）**: `safety.pose_freshness_motion_epsilon`（0.10 m ＝ 2 × `nav2_params.yaml:58` `update_min_d`）, `safety.pose_freshness_angular_epsilon`（0.4 rad ＝ 2 × `:57` `update_min_a`）, `safety.odom_freshness_timeout`（0.5s ＝ これを超えた odom は不明扱い→ゲート fail-closed）。いずれも Phase-2 実測で確定する暫定値。**`safety.pose_freshness_speed_epsilon` は削除済**（#524 で一度追加したが sim 実走で falsify → doc23 A-5③ に合わせて撤去。下記「速度項の撤去」）。
- topic: `/{bot}/amcl_pose`(PoseWithCovarianceStamped), `/{bot}/battery`(BatteryState), **`/{bot}/odom`(nav_msgs/Odometry ＝ doc03:77 既存契約。doc23 A-5③ 変位ゲートの独立ソース。BEST_EFFORT sensor QoS・`_on_odom` は marshal のみで判定は `gl.PoseGateTracker`)**。bot1 / bot2。 **【2026-09-16】＋ `/{bot}/scan`（sensor_msgs/LaserScan・doc03:78・BEST_EFFORT sensor QoS）を liveness のみ（`_on_scan` は monotonic 到着時刻を記録するだけ・payload 不読）に consume。`_on_odom` は `odom_seen`（sticky 生存証人）も立てる。**
- topic: **`/operator/stop_request`（std_msgs/String JSON・doc03 カタログに additive 追記済＝正本 mode-m1/05 §5・OQ-OP2 裁定）**。単一 global topic（fleet 全体）。`{"action": "engage"}` で latch ON・`{"action": "clear"}` の明示解除のみで OFF。不明・不正 payload は無視（clear 扱いにしない＝fail-safe・warn ログ）。QoS = RELIABLE/KEEP_LAST(10)（比較可能な command 系購読が無いため安全側・`ros2 topic pub` 既定 RELIABLE と互換）。producer は当面手動 `ros2 topic pub`（将来 = teleop 空きボタン・M1 ゲート jstest で index 確定）。

## 実装メモ
- 判定ロジックは rclpy 非依存の `guard_logic.py`（`evaluate`/`build_event`/`BlockTracker`/`distance`/`marshal_battery`（#44 battery scale 正規化＋非有限→last-good）/`EdgeLatch`（#126 立ち上がり検出））に分離 → `tests/unit/test_emergency_guardian.py`（`@pytest.mark.safety`）で ROS 無し検証（reflex 側の battery scale も parity test 済、EdgeLatch の rising/held/clear→recur も検証）。
- #126 edge-trigger: `EdgeLatch.rising()` が活性 `(bot, reason)` 集合の差分で立ち上がりを返し、node はそれだけ `/emergency/event` を発行。**物理停止（zero `Twist` を `/cmd_vel/emergency` へ）と Nav2 cancel は毎 tick 維持（level）**＝twist_mux prio100 入力が 0.5s で失効するため再アサートが必要。event のみ edge 化（estop 力は不変）。
- #126 freshness guard: `evaluate(..., pose_freshness_timeout=...)` が per-bot `pose_age`（node が `_last_pose_t` の monotonic 差分で算出、初回 pose 前は `None`）を判定し、`age > pose_freshness_timeout` で **precautionary estop**（`reason="pose_stale"`, action=estop, detail=`{pose_age, freshness_timeout}`）。localization ロスト疑い時に走行を止める fail-safe（正本: doc12 §「Emergency Guardian — pose freshness guard」/ 根拠 R-39 doc07:249）。`None`（未 localize）は estop しない。物理停止は level で pose 復帰時に自動解除（EdgeLatch 再武装）。scan 鮮度（`source_timeout`）は collision_monitor 責務＝Phase-2 defer（/scan 未購読）。
- doc23 A-5③ 変位ゲート（OQ-11 第1実装スライス）: `pose_stale` を **`pose_age > pose_freshness_timeout` ∧ ゲート開** に限定。ゲートは wheel odom（`/{bot}/odom`＝AMCL と独立）で「前回 pose 到着以降の累積変位 > 0.10m ／ 累積 `|Δyaw|` > 0.4rad」のいずれかで開く（`gl.pose_gate_open` / `gl.PoseGateTracker` / `gl.yaw_from_quaternion`・すべて rclpy 非依存。**速度項は持たない**＝下記）。**AMCL は motion-gated ゆえ駐機中の沈黙が正常**で、CURRENT はこれを誤 estop していた（自己ラッチ）。**odom 不明/stale(`odom_freshness_timeout` 超)/非有限・epsilon 未設定は fail-closed でゲート開＝CURRENT へ縮退**（ゲートは estop を抑止しかできず、足せない）。変位は**経路長の累積**＝単調非減少でラッチ内蔵（速度のみのゲートが作る fail-open limit cycle を排除）。R-26: `tests/unit/test_guardian_displacement_gate.py`（非緩和性＝走行中の発火 tick が CURRENT と一致・駐機・クリープ・fail-closed・ラッチ・厳密 `>` 境界。独立オラクル = upstream `nav2_amcl` の publish 則）。本スライスで `WAREHOUSE__SAFETY__POSE_FRESHNESS_TIMEOUT=999` 回避策を 5ファイル6箇所すべて削除。正本: doc23【2026-08-10 追補】A-5③/A-10 ＋ doc12 末尾【2026-08-17 追補】。
- **速度項の撤去（2026-08-17・sim 実走 P2 FAIL の是正 = code-to-docs）**: 初版が変位項に `|速度| > pose_freshness_speed_epsilon` を OR していたのは **A-5③ が禁じる形**で、Gazebo 実走が二重に falsify した——**出発デッドロック**（駐機明け最初の運動 tick・変位 3.2mm で estop → 速度 0 → AMCL `update_min_d` に届かず → nav goal 5/5 キャンセル）と**ラッチ破れ**（AMCL 死亡下で 12 秒に estop 立ち上がり 17 回・累積変位 0.023m）。速度項・config キー・`BotState.odom_speed`・tracker の速度チャネル・node の twist 読取りを**すべて削除**（`PoseGateTracker.on_odom(bot, x, y, yaw, now)` / `snapshot` は 2-tuple `(disp, dyaw)` に変更＝本パッケージ内部 IF・凍結契約は不変）。トレード: 匍匐前進の発火遅延は `motion_epsilon / v` で **v→0 では非有界**（旧 5.0s 上限は消滅）＝検出は遅れるが失われない。R-26 に**出発（estop 0 件）**・**ラッチ（立ち上がり 1 回のみ）**・クリープ遅延の回帰 unit を追加。**残: 修正後の sim gate 再実走が最終受け入れ（PENDING・human gate）**。
- **P1（駐機）は sim 実証済（2026-08-17）**: 16 分駐機で `pose_age ≈ 960s` に達しても `pose_stale` estop 0 件・AMCL 沈黙も実測（doc23 A-10【2026-08-17 追記②】）。
- #126 progress_checker 委譲（明文化）: nav 実行中の進捗喪失（`FAILED_TO_MAKE_PROGRESS`）は Nav2 `progress_checker`（`nav2_params.yaml:94-97`, 所有=nav-traffic doc16:193）の責務。Guardian の `BlockTracker`（変位ベース blocked→low-harm recovery）は **nav_status 非依存の独立フォールバック**（doc12:254 別系統）として維持。idle 誤検出の解消は nav_status ゲート＝Phase-2（本スライスで `nav2_params.yaml` は無編集）。
- 操作者非常停止要求（mode-m1/05 §5・latch）: `gl.OperatorStopLatch`（rclpy 非依存・注入入力のみ・時計なし）が `/operator/stop_request` payload を `gl.parse_operator_stop_action` で解釈し fleet 全体の latch を保持。node は `BotState.operator_stop_requested` に同一値を全 bot へ供給 → `evaluate` が理由 `operator_stop_request` の **estop を latch 継続中 毎 tick 返す**（既存機構の再利用: prio100 zero-Twist level 再アサート＋毎 tick goal cancel＋event は EdgeLatch で rising edge のみ＋estop ゆえ `/negotiation/abort` も発火）。**既存 3 異常条件（near_collision/battery_critical/pose_stale）の自動解除は不変**（非回帰 R-26）。解除≠走行開始（他理由が残れば停止継続・OQ-OP3 の残 goal 確認は未実装）。R-26: `tests/unit/test_operator_stop_latch.py`＋mutation 4 体（silence 自動解除／malformed→clear／pose_stale 粘着化／bot1 限定）全 KILLED。
- 安全定数は全て `warehouse_interfaces.safety` から import（**0.3/10/20 直書きゼロ**）。距離・blocked_timeout は `load_config`。
- estop は `/bot{n}/cmd_vel/emergency` のみ（`/cmd_vel` 直 publish 禁止, doc15 race）。Nav2 cancel は非ブロッキング `call_async`（dev で Nav2 無し→`service_is_ready()` で no-op）。
- R-40: `main()` で `gc.disable()`/`gc.freeze()`（best-effort。最終防衛は ESP32 Layer 0）。
- scan 鮮度ガード `scan_stale`（doc12 末尾【2026-09-16 追補】(3)・2026-09-16）: `BotState.scan_age`（`/{bot}/scan` の monotonic 到着 age・初回前 None）＋ `BotState.odom_seen`（sticky）を additive 追加し、`evaluate(..., scan_freshness_timeout=)` の (6) ブロックが `scan_age > timeout`（厳密）**または**（`None` ∧ `odom_seen`）で `Decision(estop, "scan_stale", {scan_age, freshness_timeout})`。bot 不在（単騎の bot2）は無音。level・自動解除（`EdgeLatch`）・`/bot{n}/stop_state` へは `stop_requested_for` 経由で自動伝播。config `safety.scan_freshness_timeout`（base 既定 1.0 s＝`collision_monitor.yaml:58` 継承・暫定）は hard-index ＋ `gl.validate_scan_freshness_timeout`（非有限/≤0/非数値は起動拒否）。R-26 = `tests/unit/test_guardian_scan_stale.py`（純ロジック＋AST 配線 pin・5 変異体 KILLED）。**運用上の帰結**: full-stack（`bringup.launch.py`）で odom が流れているのに `/bot1/scan` が一度も来ない（LiDAR 未起動）と bot1 は estop され続ける＝意図した fail-closed。teleop 専用 launch（`m1_teleop.launch.py`）は Guardian を起動しないため影響なし。 **live 検証済（2026-09-16・Humble 1.1.20 rclpy コンテナ・Gazebo なし・[deploy/dev/humble-guardian-livetest/](../../../deploy/dev/humble-guardian-livetest/README.md)）**: 未受信＋odom → `scan_stale`（detail `scan_age: null`）・途絶 → `scan_age` 1.035 s で estop（prio100 zero 20 Hz・`stop_requested=true`）・再開 → 次 tick で解除・bot2 無音。

## 前提・未確定 (TODO)
- # ✅(#44, SAFETY-BLOCKER 解消) battery は config `safety.battery_percentage_scale` で明示スケール＋共有 `normalize_battery_percent` で State Cache と単一正規化（`_on_battery` の raw 転送＋`<=1.0` ヒューリスティックを撤去）。既定 `percent`=fail-safe（誤 estop=安全側、critical 見逃しなし）。**残: Phase 1 で実機 Yahboom の実スケールを計測し config 確定＋実機 estop テスト**（safety.md / doc16 §11）
- # TODO(Phase 2) R-39: /scan or ESP32 Layer0 を 2台間近接の主担当に（amcl_pose は 5-10Hz＝実効 100-200ms stale）
- # ✅(#126) pose 途絶 freshness ガード実装済（`pose_freshness_timeout` 1.0s 暫定 → `pose_stale` precautionary estop。amcl_pose のみ・Guardian policy 層）。残: scan 鮮度（`source_timeout`）は collision_monitor 委譲＝Phase-2 defer（/scan 未購読） → **【2026-09-16 裁定】Humble 1.1.20 では collision_monitor の `source_timeout` は fail-open（停止しない）。scan 途絶停止は本 package の Guardian 拡張 `scan_stale`（consume `/{bot}/scan`・config `safety.scan_freshness_timeout` 既定 1.0s＝yaml:58 継承・event type additive・`odom_seen` 証人付き None 規則）＝**✅ 実装済（`feat/guardian-scan-stale`・`tests/unit/test_guardian_scan_stale.py`）**（仕様 = doc12 末尾【2026-09-16 追補】 (3)）
- # TODO(Phase 2) blocked 検出を Nav2 nav_status でゲート（現状は変位ベース low-harm recovery event のみ＝goal なし idle も誤検出しうる）。progress_checker 委譲は責務を明文化済（#126・下記 ✅）、Guardian 受信の完全委譲は nav_status feed 配線＝Phase-2
- # ✅(#126) /emergency/event の edge-trigger 化（`gl.EdgeLatch`: 立ち上がり `(bot, reason)` のみ発行、解消→再発で再発火）。物理停止 cmd_vel/Nav2 cancel は毎 tick 維持＝level。State Cache 側 active/history 50件 ring は維持。残: 近接の collision_monitor 委譲（**cmd_vel 挿入トポロジは doc12 §「collision_monitor 委譲: cmd_vel 挿入トポロジ」で ✅ 確定 #126**＝twist_mux nav2(prio10) 上流に挟む・emergency prio100 不変。配線 impl は nav-traffic 所有 doc16:193 に触れるため調整で defer）／blocked の progress_checker 完全委譲（nav_status feed＝Phase-2）。**freshness（pose 途絶）・progress_checker 委譲の明文化・collision_monitor トポロジは ✅#126 実装/確定済（上記）**
- ✅(PR#287/#293) negotiation abort → `/negotiation/abort` 実装済（estop 時のみ・上記 produce §13 参照）＝旧 `TODO(Mode-A) defer` は解消。
- ~~# TODO(nav-traffic) twist_mux.yaml を warehouse_bringup/config/ へ移設~~ → 完了（#40, nav-traffic）。本パッケージ `config/` は空。

## 【2026-09-10 追記】終了経路（Humble 正常停止 = exit 0）

`emergency_guardian`（**L1** 反射安全ノード・`.claude/rules/layer-annotation.md`）の `main()` を
`warehouse_teleop/warehouse_teleop/node_runtime.py` が定める **3 規則**へ書き換えた（**参照実装であって
import はしていない**＝依存してよい共有 package は `warehouse_interfaces` / `warehouse_description` の 2 つだけ
`.claude/rules/parallel-workflow.md:71-74`。3 規則をインラインで写す）:

1. 正常停止 = `KeyboardInterrupt`（SIGINT）**または** `ExternalShutdownException`（SIGTERM）を握って normal return。
2. spin 後に ROS context を触る処理は `rclpy.ok()` ガード付き best-effort にする（本ノードは該当なし＝
   `finally` は `destroy_node()` + `try_shutdown()` のみで、終了時に publish しない）。
3. 素の `rclpy.shutdown()` ではなく `rclpy.try_shutdown()`（冪等）。

**実害の根拠**: Jetson 実測（ROS 2 Humble / rclpy 3.3.21・2026-09-10）で rclpy のシグナルハンドラは `finally`
より先に context を破棄するため、旧・教科書パターン（`suppress(KeyboardInterrupt)` ＋ 素の `rclpy.shutdown()`）は
SIGINT / SIGTERM とも **exit 1**（entry point 実行ファイル直呼びで実測）。`deploy/jetson/systemd/warehouse-safety.service:30`
は `Type=simple`＋`Restart=on-failure` なので、通常停止での非ゼロ exit が `status=1/FAILURE` として journal に残り
本物のクラッシュが埋もれる（`Restart=` は stop job には効かないが、stop 以外の終了では `StartLimitIntervalSec=0` と
合わさって L1 が無駄に再起動する）。**本番 `ExecStart` は `ros-exec.sh ros2 run …` 越し（main PID = ラッパ）で、systemd が
見る exit code は未実測＝#634 のボード確認項目**（unit 一覧は [docs/setup/jetson-deploy.md](../../../docs/setup/jetson-deploy.md)）。

**安全は終了時メッセージに依存しない**: estop 権限は 50ms ループが毎 tick 再アサートする
`/bot{n}/cmd_vel/emergency` の zero `Twist`（twist_mux prio 100・level）であって、終了経路で送る何かではない。
R-40 の `gc.disable()` / `gc.freeze()` は従来どおり `main()` 内に残す（挙動不変）。

**テスト**: `tests/unit/test_node_shutdown_lifecycle.py`（repo 全体ラチェット・AST pin）。本ノードは
`KNOWN_UNSAFE_STOP_ON_HUMBLE` baseline から削除済＝以後この形を崩すと `regressed` で CI 赤。

**残件**: 3 規則の共通化先（`warehouse_interfaces` への lazy-import か新 shared package か）は #634 で裁定。裁定まで各 package が 3 規則を写す＝暫定 (b)。参照実装 = `ws/src/warehouse_teleop/warehouse_teleop/node_runtime.py`（import はしない）。本 PR: #638。

## 【2026-09-16 追記】提供 (produce) 追補 — X2 `sensor_health` / 停止距離（**未配線**・Mode Outdoor L1） **【2026-09-18 更新】`sensor_health` は Emergency Guardian へ配線済（`OQ-OD95` = A）＝下記【2026-09-18 追記②】。`stop_distance` は依然 未配線。**

> **末尾追記**（[.claude/rules/status-maintenance.md:26](../../../.claude/rules/status-maintenance.md:26) の原則）: 内容は
> 「提供 (produce)」の追補だが、本ファイルの `:16` / `:22` / `:23` / `:33` は
> [docs/mode-outdoor/05-safety-envelope-and-intervention.md:122](../../../docs/mode-outdoor/05-safety-envelope-and-intervention.md:122) や
> `ws/src/warehouse_teleop/CLAUDE.md` から**行で参照されている**ため、中段挿入せず末尾に足す（#165 行ズレ回避）。

- module: **`warehouse_safety/sensor_health.py`**（**L1** 安全・純ロジック。`rclpy` 非 import・時計は注入・状態は source あたり「最後の観測 + (digest, 連続回数)」で有界）
  - `SourceVerdict`(Enum): `ABSENT` / `STALE` / `FROZEN` / `INVALID` / `OK`（**宣言順 = 優先順**）。複数該当時の優先順は **`ABSENT > STALE > FROZEN > INVALID`**（`healthy` は順序に依らない＝報告する理由をより根本的な方に寄せるだけ）。`enum.StrEnum` は py3.11+ ゆえ使わない（`pyproject.toml:33` py310 floor）。
  - `SourceThresholds(stale_after_s, min_valid_fraction, frozen_repeats)`（frozen dataclass・`__post_init__` 検証: 有限 / `stale_after_s > 0` / `0 < min_valid_fraction <= 1` / `frozen_repeats >= 2`・`bool` 拒否。**既定値なし**＝下記 TODO(契約)）。
  - `SourceObservation(stamp_s, received_monotonic_s, valid_fraction, digest)`。`stamp_s` は**元の計測時刻**（[docs/mode-outdoor/04-perception-sidewalk-and-signals.md:177](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:177) 「変換後も元の計測時刻を維持」）で、鮮度判定には使わない。`digest` は payload 内容の指紋（`None` = この message は指紋を持たない）。**marshalling 則**: 実数でない数値フィールド（`None` / `str` 等）は `NaN` として保持し verdict で決定的に分類する（`received_monotonic_s` 不読→`STALE`・`stamp_s` / `valid_fraction` 不読→`INVALID`）＝**`evaluate` はデータで raise しない**。ただし `bool` と `str|None` 以外の `digest` は**構築時に `ValueError`**（call-site の marshalling バグであり、`True` を `1.0` と読めば garbage message が OK と判定される）。範囲外だが可読な値（`valid_fraction=1.5`）は拒否せず `INVALID`。
  - `HealthReport(verdicts, healthy, health_epoch)`（`verdicts` は read-only mapping）。
  - `SensorHealthMonitor(thresholds)` / `.observe(source, obs)`（未宣言 source → **`KeyError`**）/ `.evaluate(now_monotonic_s) -> HealthReport` / `.required_sources`。必須 source が空の mapping は `healthy` が空虚に True＝fail-open なので **`ValueError`**。
  - 判定則（[docs/mode-outdoor/09-external-review-v3-response.md:72](../../../docs/mode-outdoor/09-external-review-v3-response.md:72) §2-b ＋ [04:174](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:174)）: 未観測 → `ABSENT`／`age = now - received_monotonic_s` が `age < 0` **または** `age > stale_after_s` → `STALE`（**受信側の単調時計**で判定し `stamp_s` では判定しない＝[09:67](../../../docs/mode-outdoor/09-external-review-v3-response.md:67) 規則 (4)・[:60](../../../docs/mode-outdoor/09-external-review-v3-response.md:60) 受信側は単調時計）／同一 `digest` が **stamp 前進を伴って** `frozen_repeats` 連続 → `FROZEN`（「新しい stamp でも同一画像」）／`valid_fraction` が NaN・`[0,1]` 外・`min_valid_fraction` 未満、または `stamp_s` 非有限 → `INVALID`。**負の age を fresh としない**のが要（単調時計は逆行しない＝負 age は「別の時計で記録された」印。呼び出し側が wall/ROS 時刻を入れると age は ~-1.7e9 で上限判定を永久に通過し、死んだ source が OK を出し続ける＝本 module が塞ぐ fail-open そのもの。`guard_logic.PoseGateTracker.snapshot` の閉区間（#684）と同型＝兄弟）。再配信（同一 digest かつ stamp 非前進）**および指紋なし message（`digest is None`）**は**加算も reset もしない**（reset は交互再配信／N フレームごとの digest 省略で fail-open）。
  - `healthy` = **全必須 source が `OK`** のときのみ True。`health_epoch` は「verdict map が前回と変化した評価」でのみ +1（初回は 0）＝古い評価で許可を更新させないための**版**（[09:65](../../../docs/mode-outdoor/09-external-review-v3-response.md:65)・[:67](../../../docs/mode-outdoor/09-external-review-v3-response.md:67) 規則 (1)）。状態 id ではなく版なので、以前と同じ verdict map へ戻っても epoch は進む。非有限 `now` は **raise せず**観測済 source を全て `STALE`（fail-closed。未観測は `ABSENT` のまま＝優先順どおりで、どちらも `healthy=False`）。
- module: **`warehouse_safety/stop_distance.py`**（**L1** 安全・純算術）
  - `required_stop_distance(v, t_total, a_min, margin)` = `v·T_total + v²/(2·a_min) + margin`（[docs/mode-outdoor/09-external-review-v3-response.md:147](../../../docs/mode-outdoor/09-external-review-v3-response.md:147) / [05:135](../../../docs/mode-outdoor/05-safety-envelope-and-intervention.md:135)）。`v = 0` では `margin` のみ。
  - `max_total_latency(d_available, v, a_min, margin)` = 上式を `T_total` について解いた逆関数＝**観測可能距離と停止性能から期限を導く**ための関数（[09:257](../../../docs/mode-outdoor/09-external-review-v3-response.md:257) `OQ-OD97` / `OQ-OD25` 再導出）。返り値は **0 で clamp しない**（`<= 0` は「その速度はその視界では許されない」＝呼び出し側が比較する。clamp すると「反応が瞬時なら辛うじて足りる」と区別できず fail-open）。`v = 0` は予算が無限＝**`ValueError`**（`inf` の期限を返さない）。
  - 設定値の誤り（非数・`bool`・非有限・負・`a_min <= 0`）はすべて **`ValueError`**。安全側の分岐（許可を出さない）を呼び出し側で回すため、数値に吸収しない。
  - **値は一切持たない**: 09:147 の計算例（v=0.5・T=0.30・a_min=0.5・margin=0.15 → 0.55 m）は docs が明記するとおり**レビュー値であって M1 実測ではない**ため、定数化していない。
- **未配線（重要）**: この 2 module は **どこからも import されていない**。置き場（Emergency Guardian の拡張か新 node か）は **`OQ-OD95` の裁定待ち**（[docs/mode-outdoor/09-external-review-v3-response.md:255](../../../docs/mode-outdoor/09-external-review-v3-response.md:255)）。本スライスは topic・ROS parameter・launch・`rclpy` 依存を**一つも追加しない**（[09:211](../../../docs/mode-outdoor/09-external-review-v3-response.md:211) §4 順序 3 が offline で認めている範囲＝「`sensor_health`（stale / NaN / 凍結）判定関数 + R-26」ちょうど）。 **【2026-09-18 解消（`sensor_health` のみ）】**: `OQ-OD95` = **A** が裁定され、`sensor_health` は `emergency_guardian` が import・配線した（下記【2026-09-18 追記②】）。**`stop_distance` は依然どこからも import されていない**（停止距離式の消費者は未定）。
- R-26: `tests/unit/test_sensor_health.py` ＋ `tests/unit/test_stop_distance.py`（計 148 unit・`@pytest.mark.safety`）。独立オラクル = docs のリテラル `0.55` と手計算値（実装から再導出しない＝[docs/architecture/20-dev-quality-and-testing.md:139](../../../docs/architecture/20-dev-quality-and-testing.md:139)）。境界 `>` の独立オラクルは repo idiom＝`guard_logic.PoseGateTracker.snapshot` の厳密 `>`（09:72 は演算子を定めていない）。**mutation 14 体すべて KILLED**（[:140](../../../docs/architecture/20-dev-quality-and-testing.md:140)）: FROZEN 分岐削除／鮮度を `stamp_s` で判定／上限 `>`→`>=`／`healthy` を `any`／epoch を毎回 +1／再配信で run reset／`v²` 項削除／`2·a_min`→`a_min`／`min_valid_fraction` 比較の無効化／**負 age を許容**／**`digest is None` で run を clear**／下限 `<`→`<=`／observation の `bool` 許容／不読フィールドで raise。

### 前提・未確定 (TODO) — 上記 2 module の分

- `# TODO(契約)` **しきい値が docs に無い**: `stale_after_s` / `min_valid_fraction` / `frozen_repeats` の数値は 09 / 05 / 04 のどこにも無い。よって**既定値を置かず constructor 引数**にした（docs に無いしきい値を発明しない＝`.claude/rules/docs-first.md`）。確定は `OQ-OD97`（停止距離式からの導出）＋実測。確定後の置き場（`config/warehouse.base.yaml`＝所有 bringup/skeleton へ予告、または mode 別 profile）も未定。 **【2026-09-18 更新】置き場は決定 = config `perception.terrain.health.{stale_after_s, min_valid_fraction, frozen_repeats}`（コメントアウト placeholder・base に実値なし・[04 追補 ⑬ §4](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)）。数値そのものは依然 未確定＝`OQ-OD4Y-o1`。**
- `# TODO(契約)` **health → 許可の wire 形式が未凍結**: `health_epoch` を載せる先が `/bot{n}/stop_state` の additive 拡張（`session_epoch` / `sequence` / `mode` / `speed_envelope` / `health_epoch` の 5 フィールド）か別 topic かは **`OQ-OD89`** で未決（[docs/mode-outdoor/09-external-review-v3-response.md:249](../../../docs/mode-outdoor/09-external-review-v3-response.md:249)）。本 module は wire を**持たない**（JSON 化も topic 名も無い）＝裁定後に配線側が写す。
- `# TODO(契約)` **モード別の必須 source 集合が未定**: [09:72](../../../docs/mode-outdoor/09-external-review-v3-response.md:72) は scan / cliff_scan / GNSS / depth を挙げるが、AUTO / REMOTE / MANUAL それぞれで何が必須かは未裁定（`OQ-OD91` 運転状態機械＝[:251](../../../docs/mode-outdoor/09-external-review-v3-response.md:251) と併せて決める）。現状は**呼び出し側が渡した集合が必須集合**。

## 【2026-09-16 追記】変位ゲートの odom 鮮度に下限（負 age ＝ 時計不一致 → fail-closed）

`gl.PoseGateTracker.snapshot`（**L1** Safety・`.claude/rules/layer-annotation.md`）の odom 鮮度判定を、上限のみ（`now - t > stale_after` で stale）から **閉区間 `0 <= now - t <= stale_after` のときだけ fresh** へ変更した。単調時計は逆行しないので、**負の age は鮮度の測定値ではなく「`on_odom` の `t` と `snapshot` の `now` が同じ時計で採られていない」証拠**（例: 受信を wall / ROS time ≈1.7e9 s で刻み tick を `time.monotonic()` ≈1e4 s で回す → age は巨大な負 → 上限だけでは**死んだ odom が永遠に fresh** → ゲート閉 → `pose_stale` 抑止＝fail-open）。doc23:349 / doc12:608 の「odom **不明**/stale/非有限は fail-closed でゲート開」の「不明」に負 age と非有限 `now`（NaN は両側の比較が False）を含める＝CURRENT（ゲート無し）へ縮退する向きのみ（restrict-only 不変・estop を足す/早める経路なし）。

- **不変**: 厳密 `>` 境界（`age == stale_after` は fresh）・`age == 0` は fresh（odom callback と tick が同一読みを共有しうる。既存 sim 系 unit は毎 tick この形で、下限を `<` にすると既存 14 本＋§11 の境界 1 本＝15 本が赤）・`stale_after` 非有限の拒否・累積/ラッチ・`on_odom` の非有限 drop・config 3 キー・凍結契約・topic・`/emergency/event` コア形。
- **wired node では到達不能**: `emergency_guardian.py` は `_on_odom` / `_check_safety` の両側を `time.monotonic()` で刻み、single-threaded `rclpy.spin` ゆえ callback が tick に割り込まない＝production は bit-identical。本変更は注入時計 seam（pure tracker を別時計で呼ぶ将来の caller・sim harness）の defence in depth。同型の修正＝branch `feat/safety-sensor-health-stop-distance` の `sensor_health.py`（`age < 0` を STALE）。
- **R-26**: `tests/unit/test_guardian_displacement_gate.py` §11（末尾追記）——wall-vs-monotonic で死 odom が毎 tick `(None, None)` かつ `pose_stale` 発火 tick が CURRENT と同一（STALE_TICK＝鮮度契約由来の独立オラクル）／未来 stamp 4 値／`age == 0` fresh・`-1e-9` stale の境界／`now` = NaN・±inf／時計 skew 下でも走行中の発火 tick 不変（非緩和の回帰 guard・下限の mutant pin ではない）／§12 AST pin: `_on_odom` は tracker に `time.monotonic()` を渡し `header`/`stamp`/`twist` を読まない・`_check_safety` の単一 `now` が `snapshot` に渡る（docstring の「node では到達不能」を機械 pin・`test_guardian_scan_stale.py` の `_on_scan` pin と同型）。fix 前は 8 本赤（再現）→ fix 後 全緑。mutation（実ファイル差替・restore を sha で検証）: M1 下限撤去＝8 本赤・M2 片側境界 2 本（NaN 素通り）＝1 本赤・M3 下限を `<` に＝15 本赤・M4 上限を `<` に＝15・M5 `abs()` 対称窓＝5・M6 被演算子交換＝6・M7 常に `(None, None)`＝17（§§1-10 が捕捉）・M8 `isfinite(stale_after)` 削除＝1（`+inf` leg）＝全 KILLED（レビュー R2 が独立再現）。node mutants（§12 pin）: N1 `_on_odom` が `msg.header.stamp` を刻む＝1 本赤・N2 `_bot_state` が fresh clock を採る＝1 本赤。ゲート: `ruff check` / `ruff format --check` / `pytest tests/unit` 緑・`check_consistency.py` 0 ERROR（C1 STATUS-pin WARN は origin/main の進みで出る既存事象・本 PR 無関係）。
- **同型の残件（本追記では未変更・別裁定）**: `evaluate` の `pose_age > pose_freshness_timeout` と `scan_age > scan_freshness_timeout`（scan_stale・doc12 末尾【2026-09-16 追補】(3)）も上限のみで、負 age は「fresh」扱い（＝estop が消える fail-open の向き・本ゲートの fail-closed 縮退より悪い）。どちらも node が `time.monotonic()` 差分で算出する入力ゆえ現状到達不能だが、pure `evaluate` を別時計で呼ぶ caller には同じ穴。`docs/mode-m1/05-operation-state-and-stop-authority.md:44` の `guard_logic.py:385-416`（EdgeLatch）pin は本変更前から stale（EdgeLatch はその範囲に無く、本追記と main 側 scan_stale 追加でさらに下へずれる＝symbol で指す）＝mode-m1 docs は本レーンの編集境界外につき re-pin は別 PR。branch `feat/safety-sensor-health-stop-distance` は `sensor_health.py` のコメントで `guard_logic.py:407` を「上限のみの repo idiom」として pin しており（main では既に行ズレ・本 PR で前提自体が変わる＝symbol `PoseGateTracker.snapshot` で指し直す）、かつ本ファイル末尾に自身の【2026-09-16 追記】を append するため後 land 側で本節と textual conflict＝要 reconcile。 → **解消（2026-09-16・branch `docs/repin-guardian-symbol-refs`）**: conflict は #684 の union merge で reconcile 済。mode-m1/05:44 と `sensor_health.py` / 本ファイル :90・:98 / `test_sensor_health.py` の `guard_logic.py:407` 参照を symbol（`EdgeLatch` / `PoseGateTracker.snapshot`）へ re-pin。

## 【2026-09-16 追記②】age 不明の裁定（負 age・NaN）— `scan_age` は `None` 同格・`pose_age` は stale 扱い

上記【2026-09-16 追記】の残件 1（`evaluate` の `pose_age` / `scan_age` が上限のみ）を doc12 末尾【2026-09-16 追補】(3) 追記② で裁定し実装した（**L1** Safety・`gl.evaluate`）。負または NaN の arrival age は同一 monotonic 時計の読みではない＝**age 不明**（`gl.age_is_unknown`: `None`／NaN／`< 0`）。① (6) scan 規則: 不明なら `odom_seen` 証人規則（証人あり → `scan_stale` estop・bot 不在 → 無音）＝`None` と同格＝fail-closed。② (4) pose 規則: 不明（受信はあるが読みが信用できない）は **stale 扱い**→ 通常どおり変位ゲートが裁く（駐機＋odom 健全＝ゲート閉なら estop せず・走行中＝ゲート開なら `pose_stale` estop）＝fail-closed。`None`（初回未受信）の doc12:509 免除は不変。③ event `detail.scan_age` / `detail.pose_age` は有限値のみ・不明／非有限は `null`（`gl._reportable_age`・NaN / Infinity を JSON に載せない＝doc12:293。`+inf` は判定不変・detail のみ `inf`→`null`）。有限・非負の age は bit-identical（estop が増える方向のみ）。node は同一 tick の `now` から差分を取るため到達不能（AST pin: `_on_scan` 到着側＝`test_guardian_scan_stale.py`・`_check_safety` の単一 `now`／`_bot_state` に時計読み無し＝`test_guardian_displacement_gate.py` §12）＝注入時計 seam の defence in depth（#684 と同型）。R-26: `tests/unit/test_guardian_scan_stale.py` 末尾（不明 age 5 値 × 証人有無・`None` 同値・`+inf`・有限域の不変・pose: 不明→stale→ゲート裁定／`None` 免除／`+inf`／有限域不変・truth table）＋ mutation（`< 0` 撤去／`isnan` 撤去／証人規則を常 True／detail 正規化撤去／`< 0`→`<= 0`／pose の不明句撤去／`None` 免除撤去 → 赤）。凍結契約・config・topic・event コア形は不変。

## 【2026-09-18 追記】提供 (produce) 追補 — coverage → X2 アダプタ `terrain_health`（**未配線**・Mode Outdoor L1） **【2026-09-18 更新】配線済**（Emergency Guardian が `/{bot}/terrain/coverage` を購読＝下記【2026-09-18 追記②】）。

> **末尾追記**（[.claude/rules/status-maintenance.md:26](../../../.claude/rules/status-maintenance.md:26) の原則）: 本ファイルの `:16` / `:22` / `:23` / `:33` / `:90` / `:98` は他 doc・他ファイルから**行で参照されている**ため、中段挿入せず末尾に足す（#165 行ズレ回避）。
> 正本 = [docs/mode-outdoor/04 追補 ⑪](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1093)（写像表・fail 方向・`OQ-OD4Y-a` 裁定・`OQ-OD95` 裁定資料）。

- module: **`warehouse_safety/terrain_health.py`**（**L1** 安全・純ロジック。`rclpy` / `numpy` 非 import・**時計を読まない**・**しきい値と source 名を持たない**・状態を持たない）
  - `parse_terrain_coverage(payload: str | bytes | bytearray) -> TerrainCoverage | None` — `/bot{n}/terrain/coverage` の JSON（[doc03:323](../../../docs/architecture/03-software-architecture.md:323)）を凍結契約で検証する。契約が拒否したもの（壊れた JSON・空・必須欠落・未知 `TerrainState`・`NaN` / `Infinity` トークン・`valid_fraction` 範囲外・`str` / `bytes` 以外の型）は **`None`**。**raise しない・log しない**（[04:482](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:482)「`ValidationError` は safety loop の外で捕捉」の実装点）。
  - `coverage_observation(payload, received_monotonic_s) -> SourceObservation` — 写像は `source_stamp_s → stamp_s` / `quality.valid_fraction → valid_fraction` / `quality.frame_digest → digest`（[04 追補 ④ 1-2](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:411)）。`received_monotonic_s` は**呼び出し側の単調時計を素通し**（[09:67](../../../docs/mode-outdoor/09-external-review-v3-response.md:67) 規則 (4)。補正・`abs()`・時計読みを行わない）。parse 失敗時は `stamp_s = NaN` / `valid_fraction = NaN` / `digest = None` ＝ X2 の既存規則で **`INVALID`**（`0.0` にしないのは「読めなかった」と「無効 0」を潰さないため＝[04:487](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:487) `OQ-OD4Y-b`）。`digest = None` ゆえ壊れた frame は**凍結 run を消さない**（合成 digest は fail-open）。
  - **写さない**: `state`（coverage は品質の証人であって停止判断ではない＝[04:203](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:203) / [:222](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:222)。`DROP_DETECTED` は健康なセンサの報告）／`quality.ground_*` 5 field（閾値化は [`OQ-OD4Z-d3`](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:899) の別レーン）／`device_frame_seq`・`processing_latency_s`・幾何 field（X2 の 4 入力に対応物なし）。
  - `OQ-OD4Y-a` 裁定（2026-09-18）: **両側の field 名は据え置き**（改名は破壊的＝[parallel-workflow.md §7.2](../../../.claude/rules/parallel-workflow.md:192)）。綴りの差は本 module 1 か所が吸収する＝[04:486](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:486) 同一行。
- **消費 (consume)**: 凍結契約 **`warehouse_interfaces.perception.TerrainCoverage` / `ObservationQuality` / `TerrainState`**（契約ハブへの一方向依存・`package.xml` の `exec_depend warehouse_interfaces` は既存＝追加なし）。topic は購読しない（未配線）。
- **未配線（重要）**: `terrain_health` は `sensor_health` / `stop_distance` と同じく**どこからも import されていない**。誰が `/bot{n}/terrain/coverage` を購読し `monitor.observe(<name>, obs)` を呼ぶかは **`OQ-OD95`**（[09:255](../../../docs/mode-outdoor/09-external-review-v3-response.md:255)）の裁定待ちで、A（Guardian 拡張）/ B（新 node）の対称比較と**推奨 A** は [04 追補 ⑪ §4](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1093)。本スライスは topic・ROS parameter・launch・`setup.py` エントリ・`rclpy` 依存を**一つも追加しない**。 **【2026-09-18 解消】**: `OQ-OD95` = **A**（Guardian 拡張）に裁定され、`emergency_guardian._on_terrain_coverage` が `/{bot}/terrain/coverage` を購読して `coverage_observation` → `SensorHealthMonitor.observe("cliff_scan", …)` を呼ぶ（下記【2026-09-18 追記②】）。
- R-26: `tests/unit/test_terrain_health.py`（**54 unit**・`@pytest.mark.safety` + `unit`）。独立オラクル = **手書き JSON リテラル**（`model_dump_json()` で作らない）＋ docs の規則。**mutation 10 体すべて KILLED**（実ファイル差替・sha256 で復元検証）: `digest` に `state`／parse 失敗で raise／失敗時 `valid_fraction = 0.0`／`stamp_s` に受信時刻／`bytes` を decode しない／**`bytes` を `errors="ignore"` で decode してから検証**（#722 stage-2 指摘・UTF-8 不正 bytes が黙って修復され `OK` になる穴）／失敗時に合成 `digest`／`ground_inlier_fraction` を `valid_fraction` の代用／受信時刻を `abs()` で補正／失敗時 `stamp_s` に受信時刻。

### 前提・未確定 (TODO) — `terrain_health` の分

- `# TODO(契約)` **coverage が代表する source 名が未定** = `OQ-OD4Y-m1`（[04 追補 ⑪ §5](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1093)）。[09:72](../../../docs/mode-outdoor/09-external-review-v3-response.md:72) の列挙（scan / cliff_scan / GNSS / depth）に coverage は無い。よって module は **source 名の定数を持たず**、呼び出し側が与える。推奨は「`cliff_scan` の品質証人」（採否は `OQ-OD95` / `OQ-OD91` と同時）。 **【2026-09-18 裁定済】= `"cliff_scan"`**（[04 追補 ⑬ §6](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)）。定数は `emergency_guardian.TERRAIN_SOURCE` **1 か所**で、本 module は引き続き名前を持たない。
- `# TODO(契約)` **`age = now − source_stamp_s` の置き場が未定** = `OQ-OD4Y-m2`。[04 追補 ④ §2 1.](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:477) が消費側に課す入力妥当性検査は、X2 の staleness（受信側単調時計）とは**別の検査**で、本 module では果たされない。
- `# TODO(契約)` **`state` / `ground_*` を health に持ち込むか** = `OQ-OD4Y-m3` / [`OQ-OD4Z-d3`](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:899)。現状は写さない（発明しない）。
- `# TODO(契約)` **拒否 payload の可観測性** = `OQ-OD4Y-m4`。純関数ゆえ log を出さず、下流には `INVALID` としてしか現れない。記録の担当は X1（[04:222](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:222)）で、数える／`detail` に載せる／何もしないの選択は配線裁定と同時。
- しきい値（`OQ-OD97`）・wire 形式（`OQ-OD89`）・モード別必須 source 集合（`OQ-OD91`）は上記【2026-09-16 追記】の TODO 3 件と**同一**（本 module でも未解決・重複記載しない）。

## 【2026-09-18 追記②】配線 — `OQ-OD95` = **A**: X2 `sensor_health` → Emergency Guardian の estop 理由 `terrain_health`（**L1**）

> **末尾追記**（[.claude/rules/status-maintenance.md:26](../../../.claude/rules/status-maintenance.md:26) の原則）: 本ファイルの `:16` / `:22` / `:23` / `:33` / `:90` / `:98` / `:115` は他 doc・他ファイルから**行で参照されている**ため、中段挿入せず末尾に足す（[#165](../../../docs/dev/03-retrospectives.md) 行ズレ回避）。既存節への反映は**同一行追記**のみ（行数不変）。
> 正本 = [docs/mode-outdoor/04 追補 ⑬](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)（裁定・規則表・config キー・fail 方向・OPEN QUESTIONS）。裁定はユーザー（2026-09-18）。

**消費 (consume)**

- topic: **`/{bot}/terrain/coverage`**（`std_msgs/String` JSON = [doc03:323](../../../docs/architecture/03-software-architecture.md:323)）。QoS = **RELIABLE / KEEP_LAST / depth 10**（node 内の既存 `reliable_qos`）＝producer の既定 depth 10 プロファイルに合わせる（[doc03:325](../../../docs/architecture/03-software-architecture.md:325)）。sensor 系の BEST_EFFORT は**使わない**（落ちた message が「流れの欠け」に見えてしまう）。
- config: **`perception.terrain.enabled`**（gate・producer / `collision_monitor` の `cliff_scan` source と**同一キー**を同じ `bool()` で読む＝[04 追補 ⑫](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1244)）＋ **`perception.terrain.health.{stale_after_s, min_valid_fraction, frozen_repeats}`**（gate が true のときだけ **hard-index**。欠落は起動時 `KeyError`・範囲外/非有限/`bool` は `SourceThresholds` が `ValueError`）。**base に実値は 1 つも無い**（コメントアウト placeholder のみ・`OQ-OD4Y-o1`）。
- module: `warehouse_safety.terrain_health.coverage_observation`（#722 アダプタ）/ `warehouse_safety.sensor_health.SensorHealthMonitor`・`SourceThresholds`（#680 判定核）。**どちらも 1 文字も変更していない**。

**提供 (produce)**

- `/emergency/event` の `type` 新値 **`terrain_health`**（additive・コア形不変）。`detail` = `{"source": str|null, "verdict": str, "health_epoch": int|null}` — 3 値とも `str` / `int` / `null` **型で** JSON 安全（NaN / Infinity が構造的に入らないので `_reportable_age` 相当は不要）。
- `Decision.reason` 新値 `terrain_health`（`action == "estop"`）→ 既存 `gl.stop_requested_for` 経由で **`/{bot}/stop_state` へ自動伝播**（L0' 停止上乗せ有効時は teleop 経路も止まる）。**新 topic・新 publisher・新 ROS parameter・新 launch エントリ・`setup.py` エントリはゼロ。**

**実装**

- `emergency_guardian.TERRAIN_SOURCE = "cliff_scan"` — `OQ-OD4Y-m1` の裁定（[04 追補 ⑬ §6](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)）。**名前はこの 1 か所だけ**が知る（`terrain_health` は名前を持たない・`sensor_health` は汎用・`guard_logic` は `BotState` のラベルとして受け取るだけ）＝**名前は wiring の事実で rule の事実ではない**。
- `emergency_guardian.__init__` — gate が真のときだけ per-bot `SensorHealthMonitor({TERRAIN_SOURCE: limits})` を作り購読する。偽なら monitor も subscription も**作らない**＝**今日と bit 等価**（base 既定 OFF・dev / Gazebo は depth camera 無し）。
- `emergency_guardian._on_terrain_coverage` — **marshal only**（`now = time.monotonic()` → `observe(TERRAIN_SOURCE, coverage_observation(msg.data, now))`）。parse も判定も log もしない。`coverage_observation` は payload で raise しないので、壊れた producer が executor に例外を投げる経路が無い。
- `emergency_guardian._bot_state` — tick の**同じ `now`** で bot ごとに **1 回だけ** `monitor.evaluate(now)`（`health_epoch` は状態なので二重評価すると版が二重に進む）。時計はここで読まない。
- `gl.BotState` — additive 3 field `terrain_source` / `terrain_verdict` / `terrain_health_epoch`（既定すべて `None` ＝安全な不在）。`gl.evaluate` — **(7) ブロック**（`None` / `"OK"` → 無音・`"ABSENT"` は `odom_seen` 証人があるときだけ estop・他はすべて estop）。**しきい値引数は増やさない**（窓は monitor 側）＝1 プロセスに並ぶ 2 つの鮮度語彙を**混ぜない**ことで解いた（[04 追補 ⑬ §3](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)）。`guard_logic` は `sensor_health` を **import せず綴りで比較**するので、**未知の綴りは `"OK"` ではない＝estop**（fail-closed by construction）。
- R-26: `tests/unit/test_guardian_terrain_health.py`（**60 unit**・`@pytest.mark.safety`）。独立オラクル = 追補 ⑬ §3 の規則表と §5 の fail 方向のリテラル（module から再導出しない）。3 部構成 = PURE（規則表の全行 ＋ additive 性 ＋ level / 自動解除 ＋ JSON 安全性 ＋ EdgeLatch）／**END-TO-END 純経路**（手書き JSON → `coverage_observation` → `SensorHealthMonitor`（**test-local しきい値**＝docs が値を持たないため）→ `evaluate` → `Decision`。沈黙 / 凍結 / 壊れた payload / 低有効率 / 未観測 / `DROP_DETECTED` / 非有限時計）／**AST pin**（gate 無しでは購読も monitor も作らない・topic 文字列・`reliable_qos`・callback が `evaluate` も `json` も `get_logger` も呼ばない・3 キーの hard-index・`TERRAIN_SOURCE` の値と唯一性・`_bot_state` に時計読み無し・`_check_safety` は `now` 1 本 / `evaluate` 1 回のまま・terrain publisher 不在・`guard_logic` が `sensor_health` を import しない）。
- **mutation 12 体すべて KILLED**（実ファイル差替・各体のあと `git diff --quiet` で復元検証・harness は `tests/unit/test_guardian_terrain_health.py` ＋ 既存 `test_guardian_{scan_stale,stop_state,displacement_gate}.py` を回す）: `ABSENT` を無条件 estop（odom 証人を落とす・2 赤）／証人規則を全 verdict へ拡大（証人が無ければ無音・10 赤）／`INVALID` を健康扱い（非 OK の allow-list 化・15 赤）／未知の綴りを fail-OPEN（既知 4 名のみ estop・7 赤）／gate を無視して常に購読・常に monitor 生成（1 赤）／しきい値を `.get` 既定で埋める（1 赤）／callback 内で `evaluate`（1 赤）／`_bot_state` が時計を読み直す（2 赤）／base config に実値を入れる（1 赤）／source ラベルを `guard_logic` 内で発明（2 赤）／**`_bot_state` の marshalling 2 体（#728 stage-2 レビュー指摘で追加）= `report.verdicts[…].name`→`.value`（小文字綴り＝規則 (7) から見て未知＝**恒久 estop**）／`terrain_verdict` を `"OK"` 固定（判定を捨てる＝**fail-OPEN**）**——どちらも kwarg 名だけの pin では全緑を素通りしており、値の式を pin して赤にした（各 1 赤）。
- 既存 `tests/unit/test_guardian_*`（scan_stale / displacement gate / stop_state / mirror wiring）は**全緑のまま**・`test_node_shutdown_lifecycle.py` のラチェット baseline は不変。

### 前提・未確定 (TODO) — 本配線の分

- `# TODO(契約)` **しきい値 3 本の実値** = `OQ-OD4Y-o1`（[04 追補 ⑬ §7](../../../docs/mode-outdoor/04-perception-sidewalk-and-signals.md:1270)）。`scan_freshness_timeout` のような継承元（`collision_monitor.yaml:58`）が無い——coverage は depth frame ごとで**カメラ FPS 依存**、そのカメラが未購入（`OQ-OD4Y-i4`）。`OQ-OD97` は heartbeat 途絶判定の再導出であって本 3 本を与えないので、別 OQ に分けた。
- `# TODO(契約)` **producer gate と Guardian gate の分離** = `OQ-OD4Y-o2`。現在 1 キーで 3 つ（producer / `collision_monitor` source / Guardian health）が同時に arming される。producer だけ試す運用ができない代償と、分けたときの「誰も健康を見ていない」fail-open の余地を比べて未決。
- `# TODO(契約)` **`health_epoch` を permit（`/bot{n}/stop_state`）へ載せるか** = `OQ-OD89` / `OQ-OD4Y-o3`。本 PR は `/emergency/event` の `detail` に載せて**観測可能にしただけ**で、wire には載せない（[05:131](../../../docs/mode-outdoor/05-safety-envelope-and-intervention.md:131) の二重経路のうち停止側のみ）。
- `# TODO(契約)` **モード別の必須 source 集合** = `OQ-OD91` / `OQ-OD4Y-o4`。本配線の必須集合は `{cliff_scan}` 固定で、`scan`（`scan_stale`）と同じ monitor に統合するかも未決。
- `# TODO(契約)` **拒否 payload の可観測性** = `OQ-OD4Y-m4` / `OQ-OD4Y-o5`。parse 失敗を数えず log もしないので、「なぜ `INVALID` か」は外から区別できない。
- `OQ-OD4Y-m2`（`age = now − source_stamp_s` の入力妥当性検査の置き場）は**本配線でも解いていない**。Guardian は受信側単調時計だけで判定する。
- **`stop_distance.py` は依然 未配線**（本 PR の射程外）。
