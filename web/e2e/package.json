{
  "name": "mwr-web-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E smoke for web/console (Web Observability SPA, doc22 §15)",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "serve": "^14.2.4"
  }
}
