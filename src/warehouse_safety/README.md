# warehouse_safety

Emergency Guardian（50ms安全監視・LLM非経由）・twist_mux（Emergency=100/Nav2=10）・State Cache（file + /state_cache/snapshot）。

> Phase 0.5 以降に ament パッケージとして実体化（package.xml / setup.py 追加）。
> 詳細は [../README.md](../README.md) と各設計ドキュメントを参照。
