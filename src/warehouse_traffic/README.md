# warehouse_traffic

TrafficManager 実装。NoTrafficManager(none) / SimpleTrafficManager(simple) / RMFTrafficManager(open-rmf, Fleet Adapter)。

> Phase 0.5 以降に ament パッケージとして実体化（package.xml / setup.py 追加）。
> 詳細は [../README.md](../README.md) と各設計ドキュメントを参照。
