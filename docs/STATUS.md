# プロジェクト現況（STATUS）

最終更新: 2026-09-10

> 本書は**現況スナップショット**（随時更新）。詳細の正本は各 doc / `.claude/rules/`、リスクは [07-research-notes](shared/07-research-notes.md)。docs 中心主義は [rules/docs-first.md](../.claude/rules/docs-first.md)。

## サマリ
実機未到着のため**ソフト・基盤・ドキュメントを先行整備**中（doc06 方針）。
**doc17 Step 0（リポジトリ骨格＋契約凍結）完了**（2026-05-29, Issue #1 closed）。
**Step 1＝各トラックの並列実装フェーズが進行中**。#4 llm-bridge S1 / #8 nav-traffic / #6 wo / #5 safety-state / #7 sim / #25 gen_id 冪等化 / Mode C 契約(#66) を並列実装し、**Phase 0.5 スライスが main に land 済**（下表）。**S2-PR1 も land 済**: Bridge-owned Langfuse trace(#78) / wo v4 `create_score`(#83) / nav2_bridge REST→BasicNavigator part1(#86)。**Langfuse trace 所有 #73 は完了 → close、Phase3 実トレース検証は #88 に分離**。次ラウンドは4並列: #4 S2-PR2（実 dispatch + MCP→nav2_bridge REST）/ #76 sim `/clock`+占有マップ（critical-path, #67 解錠）/ #6 wo node-wiring / #75 bringup 合成 + #2 jetson。
**【現況 2026-06-03 更新】**上記のうち **#76 sim `/clock`+占有マップ(#94, critical-path) / #6 wo node-wiring(#92) / #75 bringup 合成(#93)** は **3 本ともマージ済**、**#2 jetson deploy scaffold(#96) もマージ済**。**#4 S2-PR2 HALF B（実 dispatch + MCP→nav2_bridge REST）=#104 / docs CTRV(08a)=#100 もマージ済**。派生 issue **#101(CTRV実装・#100 merge で解錠)/#102(history充填+current_task)/#103(Hermes Memory)** を起票。その後 **#105(STATUS鮮度)/#106(jetson safety doc)/#107(doc03 内部トピック表網羅, closes #61)/#110(CTRV 実装, closes #101)/#111(Phase4 比較公平性＝memory/skills 強制 OFF の運用契約を docs 化。#103 本体は未実装 OPEN)/#113(nav-traffic #67 E2E gate 修正)** もマージ済 → さらに **#115（#102 短期記憶 + #108/#109 cross-lane Langfuse join 修正、closes #102/#108/#109）/ #118（sim odom→base_link TF, closes #112）/ #119（nav2 MPPI footprint, #67 E2E）/ #121（#44 battery scale 単一正規化・contract additive）/ #120（STATUS 鮮度）** も land → **main=`5814f08`、open PR 0**。**【2026-06-04 並列ラウンド land】**4レーン: **#128**(L3 #55 deadlock を `status=="blocked"` 依存→凍結 RobotState 信号へ再基礎付け＝option b)/**#130**(L1 #67 follow-up nav2_params・11a doc-drift)/**#129**(L4 #88 subset Phase3-4 比較指標 docs 凍結)/**#132**(L2 #116 R-37 host spike＝根本原因 client_key 衝突を再現＋distinct key で単一 Agent 2台双方向 OK 実証＝**R-37 de-risk**)。**#67 は close**（コア 2台 E2E 達成・Phase2 残=#125 margin/#124 隘路）。新 follow-up: #124/#125/#126(R-39)/#127(jetson fidelity)/#131(#55 波及同期)/#133(wo Grok cost code)。稼働は H（#103）。R-37/R-44 tracking=#116(✅#132)/#117。#91 で **docs-first 実装ゲート**（[parallel-workflow.md §1.1](../.claude/rules/parallel-workflow.md)：着手前 file:line 引用 → 実装中 CLAUDE.md 記録 → 完了前 `check_consistency.py` 0ERROR + `/consistency-audit` + 未決列挙）が**完了条件として必須化**された。

**【現況 2026-08-30 更新】** M1 の一次資料（公式回路図 / プロトコル V2 / STM32 V3.6.5 / 全25頁説明書 / NVIDIA CAD）の裏取りが **#559 で main に land**。同時に doc02 §A が ESP32 Car ×2 の旧記述から **ROSMASTER M1（単騎・メカナム・USB シリアル直結）** へ揃い、「現行実機がどれか」を doc の冒頭で取り違える余地が消えた。ベンダー配布コードの位置づけは **実装依存ではなく仕様の裏取り資料**と裁定済（doc02 P-7e）。

## main の状態（CI 常時 green, origin/main = `1937cb6`）
- **契約ハブ `warehouse_interfaces`（凍結）**: pydantic `Situation`/`Command`/`Proposal`(+ `gen_id`、`CommandItem.idempotency_key`)、`StateSnapshot`/`RobotSnapshot`、`KNOWN_LOCATIONS` 9キー、共有パス（doc16 §4）、`StateStore`/`GenStore`/`IdempotencyStore` IF + file 実装、`safety`（速度/battery しきい値の単一ソース）、`config`。
- **実装済みノード（main 入り）**:
  - `warehouse_mcp_server` — 7ツール + Policy Gate + gen_id B-3 + **per-call idempotency 強制**（#35/#41。偽 store でユニット検証、MCP SDK は遅延 import の pip extra）。
  - `warehouse_state` / `warehouse_safety` — State Cache 100ms（`StateSnapshot` 形で `state.json` を書く producer）+ Emergency Guardian 50ms reflex（#39。偽入力でユニット検証）。
  - `warehouse_sim` / `warehouse_description` — 1.8×0.9 world 単一定数生成 + minicar URDF（凍結フレーム `bot{n}/base_link→{lidar_link,imu_link}`）+ `sim.launch.py`（bot1/bot2 spawn）（#43）。`/clock`/`sim_time`/占有マップは #94 で main 入り、2台 Gazebo Nav2 E2E のコアゲート #67 は close 済み。
  - `warehouse_llm_bridge` — `llm_client`(ABC) / `action_map`（Command→ToolCall 変換、`gen_id` は注入・**per-call UUID `idempotency_key` を mint**）（#27/#41）。
- **S1/scaffold が main 入り（#68/#69/#70）**: `warehouse_llm_bridge`（`scheduler`/`hermes_client`/`situation`/`executor`/`llm_bridge` = 司令官サイクル S1、tool dispatch は log stub＝**#104 で実 dispatch に置換済**）、`warehouse_traffic`（TrafficManager None/Simple + VirtualScanNode `/bot{n}/virtual_scan`）、`warehouse_orchestrator`（audit reader + result/task_completion_time KPI + Langfuse seam NO-OP）。
- **`warehouse_nav2_bridge` part1 main 入り（#86）**: REST→BasicNavigator パッケージ（`core`/`backend`/`errors`/`app`/`nav2_bridge`、23 unit ケース）。**MCP→nav2_bridge REST 配線は #104（S2-PR2 HALF B）で完了**。
- **残り `main()` スタブ / 未実装**: なし（`warehouse_teleop` は #158 で keyboard teleop 実装済み）。`warehouse_bringup` の per-bot launch 合成は **#75=#93 で main 入り**。
- **firmware 雛形**（ESP32 micro-ROS、**Layer-0 速度クランプ ≤0.3 m/s**、#23）。実機実装は Phase 1。
- **開発基盤**: Ruff + pytest（安全契約テスト, R-26）+ pre-commit + GitHub Actions CI + governance CI / main-worktree edit guard / docs-first ルール（#45）+ Playwright 雛形（Phase 4 までゲート）。

## 開発体制（正本: parallel-workflow.md / merge-and-communication.md / implementation-and-dependencies.md / docs-first.md / doc17）
- **トランクベース**: 全コードは PR 経由で `main` へ（squash）。**マージ後はブランチ削除**（stale 防止）。**直 main 編集/push 禁止・同一ターン self-merge 禁止**（#31）。
- **docs 中心主義**: docs を正本とし、コードは検証・具現側。docs に無い契約/トピック/スキーマ/しきい値を発明しない（#45, rules/docs-first.md）。**完了条件＝docs-first 実装ゲート**（#91, parallel-workflow.md §1.1）: 完了前に docs 再照合 → `check_consistency.py` 0ERROR → `/consistency-audit` → 残る未決を PR 本文に列挙、を満たして初めて「完了」。
- **dev/stg/prod は実行環境**（`config/<env>` + `WAREHOUSE_ENV`）。Git ブランチではない。昇格はデプロイ。
- **並列 git worktree**。連絡は GitHub Issue/PR に **`[worktree | branch | track]` タグ**必須。
- **依存の扱い**: 各トラックは凍結契約 `warehouse_interfaces` のみに依存し、他トラック内部を import しない。新依存は契約PR(`contract` ラベル)+予告+凍結してから実装。

## トラック状況（epic Issue #1〜#8 + #25）
| # | track | 状態 |
|---|-------|------|
| 1 | skeleton | ✅ **CLOSED**（契約凍結 #22 + 12pkg #24） |
| 2 | jetson | ready（実機不要で着手可） |
| 3 | firmware | 雛形 main 入り（#23）。実機実装は Phase 1 |
| 4 | llm-bridge | 🟢 **S1 + S2-PR1 + S2-PR2 HALF B main 入り（#70/#78/#86/#104）**: 司令官サイクル(Scheduler+HermesClient+Situation+action_map+排他 A/B-3+C) / **Bridge-owned Langfuse trace `tracing.py`(#78)** / **nav2_bridge part1(#86)** / **実 in-process tool dispatch（`WarehouseTools().dispatch` 注入・同一トラック #81）+ MCP→nav2_bridge REST 転送（`nav2_client`・受理時のみ・R-26）(#104)**。MCP 層(#35/#41) 済。**残**: Mode C 忠実化 / Open-RMF 転送経路 / Phase4 起動ハーネスでの実 Hermes memory/skills OFF 配線。**✅完了: 短期記憶 history+current_task=#102(#115) / 予測 CTRV 化=#101(#110) / cross-lane Langfuse live-join=#108・#109(#115) / R-35A=#54(明示 /stop 撤回・Layer A client-side のみ・主担保 B-3+C) / #103 fairness guard(intent-only) / #181 Mode-A prompt+task seed / #192 malformed response cycle ignore**。Langfuse Phase3 検証は #88 |
| 5 | safety-state | 🟢 **実装 main 入り（#39）**: Emergency Guardian 50ms + State Cache 100ms（`StateSnapshot` producer, atomic `state.json`）。残: 実機統合・twist_mux 連携（Phase 2+） |
| 6 | wo | 🟢 **KPI scaffold + Langfuse v4 配線 main 入り（#69/#83）**: audit.jsonl reader + result/task_completion_time + **v4 `create_score` 実配線 + trace_id 導出 + efficiency(#83)**。**node-wiring（provider+gen_id metadata + collector test）= #92 main 入り**（`score_send.py` pure helper 抽出 + `provider` param[`WAREHOUSE_PROVIDER` env] + score metadata に provider/gen_id 追加 + blank-run_id ゲート修正）。**残**: 他 score 名（docs-PR 前提）/ Grok cost(=#88) / Metrics+Experiments。trace_id は seed 導出で契約不要 |
| 7 | sim | 🟢 **環境スパイク GO + 実装 + `/clock`/`sim_time`/占有マップ main 入り（#43/#46/#94）**: world/URDF/`sim.launch.py` + `/clock` bridge + use_sim_time + 占有マップ生成（#76=#94, critical-path 解錠済）。2台 Gazebo Nav2 E2E コアゲート #67 は close 済み。 |
| 8 | nav-traffic | 🟢 **TrafficManager/VirtualScan/nav2_params(#68) + E2E enablement(#82) main 入り**: None/Simple + `/bot{n}/virtual_scan` + DWB→MPPI + twist_mux 移設(#40) + AMCL initialpose seeding + container nav2 provisioning(#82)。2台 Gazebo E2E ゲート **#67 は close 済み**。残は Phase2/実機寄りの collision_monitor 配線・nav_status gating など（#126 系）と、live provider 比較 run の確認。`bringup.launch.py` per-bot 合成 **#75 = #93 main 入り**。 |
| 25 | gen_id UUID冪等化 | ✅ **CLOSED/merged**: 契約(#36 `IdempotencyStore`/`FileIdempotencyStore` + `CommandItem.idempotency_key` + `idempotency_store_path()`) + enforcement(#41) が main 入り。`action_map` が tool-call 毎に per-call UUID を mint（LLM は echo しない＝信頼の非対称性）、`GenChecker.check` が gen→idempotency を強制（replay=`duplicate_command`）、MCP 7ツール全てに `idempotency_key`（per-call UUID）貫通 |

## ⚠️ 要決着事項（latent / 監視）
- **（解決済・参考）R-35(A)** は **Issue #54 で解決（2026-06-05）**＝明示 `POST /v1/runs/{id}/stop` を撤回。採用トランスポートは同期 `chat/completions`（ステートレス・run_id 無し）かつ tool dispatch は Bridge 仲介の in-process（doc08 採用実装📌・#104）でサーバー側 tool 実行が無いため、止めるべき run／tool 実行が存在しない。Layer A はクライアント側キャンセル（`asyncio.wait_for`）のみ、安全主担保は B-3（古い世代 reject）+ C（冪等キー）。#41 は R-35(B) 冪等化を解決済（同一世代 replay を `duplicate_command` で拒否）。unit: `test_stale_call_rejected_when_stop_noop_54` / `test_end_to_end_stale_generation_rejected` / `test_older_generation_rejected`（R-26）。
- **（解決済・参考）実行モデル**（Bridge mint vs LLM echo）の未確定は #41 で確定済: **`action_map` が per-call UUID を mint・LLM は触らない**（信頼の非対称性。doc08 §C / doc15 §競合状態の防止）。
- **（決定・実装済）Langfuse trace 所有 + provider access**（#72 docs / #78 Bridge seam / #83 wo wiring。**#73 は close、Phase3 実トレース検証は #88**）: trace 所有は **Bridge**（`langfuse.openai` + `base_url`=Hermes、Hermes 内蔵 Langfuse は無効化）。score は v4 `create_score`（v2 `langfuse.score` 廃止）。`trace_id`=32hex no-dash、#4↔#6 は `create_trace_id(seed)` 導出で契約不要、audit 突合は `gen_id`+timestamp。**live join は従来 no-op だった**（#108: Bridge が `WAREHOUSE_RUN_ID` 未読＝#4↔#6 trace_id 不一致 / #109: MCP executed audit 行に `gen_id` 欠落＝WO score join 不成立）が **#115 で修正・MERGED（#108/#109 close）＝Phase3/4 比較検証の前提が成立済**（`resolve_run_id`=WAREHOUSE_RUN_ID 起点 + executed 行 `detail.gen_id`、cross-leg test 済）。**Vertex AI SDK は不採用・Hermes 単一経路維持**（doc13 §7.5/§7.6）。

## 次の山（kickoff brief: `~/Developer/mwr-handoff/round-2026-06-04/`）
**直近マージ済（2026-06-03）**: #94 sim `/clock`+`sim_time`+占有マップ（critical-path 解錠） / #92 wo node-wiring / #93 bringup.launch.py 合成 / #96 jetson deploy scaffold / **#104 #4 S2-PR2 HALF B（実 dispatch+MCP→nav2_bridge REST 転送）** / **#100 docs CTRV（08a linear→CTRV）** / **#105 STATUS 鮮度** / **#106 jetson safety doc（Requires→BindsTo）** / **#107 doc03 内部トピック表網羅（closes #61）** / **#110 CTRV 実装（closes #101）** ＋ **#111(Phase4公平性docs)/#113(nav #67 E2E gate)/#114(CTRV横展開)/#115(短期記憶#102+#108/#109 cross-lane join)/#118(sim odom→base_link TF, closes #112)/#119(nav2 MPPI footprint #67)/#121(#44 battery scale 単一正規化)/#120(STATUS鮮度)** も land 済（→ `83eec5b`）。**2026-06-04 並列ラウンド: #128(L3 deadlock)/#130(L1 doc-drift)/#129(L4 比較指標凍結)/#132(L2 R-37 spike) land → 当時 main=`5814f08`**。 **【2026-06-05 round2/3 land】** #134-136/#138-144/#149-154/#157 land（#103 Hermes Memory fairness guard / #124 真200mm隘路 / #125 yield ≥0.15m を **live 0.2317m** で実証 / #126 edge-trigger+pose-freshness+collision_monitor cmd_vel 挿入トポロジ docs(#157) / #131 deadlock 波及同期 / #133 wo Grok cost / #140 .claude Langfuse / #154 create-issue skill）→ **当時 main=`7185955`**。**CLOSED: #103/#124/#125**。**OPEN: #126**（edge-trigger+pose-freshness+cmd_vel 挿入トポロジ docs 確定（#157, doc12:522 §collision_monitor 委譲）land・collision_monitor 配線 impl は nav-traffic hand-off で defer・nav_status gating Phase2）。**LLM-in-Gazebo 統合 capstone の部品が全て揃った**（Nav2 2台E2E#67 ＋ 真隘路#124 ＋ yield ≥0.15m 機械実証#153 ＋ Hermes Memory fairness#103）＝次の大物。

**残り（着手待ち / 実測待ち）**:
- **#156 / slice3 live** — **全 slice land 済（origin/main=`1937cb6`）**: slice1 統合 launch 合成（#162）＋ slice2 commander-cycle wiring（#181 task injection + Mode-A prompt / #192 malformed cycle ignore、両 CLOSED）＋ **slice3-live = #218 MERGED（2026-06-10）**: SCENARIO 整合 seed（`head_on`=sim `head_on_spawn_poses` 導出・導出失敗時は **fail-hard exit 2** で berth 誤 seed を拒否）＋ runbook demo-breaker 2件修正（Nav2 Bridge :8645 は in-process 合成＝外部起動禁止 / ≥0.15m は当時 座標ゴール injector 未実装）＋ host 24 tests。**PENDING（人間/secret ゲート・live のみ）**: 実 Hermes/4-provider の yield 判断＋RViz/noVNC mp4 録画＋L6 サイクル長。**≥0.15m 座標ゴール経路は実装・文書化済（DEFERRED 解消）**: #230 MERGED（`Nav2BridgeCore.navigate` に additive 座標 `goal=(x,y[,yaw])`＝XOR・yaw drop・`INVALID_GOAL`、`HeadOnInjector` が route_A 排他で head-on swap を直列化）＋ #236 MERGED（doc12a に座標ゴール variant/`INVALID_GOAL` を EOF 追補）。**残るゲートは LIVE ≥0.15m 計測のみ**（user-docker・`WAREHOUSE_MINSEP_STREAMS` でゲート＝Gazebo 物理が要る）。**#223 / #156 は OPEN 継続**（live 録画/計測＝人間ゲート）。
- **#193 jetson** — deploy scaffold #96 main 入り。G0-G7 runbook + `deploy/jetson/bin/preflight.sh` を整備済み（到着前 `--offline` / 到着後 `--arrival --gates G0,G1,G7`）。実測値は Jetson 到着後に記録する（#2 本体は close 済み）。
- 派生 issue: **#101**(CTRV ✅#110) / **#102**(history充填+current_task ✅#115) / **#103**(fairness guard intent-only ✅。実 Hermes memory/skills OFF 配線は Phase4 起動ハーネス責務) / **#192**(malformed response cycle ignore ✅本修正)。
- **latent 整合（✅解消済）**: #108/#109（Langfuse live-join 二重故障）は **#115 で修正・close**（`resolve_run_id`=WAREHOUSE_RUN_ID 起点 + executed 行 `gen_id`、cross-leg test 済）＝Phase3/4 比較検証の前提が成立。

- マージ順（doc17 §6）: skeleton(#75/#93) → 独立トラック(#92 wo / #4) 随時 → **sim(#76/#94, critical-path)** → nav-traffic(#67 E2E) → 統合。
- 既知の設計リスクは [07-research-notes](shared/07-research-notes.md) R-35〜R-52。

## git 衛生（現況, 2026-09-10）

### 2026-09-10 round 境界 refresh (4)（open PR 全消化＝jetson Build/Deploy/Run 記録一式 #646/#650/#647/#648 ＋ ADR-0010 car_type 実測クローズ #631 ＋ LLM 表示面 emergency cap #610・6 PR land / open PR 0）

- main ref = `origin/main`(`1937cb6`) 同期（`67d7f69`→`1937cb6`・`refresh_status.py --fix` @ python3.12・13 pin 行書換・履歴 SHA 不触。#651(144cbbe)=前回 refresh 自体は land 列挙から除外。#618(6835412) は前回 pin 後・本ラウンド開始前に別セッションが land させたため本ブロックで初計上）。
- **2026-09-10 land (4)（`67d7f69`→`1937cb6`・新しい順）**: #610(1937cb6 [llm-bridge] **LLM 表示面 2 面（`situation.py` / `get_fleet_status`）の emergency `history` を直近 10 件に切詰め**〔単一定数 `warehouse_mcp_server.tools.EMERGENCY_HISTORY_LLM_MAX=10`・出所は doc08 追補（`docs/architecture/08-llm-bridge-common.md:560`/`:564`、10 は暫定値）・**state.json 側 50 件 ring と producer `warehouse_state/aggregator.py` は不変**＝安全判断の入力は細らない（`self_action_gate.py:192` は store 直読で本経路を通らない）・`warehouse_interfaces` 不触＝`contract` ラベル不要〕) / #631(f10d08b [m1] **ADR-0010 Open 1 クローズ**〔実機 car_type を 0x02→**0x0A** 書込・FW 3.6・電源断保持を時刻付き実測で記録〕＋`backend.py`/`driver_node.py` の car_type コメント同期〔**実ロジック差分ゼロ**＝`car_type>=0` センチネル・送信順序とも不変〕・**契約値 0.3 m/s は不変**〔`docs/adr/0010-raise-speed-cap-to-platform-max.md:86`/`:107` 明記・0.7 は contract PR 待ちの pin 候補〕) / #648(b1827c1 [docs] **`.claude/rules/build-deploy-run.md` 新設**＋root `CLAUDE.md`/`.claude/skills/build.md`/`.agents/skills/build/SKILL.md` ポインタ〔素の `colcon build` を打たない・記録付き build/走行を常時ロード規約化〕・`.claude/settings.json`/`hooks`/`.github` は不触＝自己配線なし) / #647(052a0e8 [jetson] **`deploy/jetson/bin/{build.sh,record-run.sh,mwr_build.py,mwr_run_record.py}`** ＋ preflight/status hooks ＋ host unit 2 本〔`tests/unit/` 配置＝CI 可視・rosdep 未解決は `"ok"` でなく `"error"`＝**fail-closed**・ビルド失敗時は `install/.mwr-build-info.json` を書かない・`rm -rf`/`sudo`/`systemctl` 変更系 0 件〕) / #650(ce2cd80 [jetson] env 雛形 `ROS_DISTRO=humble`（ADR-0008）＋Ubuntu 24.04 残骸除去〔land 後 `deploy/jetson/**`・`docs/jetson/01-*`・`docs/setup/jetson-deploy.md` に `jazzy|24.04|noble` grep **0 件**／`deploy/dev/**` の jazzy は dev sim コンテナの正しい記述で残骸ではない〕・雛形に実キー混入なし) / #646(bcf8c53 [docs] **`docs/jetson/03-build-deploy-run-and-run-records.md` 新設**＝Build/Deploy/Run 分離とビルド記録 v0・走行記録 v0 の正本＋`docs/README.md:157`（jetson 表 1 箇所のみ＝二重索引なし）・`docs/GLOSSARY.md:169-170`（§11 末尾）・`docs/jetson/02`/`docs/setup/jetson-deploy.md` から net-zero backlink) / #618(6835412 [jetson] ch341 out-of-tree driver＋brltty purge＋udev `/dev/myserial`〔jetson/02 §10〕＋USB VBUS ブザー〔doc02 P-9d〕・前回 pin 後に別セッションが land＝本ブロックで初計上)。
- **merge 順の根拠（agent teams 3 レーン review の実 Read 由来・textual conflict はゼロなので「壊れる/壊れない」ではなく中間状態の健全性で決めた）**: `#646 → #650 → #647 → #648`（独立 2 本 #631/#610 は最後）。①#647 の `deploy/jetson/bin/build.sh:18`・`record-run.sh:17`・`mwr_build.py:16-17`・`deploy/jetson/README.md:38` が `docs/jetson/03` を "Source of truth" として指すため **#646 が先**（逆順は dead link）。②#648 の `.claude/CLAUDE.md`・`.claude/rules/build-deploy-run.md` は 03 と `bin/*.sh` の両方を指すため **#646/#647 の後**（`scripts/check_consistency.py` の B4 は `path:NN` の行数範囲しか見ず **この dead link を検出しない**＝順序管理が唯一の防波堤）。③#650 は 3 ファイル・net-zero 行数で行 pin をずらさず、#647 が増やす `ROS_DISTRO` 消費者 3 箇所（`build.sh:26`/`record-run.sh:24`/preflight の rosdep）より先に雛形を humble 化すると、これから provisioning するボードの初回 build で `/opt/ros/jazzy` 不在 WARNING と rosdep 解決不能を踏まない。
- **land 後ゲート（merge 済 `origin/main`=`1937cb6` で実走）**: `python3.12 scripts/check_consistency.py` **0 ERROR**（WARN は本 refresh 前の C1-status-sha のみ）／`ruff check` 全通過・`ruff format --check` **418 files already formatted**／`pytest -q` **exit 0**（.venv py3.12）。merge 前にも 6 本を逐次 merge した統合ツリーで同一ゲートを先行実行し、**textual conflict ゼロ**と共有ファイル `deploy/jetson/README.md`（#647∩#650）の意味的健全性を実ファイルで確認済み。
- **blocked / next（本ラウンド review 由来・未起票）**: ①`deploy/jetson/bin/mwr_build.py:125-141` は `systemctl`/`pgrep` 不在ホストで **fail-open**、かつ判定が `stdout == "active"` 完全一致のため `activating` を素通しする ②`mwr_run_record.py:277-290` が `ros2 param dump` を無加工保存＝将来 param に鍵が載ると走行記録に落ちる（redaction 未実装。現状 `ws/src/**` に秘匿 param 宣言は 0 件） ③`docs/jetson/02:349-350` が「16 pkg」「雛形の jazzy 記述を踏まない」のまま＝#646/#650 land 後は時点注記（2026-08-30 実測）が要る ④`docs/adr/0010-raise-speed-cap-to-platform-max.md:102-107` が「電源断保持」を **§Open 1 の残り**と結論行の両方に置いて自己矛盾 ⑤`ws/src/warehouse_m1_driver/CLAUDE.md:118`(調査ソース V3.5.1) と `:119`(実機 FW 3.6) が隣接で不一致 ⑥`ws/src/warehouse_llm_bridge/CLAUDE.md:63` の consume 行に新規 import `tools.EMERGENCY_HISTORY_LLM_MAX` が未登録 ⑦`docs/architecture/12-infrastructure-common.md` 側から doc08 追補への backlink 未張り（片方向） ⑧#610 により **Langfuse trace の input に載る emergency history も 10 件**になる（事後解析の正本は state.json の 50 件）。⑨前ラウンド積み残し #632 B2–B4／#652/#653/#654 は不変。
- **worktree / branch 実査（2026-09-10 round(4) 後）**: `git ls-remote --heads origin` = **`main` のみ＝stale ゼロ**（merged 7 branch を `git branch -D` ＋ `git push origin --delete` で掃除）。`git worktree list` = 本 refresh 用 `mwr-status-4` と一時統合ツリーを除き **4 本**: repo 本体（detached `1fc6259`）／`mwr-procurement`（`main`）／`.claude/worktrees/epic-borg-0c48ae`（`docs/oq3-footprint-decision`・**未 push・7 ファイル未コミット**＝他セッション作業として残置）／`.claude/worktrees/quirky-rhodes-c4fe56`（detached `144cbbe`・clean）。

### 2026-09-10 round 境界 refresh (3)（contract `IDLE_SPEED_EPS` #649＋Humble 3-rule shutdown 横展開 #638/#644/#645/#639＋M1 joy 手順/launch #641/#643・7 PR land）

- main ref = `origin/main`(`1937cb6`) 同期（`91662a3`→`67d7f69`・`refresh_status.py --fix` @ python3.12・12 pin 行書換・履歴 SHA 不触。#640(19e8a3a)=前回 refresh 自体は land 列挙から除外。他セッション PR は本文を実読して要約）。
- **2026-09-10 land (3)（`91662a3`→`67d7f69`・新しい順）**: #645(67d7f69 [llm-bridge] **L4 4 ノード（`llm_bridge`/`character_llm`/`x_er_bridge`/`operator_feedback`）の Humble 3-rule shutdown**〔context 破棄後の rclpy 触りを best-effort 化・終了時に失われる/失われないものを CLAUDE.md にノード別明記・`x_er_bridge` の起動拒否 fail-closed 不変・別セッション〕) / #643(d7a14b4 [teleop] **`m1_teleop.launch.py`**〔`joy_node` の安全前提 param を機械固定＝`device_id`(引数)/`autorepeat_rate` 20.0/`sticky_buttons` False/`deadzone` 0.0（中立判定は `teleop_joy` 0.1 に一本化）・**`m1_driver` は起動しない**（W-4）・Nav2/twist_mux なし・AST wiring pin・`joymap.py:42` LED コメント訂正・別セッション〕) / #644(6c13d9f [nav-traffic] **`virtual_scan`（L2 traffic）/`traffic_manager`/`speed_band_node` の 3-rule shutdown**〔挙動は終了コード以外不変・安全は終了時メッセージに依存しない・ratchet baseline −3・別セッション〕) / #639(3a21287 [skeleton] **shutdown ratchet の分類器を締める**〔穴 3: 形だけの try/suppress をガード扱い→`Exception`/`RuntimeError`/`RCLError` 系のみ／自前 SIGTERM handler だけで safe→タイマー保有を必要条件に／ラッパ entry point が scan 外→`setup.py` `console_scripts` 20 本を読む〕＋`.gitignore` に `docs/assets/`＋teleop CLAUDE.md の存在しない `track:teleop` を修正・別セッション) / #641(eb62a01 [docs] **mode-m1/03 §5**〔正本 `joy_node` コマンド・jstest/`/joy` 判定規約・`:3`/`:52` 同一行置換（下流 pin 10 本不変）〕＋mode-m1/01 に M1 systemd N/A 注記・電源投入順序は #618/#631 に委ねる・別セッション) / #638(1983412 [safety-state] **`emergency_guardian`（L1）＋`state_cache`（帰属未定 F3・暫定 Safety）の 3-rule shutdown**〔`finally` は destroy+shutdown のみ・R-40 gc freeze 維持・停止権限は終了時メッセージに依存しない（twist_mux prio-100）・別セッション〕) / #649(c23cd16 [contract] **`warehouse_interfaces.safety.IDLE_SPEED_EPS = 0.01` を凍結**〔#642＝#632 B1・additive・値/挙動 byte-identical（reviewer が旧新 `derive_status` を 30 万件 fuzz＋ULP 境界で差分 0）・`warehouse_state.aggregator.derive_status`（net-zero 行・pin `:165-182` 不動）と `warehouse_orchestrator.motion`（re-export）が import・docs 先行＝doc12 EOF 追補 `:641-662`＋doc21 §17 ① `:451`/`:469` in-place・`safety.py:8-12` docstring を同 5 行で改稿（ε は cap でない）・定数は末尾（`:18` 以下 ~20 inbound pin 保護）・identity `is`＋fold-literal AST scan（`10/1000`・`float("0.01")` も検出）＋actuation pkg ratchet（m1_driver/safety/mcp_server が ε を bind しない）・3 軸 review で BLOCKING 1（`warehouse_state/CLAUDE.md` 中段挿入が `bringup.launch.py:57`→`:23` pin を破壊→in-place 折込）修正・予告 #430/#5/#632・3012 passed〕)。**閉じたもの**: #642（`Closes`）・#632 **B1 ✅**・wo `CLAUDE.md` voids 15 の ε 所有残件。**shutdown 横展開**（#633 起点）は L4/L2 traffic/L1/State Cache/teleop まで到達＝ratchet baseline で機械管理。
- **blocked / next**: ①#632 **B2–B4**（非一様 odom・欠落区間跨ぎ／`speed_cap` 出所開示／clock-reset で run 累算 silent freeze＝`rejected` counter）＝**doc21 §17 ②③ の docs 裁定が先** ②#649 review 由来の follow-up（未起票）: `scripts/check_consistency.py` が `IDLE_SPEED_EPS`/`MAX_LINEAR_VELOCITY` の docs 側数値コピーを機械 pin しない／既存 0.3 scan の remediation 文言（「module 定数にせよ」は無効）／`docs/shared/02:371` の `safety.py:26-34` off-by-one ③他セッション open PR 7 本: #650（`fix/jetson-env-example-humble`）/#648（`docs/rules-build-deploy-run`）/#647（`hw/jetson-build-run-scripts`）/#646（`docs/jetson-build-run-records`）/#631（ADR-0010 car_type）/#618（jetson ch341）/#610（emergency ring cap・裁定待ち）＝いずれも触らない。
- **worktree / branch 実査（2026-09-10 round(3) 後）**: `git ls-remote --heads origin` = **main ＋ open PR 7 本の branch（#610/#618/#631/#646/#647/#648/#650）＝stale ゼロ**（#649=`contract/idle-speed-eps` は merge 後に手動 `git branch -D`＋`git push origin --delete`）。`git worktree list` = 11 本: root（`1fc6259` detached）・`mwr-procurement`(`main`=`ca088f3`・未同期)・本 round の `quirky-rhodes-c4fe56`（本 refresh ブランチ `docs/status-20260910c`）・他セッション 8 本（`admiring-albattani`=#610・`epic-borg`=`docs/oq3-footprint-decision`(remote 無し・local のみ)・`nostalgic-driscoll`=#650・`mwr-adr0010-cartype`=#631・`mwr-jetson-build-records`=#646・`mwr-jetson-build-scripts`=#647・`mwr-jetson-ch341`=#618・`mwr-rules-build-deploy-run`=#648）。round(2) 時に居た 8 本（`ecstatic-babbage`/`kind-pike`/`pensive-sanderson`/`mwr-budget-g`/`mwr-m1-docs`/`mwr-probe-docfix`/`mwr-ratchet-fix`/`mwr-shutdown-sweep-a`）は所有者が掃除済。触らない。

### 2026-09-10 round 境界 refresh (2)（run 全体版 idle 率/速度予算消化率 #637＋teleop Humble-safe shutdown #633・2 PR land）

- main ref = `origin/main`(`1937cb6`) 同期（`c79d34c`→`91662a3`・`refresh_status.py --fix` @ python3.12・11 pin 行書換・履歴 SHA 不触。#630(94c5aaf)=前回 refresh 自体は land 列挙から除外）。
- **2026-09-10 land (2)（`c79d34c`→`91662a3`・新しい順）**: #637(91662a3 [wo] **run 全体版 `KpiReport.run_motion`**〔doc21 §17 ③ :466 が予告した窓版（#629）の twin＝同じ式・同じ ε・同じ cap で scope=起動〜現在・`eval_sdk.stats.TimeSeriesAccumulator`/`SeriesTotals`（domain 語彙なし・threshold 注入・O(1)）→ `MotionAccumulator.run_totals()`（既存 `add()` の単一検証経路内・ring-buffer 退避に無関係・新 topic/producer なし）→ `run_motion_stats`/`RunMotionStats{samples,idle_samples,idle_ratio,t_first,t_last,duration,integral_abs_speed,speed_budget_utilisation,speed_cap}`・pre-existing の `resolve_motion_buffer_samples` `OverflowError` 穴も塞ぐ・**doc21 §17 ② :460 を PR 内で改定**（run 数値 = twist 台形積分・当初の pᵢ 指定を撤回＝pᵢ は pose 差分で `add()` が落とすサンプルも積むため一致しない・可逆な docs 判断）・共有 guard `_budget_ratio` の非有限 span reject は窓版にも及ぶ（無意味な 0.0→None のみ）・agent teams 3 軸 review（BLOCKING 0・SHOULD-FIX 6 反映・**reconstruction audit**＝実装 agent が mutation harness の `git checkout --` で未コミット `motion.py` を消失→再構築、reviewer が関数単位 SHA-256 比較で無傷確認）・3005 passed(py3.12.10)・mutation 15 種 RED・**横断 Eval/Observability・L4–L0 不変更**・#632 A1–A5 完了・ユーザーが GitHub UI で merge〕) / #633(7841a9c [teleop] **node shutdown Humble-safe**〔ボード（Jetson・ROS 2 Humble・rclpy 3.3.21）で #622 `teleop_joy` の Ctrl-C/SIGTERM が Traceback→exit 1 になる回帰を修正（Humble の rclpy シグナルハンドラが `main()` の `finally` に先行）・package 共通 `warehouse_teleop/node_runtime.py`（`run_node`/`best_effort`/`NORMAL_STOP_EXCEPTIONS`/`context_is_live`・3 規則）＋ repo-wide ratchet `tests/unit/test_node_shutdown_lifecycle.py`（`KNOWN_UNSAFE_STOP_ON_HUMBLE`）・安全上の実害なし＝最終ゼロは courtesy で安全は m1_driver **W-1**（0.5 s ブレーキ）が担保・実害は Traceback ノイズと systemd `Restart=on-failure` の誤記録・`warehouse_teleop/**` のみ・L0'/L1/L2 不変更・track #621・別セッション所有〕)。**閉じたもの**: #632 A（run 全体版＋OverflowError）・doc21 §17 閉じ note (3)。**未決で残るもの**: #632 B1–B4（ε 凍結先→別 `[contract]` issue／非一様 odom・欠落区間跨ぎ積分／`speed_cap` 出所開示／**B4 新規: clock/sim reset で run 累算が silent freeze**＝`rejected` counter 要否）。
- **blocked / next**: ①#610（emergency ring LLM 二重送出 cap・裁定待ち・不変） ②#618（`hw/jetson-ch341`・別セッション open・不変） ③**#631**（`docs/adr0010-car-type-pin`＝ADR-0010 Open 1 の car_type 0x0A 書込み記録・別セッション open） ④**#638**（`fix/humble-shutdown-safety-state`＝emergency_guardian / state_cache の Humble-safe stop＝#633 の横展開・別セッション open） ⑤**#639**（`chore/shutdown-ratchet-classifier`＝shutdown ratchet の締め（guard/timer 分類）・別セッション open） ⑥#632 B1–B4 の裁定（上記）。
- **worktree / branch 実査（2026-09-10 round(2) 後）**: `git ls-remote --heads origin` = **main ＋ open PR 5 本（#610 / #618 / #631 / #638 / #639＝いずれも別セッション）＝本 round 由来 stale ゼロ**（#637=`feat/wo-tier1-run-motion` は GitHub UI merge で remote 自動削除・local は手動 `git branch -D`）。`git worktree list` = root（`1fc6259` detached）＋ `mwr-procurement`(`main`=`ca088f3`・未同期) ＋ 本 round の `quirky-rhodes-c4fe56`（本 refresh ブランチ `docs/status-20260910b`）＋ 他セッション worktree 16 本（`admiring-albattani`=#610・`ecstatic-babbage`=`docs/status-20260909b`(#627 済・local 残置)・`epic-borg`=`docs/oq3-footprint-decision`・`kind-pike`・`nostalgic-driscoll`(detached)・`pensive-sanderson`・`mwr-adr0010-cartype`=#631・`mwr-budget-g`・`mwr-jetson-build-records`=`docs/jetson-build-run-records`(`91662a3`)・`mwr-jetson-build-scripts`=`hw/jetson-build-run-scripts`(`91662a3`)・`mwr-jetson-ch341`=#618・`mwr-m1-docs`=`docs/m1-bringup-gaps`・`mwr-probe-docfix`・`mwr-ratchet-fix`=#639・`mwr-rules-build-deploy-run`=`docs/rules-build-deploy-run`(`91662a3`)・`mwr-shutdown-sweep-a`=#638）には触らない。

### 2026-09-10 round 境界 refresh（doc21 §17 idle 率/速度予算消化率の定義確定＋wo 実装・2 PR land）

- main ref = `origin/main`(`1937cb6`) 同期（`1f9e528`→`c79d34c`・`refresh_status.py --fix` を host `python3`(3.7) で実行＝**書換 10 pin 行は完了**・報告行 `zip(strict=)` の TypeError のみ〔既知・下記 refresh (2) 行に記録済・py3.12 で実行すべき〕・`python3.12 … --check` で fresh・履歴 SHA 不触。#627(f5c793f)=前回 refresh 自体は land 列挙から除外）。
- **2026-09-10 land（`1f9e528`→`c79d34c`・新しい順）**: #629(c79d34c [wo] **`SmoothnessStats.{idle_ratio,speed_budget_utilisation,speed_cap}` additive 実装**〔doc21 §17 ①②③ 準拠・数学は `eval_sdk.stats.{fraction_at_or_below,trapezoid_integral}`（domain 語彙/しきい値なし＝doc21:178）・cap は config `safety.max_linear_velocity` 注入＋import した `MAX_LINEAR_VELOCITY` fallback（0.3 再タイプ禁止を AST scan test で恒久化）・ε=0.01 は `warehouse_state.aggregator._MOVING_EPS` の documented value を mirror（cross-lane import なし・cross-check test で pin）・agent teams 3 軸 review（spec/test/safety）で **BLOCKING 2（`T=end−start` 未 pin＝epoch stamp で 3e-10 に化ける／|v| vs signed 未 pin）＋fail-open 穴 1（`5e-324` cap×短窓で `ZeroDivisionError`→node 停止・config validator が受理する値）**を land 前に是正・mutation 10 種 RED・2961 passed・**横断 Eval/Observability・L4–L0 不変更**・新 score/topic/契約なし〕) / #625(5f557db [docs] **doc21 §17 新設**〔§13.2 :310 で名前しか無かった `idle 率`/`速度予算消化率` の分子・分母・窓・ε を確定（idle = `|v|≤ε=0.01` サンプル割合／消化率 = (iii) `∫|v|dt÷(v_max·T)`・(ii) time-at-cap は別指標として不採用）＋Tier-1 観測窓（distance/detour=run 全体・smoothness/idle/消化率=ring-buffer 窓）と非一様サンプリングを明文化・EOF append＋§16 ⑤ in-place（inbound pin 上限 :409 以下は不動）・review 追補 `8d85ee1`（v_max は config tunable ≤ hard cap／(i) mean/cap は (iii) の近似＝endpoint-balanced 窓でのみ厳密／同一窓に jitter 扱い 3 通り）・ratify 3 問（ε 所有／(iii) 採用／窓スコープ）はオペレーター OK〕)。**閉じたもの**: wo `CLAUDE.md` voids 15/16 の doc-side TODO（#613 が定義不在で意図的に未実装にしていた 2 指標）。**元ブリーフの「§6 line-ref 再 pin／:189 reword」は #616 で既 land**（origin/main 実読で判明・二重作業せず）。
- **blocked / next**: ①#610（emergency ring LLM 二重送出 cap・裁定待ち・不変） ②#618（`hw/jetson-ch341`＝別セッション open・不変） ③**doc21 §17 follow-up（未起票）**＝ε の凍結先（`warehouse_state` 私有→`warehouse_interfaces.safety` へ昇格するか）／非一様 odom の厳密扱い（同一窓で spectral=平均レート・②=実スタンプ積分・①=生カウントの 3 通り同居・odom 欠落区間は ② が跨いで積分し ① には見えない）／run 全体版（`DistanceAccumulator` 同型累算器）／`load_config` 失敗時 fallback=0.3 が overlay 低減 cap 下で過小報告（`speed_cap` 開示で判別可・ceiling 重複チェックは意図的に置かず＝`load_config` 単一所有）／pre-existing `resolve_motion_buffer_samples` の `float(10**400)` OverflowError 穴 ④`scripts/refresh_status.py --fix` の `zip(strict=)`＝host py3.7 非対応（既知・py3.12 で実行）。
- **worktree / branch 実査（2026-09-10 round 後）**: `git ls-remote --heads origin` = **main ＋ open PR 2 本（#610=`feat/llm-bridge-ring-llm-cap` / #618=`hw/jetson-ch341-and-usb-phantom`＝いずれも別セッション）＝本 round 由来 stale ゼロ**（#625=`docs/doc21-tier1-metric-definitions` は `gh pr merge --delete-branch` で remote/local 削除・#629=`feat/wo-tier1-idle-budget` は detached HEAD 状態で merge したため gh の自動削除が不発→手動 `git branch -D`＋`git push origin --delete`）。`git worktree list` = root（`1fc6259` detached）＋ `mwr-procurement`(`main`=`ca088f3`・未同期) ＋ 本 round の `quirky-rhodes-c4fe56`（本 refresh ブランチ `docs/status-20260910`）＋ 他セッション worktree（`admiring-albattani`=#610・`ecstatic-babbage`=`docs/status-20260909b`(#627 済・remote 削除済・local 要掃除)・`epic-borg`=`docs/oq3-footprint-decision`・`kind-pike`・`nostalgic-driscoll`(detached)・`pensive-sanderson`=`claude/zen-margulis-f364c7`・`mwr-adr0010-cartype`=`docs/adr0010-car-type-pin`(`c79d34c`)・`mwr-budget-g`・`mwr-jetson-ch341`=#618・`mwr-probe-docfix`）には触らない。

### 2026-09-09 round 境界 refresh (3)（stop overlay producer＋順序5 teleop e-stop＋gamepad 用語・3 PR land）

- main ref = `origin/main`(`1937cb6`) 同期（`8e35ddd`→`1f9e528`・`refresh_status.py --fix` @ .venv py3.12・9 pin 行書換・履歴 SHA 不触）。
- **2026-09-09 land (3)（`8e35ddd`→`1f9e528`・新しい順）**: #626(1f9e528 [safety-state] **Emergency Guardian `/bot{n}/stop_state` producer**〔doc05 §4-1 の consumer 契約(#617)に対する producer 側・**L1 additive**＝既存 estop/level/event/operator latch/Nav2 cancel は byte 不変で publish を足すだけ・50ms tick 末尾で同一 `decisions`/同一 `time.monotonic()` から `gl.stop_requested_for` 導出・QoS RELIABLE/KEEP_LAST1/VOLATILE・`valid_until=now+0.5`〔consumer/twist_mux timeout 借用・TODO Phase1 実測〕・配線 AST 厳密 pin＋pure 極性 unit・mutation 4/4 RED・pre-PR sub-review 2 本消化〕・§4-1 契約本文 byte-identical・doc03 行追加ゼロ) / #623(285ce4d [docs] **GLOSSARY「ゲームパッド」を M1 ワイヤレスハンドルの正準語に**〔handle/joystick/joy を集約・既存ファイル名 03-joystick は改名せず〕・別セッション所有) / #622(52f8967 [teleop] **順序5 の teleop e-stop latch＋再アーム**〔gamepad B/R1 engage・SELECT+START clear〕＋`/operator/stop_request` producer＝#582 の mask≠latch を latch 化・別セッション所有)。**閉じたもの**: #617 の producer 対（#626＝`stop_overlay_enabled` が feed を得て実用可能に・ただし有効化は M1 統合構成のオペレーター判断）・doc05 §8 順序5 の teleop 側（#622）・§4-1 張り残し用語（#626 が GLOSSARY §11 に stop_state feed/期限 watermark/許可窓上限を追補）。
- **blocked / next**: ①#610（emergency ring LLM 二重送出 cap・**裁定待ち＝merge=批准をオペレーターに残置**・不変） ②#625（`docs/doc21-tier1-metric-definitions`＝#619 が残した idle率/速度予算消化率の定義・別セッション open） ③#618（`hw/jetson-ch341`＝別セッション open） ④#626 の残＝`stop_state` の **publish 周期と window 0.5s は Phase 1 実測**・cross-boot 復帰は driver 再起動（同一 boot は monotonic 継続で watermark 逆行なし）・sim/`use_sim_time` は monotonic-only producer ゆえ scope 外（§4-1 が fail-closed と規定） ⑤OQ-OP1 の本題（`/bot{n}/stop_state` を L2 feed にも流用するか＝level mirror 恒久受容か）は §4-1 で未裁定のまま＝fault injection 後のオペレーター裁定 ⑥#617 の `stop_state_max_validity_s` ハード上限の要否（未決）。
- **worktree / branch 実査（round(3) 後）**: `git ls-remote --heads origin` = **main ＋ open PR 3 本（#610 / #618 / #625＝いずれも別セッション）＝本セッション由来 stale ゼロ**（#626=`feat/guardian-stop-state` は gh 自動削除不発→手動 `git push origin --delete`）。agent isolation worktree（`agent-ac6ca9…`）は merged 後 `git worktree remove -f -f` 済。他セッション worktree（`mwr-teleop-estop`=#622 済・`epic-borg`・`kind-pike` ほか）には触らない。

### 2026-09-09 round 境界 refresh (2)（agent-teams 観測面 3 レーン＋m1 stop producer＋teleop/#597 締め・8 PR land）

- main ref = `origin/main`(`1937cb6`) 同期。SHA pin は `scripts/refresh_status.py --fix` を `.venv`(py3.12) で実行（`ad19591`→`8e35ddd`・7 pin 行書換・履歴 commit SHA 不触。host `python3`=3.7 は `zip(strict=)` で不可＝venv 必須）。
- **2026-09-09 land (2)（`ad19591`→`8e35ddd`・新しい順・orchestrator=ecstatic-babbage が agent-teams を統括）**: #619(8e35ddd [wo] **doc21:306 low-pass 実装**〔SPARC/LDLJ 入力を `eval_sdk.stats.low_pass`〔additive・`jerk()` と同じ centered MA〕で pre-filter・N_MU は raw signed 維持・LDLJ 値が sampling noise 分低下・fs/floor mutant を numpy 有無両環境で赤化〔`SmoothnessStats.filtered_samples` 追加で床を観測可能に〕・**提出前に sub-review＋consistency-audit 消化**・#613 post-merge review の是正〕) / #617(c3e24ca [m1] **stop overlay producer 契約**〔doc05 §4-1 新設=`/bot{n}/stop_state`・`std_msgs/String`JSON・RELIABLE/KEEP_LAST1/VOLATILE・**watermark 単調・逆行/非有限/非正→即 revoke**〕＋既定無効時 subscription 不生成＝topic graph 不変＋doc03:114 additive 行＋inbound pin +1 sweep・**decode 硬化**〔OverflowError/RecursionError→revoke・dup-key last-wins 封じ〕・mutation 16/16・`contract` ラベル〕) / #615(7aa44bc [docs] doc08:533/:331・spike に Option-D trace writer=`PluginTraceEnricher` を truth-sync〔#612 residual ⑦〕) / #614(fd50911 [web] **TraceLink 活性化**〔`NEXT_PUBLIC_*` build 焼込を `GET /config` runtime 取得へ・URL validator が非 http(s)/非 ASCII host〔U+3002 偽装〕/credential/制御文字を fail-close・observe-only R-26 mutation 赤化〕・PR#500 residual⑥消化) / #613(ba90603 [wo] **Tier-1 KPI**〔介入率/throughput/Jain/detour/SPARC・LDLJ・N_MU＝audit は #501 済ゆえ odom 半分・新 producer/topic/score ゼロ・既存 subscriber 内 bounded ring〕→ post-merge review で 🔴3 検出→#616→#619 で是正) / #612(e5f2484 [llm-bridge] **Option-D 事後 enrichment**〔plugin-owned trace へ prompt 名/版・env タグ再付与・`TraceIdentity` 単一語彙・never-raise・live/provider 呼び出しゼロ〕) / #582(7e40b03 [teleop] **/joy 鮮度 dead-man**〔`joy_timeout_s`・stale→zero republish・joy_node は device 喪失で無 publish=保持指令が W-1 を欺く穴を塞ぐ第一防御〕・🔴 2 点=jetson/02 `mode-m1/03:54`→`:55` 再 pin＋mask≠latch 残件開示は orchestrator 引取) / #597(f7db720 [docs] **doc03 `/bot{n}/cmd_vel/emergency` 行**〔前ラウンド stale レーンを現 main 上で rework・QoS/consumer は doc12:622 一元委譲・inbound +1 sweep 24 箇所＋audit 6 是正〕)。**閉じたもの**: #436(#612)・#433(#614)・#432(#613→残件#616→#619)・#597 audit W2 の対＝emergency 行本体・#582 順序 4(joy dead-man)。**agent-teams 運用の教訓**: ①subagent 環境に `SendMessage` 不在＝途中報告不可・最終レポート一本に集約（brief を修正） ②sub-review は **PR 提出前**に消化（#613 は待たず提出→post-merge に 🔴3＝doc21:306 low-pass 未実装／fs mutant 生存／CI numpy 不在で床テスト空転。#619 で全是正＝#616） ③orchestrator の merge 前検証は契約・境界に寄り doc 規範句の深掘りを落としうる（#613 で露呈）→ contract/安全隣接 PR は doc 正本の規範句を明示突合。
- **blocked / next**: ①#610（emergency ring LLM 二重送出 cap・**裁定待ち＝merge=批准をオペレーターに残置**） ②#616 は #619 で解消済（doc21 §16 に SPARC 低域通過の再検討余地①を残す＝可逆・要オペレーター判断） ③#617 の **OQ-OP1 縮小**〔L0' 入力チャネルは §4-1 確定・残=「L2 生存証明と 1 本統合するか＋publish 周期」〕はオペレーター確認待ち ④#617 の producer=**Emergency Guardian（L1・`warehouse_safety`）publisher 実装が次スライス**（現状 publisher 不在＝有効化しても fail-closed で停止側保持）＋`stop_state` の GLOSSARY 追補 ⑤#614 は config 未設定で **dark ship**〔`web_bridge.langfuse_base_url` overlay＋`run-mode-a-live.sh` の `WAREHOUSE_RUN_ID` export=オペレーター/config follow-up〕 ⑥productization/05:402 の `doc21:189` pin が Tier-1 行を指す既存ドリフト（#619 で :189 文言変更によりより顕在化・別レーン）。
- **worktree / branch 実査（2026-09-09 round(2) 後）**: `git ls-remote --heads origin` = **main ＋ open PR 2 本（#610=`feat/llm-bridge-ring-llm-cap` / #618=`hw/jetson-ch341-and-usb-phantom`〔他セッション〕）＋ 本 refresh レーン `docs/status-20260909`＝stale ゼロ**（本ラウンド merged 6 branch〔#612-#615/#617/#619〕は gh `--delete-branch` が効かず手動 `git push origin --delete` で削除＝**gh 自動削除の不発は MERGED 確認後に手動削除する**）。agent-teams の isolation worktree（`agent-a02b…`ほか 5 本）は merged 後に本セッションが `git worktree remove` で掃除。他セッション worktree（`epic-borg`=#618 系ほか）には触らない。

### 2026-09-09 round 境界 refresh（emergency→L2 配線一式＋doc05 停止権限正本＋順序 5 安全実装）

- main ref = `origin/main`(`1937cb6`) 同期。SHA pin は `scripts/refresh_status.py --fix` を `.venv`(py3.12) で実行（7 pin 行書換・履歴 commit SHA 不触）。
- **2026-09-09 land（`c979071`→`ad19591`・新しい順）**: #607(ad19591 [llm-bridge] **#603+#604 の re-land**〔CLEAR ログ WARNING 統一＋sorted 変異 pin＋cross-thread 原子性裁定 docstring／tools.py・policy_gate.py の stale 行 pin 18 箇所機械修正＋re-land 固有の +15 補正 4 箇所〕) / #605(720ac17 [docs] doc03 除外規約＝`cmd_vel/nav2`・`nav2_raw` は自作ノード両端不在の plumbing として catalog 対象外〔#597 audit W2 解消・全編集 in-place 行ズレゼロ〕) / #600(b97ddfc [llm-bridge] `get_fleet_status` に `emergency{active,history}`＋`l2_emergency_holds` を**別キー**で露出〔doc12:631 独立系統裁定〕＋mirror hold/clear 遷移エッジ 1 行 log＋malformed ring の isinstance ガード〔dispatch 例外非漏洩不変条件〕) / #602(f39e6c1 [safety-state] **操作者非常停止 latch**〔doc05 §5・OQ-OP2 裁定=単一 global `/operator/stop_request` String JSON engage/clear・不正 payload は無視＝clear 扱いにしない・既存 3 理由の自動解除は非回帰 pin・doc03 追記＋下流 15 pin sweep・mutation 4/4 KILLED〕) / #601(d031cc1 [m1] **停止上乗せ core**〔doc05 §4・L0'・既定無効=現行 bit 等価を negative oracle で pin・fail-closed（停止要求/未受信/期限切れ/無効期限→brake）・W-1 と AND 合成・producer 配線は OQ-OP1/OP2 スライスへ意図的 defer・mutation 4/4 KILLED〕) / #598(14971b5 [docs] **ADR-0013 起草**＝STM32 command-stream watchdog（W-3）を vendor V3.6.5 への additive 追加で実装する裁定案〔Status=proposed・入場ゲート 4 つ=U-5/license/stock-hex rollback 実証/G-g〕) / #596(4577717 [llm-bridge] Guardian estop mirror を x_er_bridge（L2）と L3 `emergency_active` へ波及配線) / #594(46a9314 [docs] **mode-m1/05 新設＝運転状態と停止権限の担当分離**〔6 状態機械不採用・停止理由の集合・L2/teleop/Guardian/driver の担当固定・#593 との整合改訂込み〕) / #599(0b2dafd [llm-bridge] mirror 配線層の AST pin unit＋`_BOTS`↔config 突合) / #589(fcad232 [docs] budget §G 保護箱部材) / #588(8399484 [docs] doc02 P-9 電源運用＝バッテリー側 T プラグが唯一の切断点) / #581(6ae05b9 [docs] HTML の ros2_control 誤「棄却」表記修正) / #580(581d2e6 [m1-driver] `m1_probe` の stale car_type/FW 版注記修正) / #593(a167857 [llm-bridge] **Guardian estop→L2 Policy Gate 配線（level mirror）**＝`set_emergency` 呼び出し元ゼロだった構造穴の閉鎖・正本=doc12 追補②) / #579(381d5f3 [docs] 組立/hardware docs の landed 裁定同期)。**閉じたもの**: `/emergency`→L2 未配線穴（#592 系: #593→#596→#599→#600 で配線・波及・テスト pin・観測面まで完結）・doc05 §8 順序 1/2/5（順序 5 は「解除しただけでは動かない」の実機確認が残ゲート）・#597 audit W2（#605）。**⚠️手順事故と是正（隠さない）**: #603/#604 は base=`feat/l2-emergency-observability`（stacked）のまま merge 操作したため squash が base branch 側に積まれ **main 未着のまま branch 削除で消失** → `refs/pull/N/head` から cherry-pick 回収し #607 で re-land 済（教訓: merge 前に `baseRefName==main` 確認。memory 化済）。**blocked / next**: ①#597（CONFLICTING=#602 の doc03 追記と衝突・🔴 range pin 2 件＋#605 後の doc03:114→:115 再スイープ・所有セッションの rebase 待ち） ②#582（🔴 §6 再アーム未実装の開示/実装待ち・land 後に jetson/02 の `mode-m1/03:54`→`:55` 再 pin） ③ADR-0013 の採否=オペレーター裁定（accept 時は mode-m1/02:64・shared/02:798 の docs sync） ④順序 3/4（joy latch/mux 3 入力）と運転モード発信元（OQ-OP6）は #582 land 後 ⑤停止上乗せの producer 配線スライス（OQ-OP1/OP2・doc03 additive 同一 PR・期限規律=watermark 逆行拒否を契約に明記） ⑥Guardian 再起動で操作者 latch 消失の裁定 ⑦実機 M0→M2（全実測の前提・不変）。
- **worktree / branch 実査（2026-09-09・9 PR land＋#607 re-land＋掃除後）**: `git ls-remote --heads origin` = **3 heads = `main` ＋ open PR 2 本（#597=`docs/doc03-cmd-vel-emergency-row` / #582=`feat/teleop`）＝stale ゼロ**（本ラウンド merged 10 branch は remote 削除済）。`git worktree list` = main checkout（detached）＋ 本 refresh レーン（`mwr-status-2026-09-09`）＋ `mwr-procurement`（branch `main`@`ca088f3`＝placeholder・要整理で不変）＋ 他セッション所有 9 本（`quirky-rhodes`=#597 レーン / `mwr-budget-g`(#589 済)・`mwr-probe-docfix`(#580 済)・`ecstatic-babbage`(#605 済)・`admiring-albattani`(#604 済)＝**merged 済で各所有セッションの掃除待ち** / `epic-borg`=`docs/oq3-footprint-decision` 据え置き / `kind-pike`・`nostalgic-driscoll`・`pensive-sanderson`）＝他セッション worktree には触らない（dual-session 衝突回避）。

### 2026-08-31 round 境界 refresh（OPEN PR 全棚卸し・#499〜#503 land＋#493/#575 close）

- main ref = `origin/main`(`1937cb6`) 同期。SHA pin は `scripts/refresh_status.py --fix` を `.venv`(py3.12) で実行（6 pin 行書換・履歴 commit SHA 不触）。
- **2026-08-31 land（2026-08-01 ラウンド持ち越し OPEN PR の全解消＋ADR-0012 slice2・`c9b66f6`→`c979071`・新しい順）**: #503(c979071 [llm-bridge] nav2_bridge 起動 preflight＝素の ImportError でなく provisioning ヒント付き fail-fast) / #502(9998016 [docs] doc21 §11 追補＝hard trace-teardown timeout の不採用裁定＋§4 抽出引用の pin) / #501(d6bf2a4 [wo] audit 由来 Tier-1 KPI〔intervention/fairness/throughput・doc21 §13.2〕＝`eval_sdk.stats` 消費) / #500(d314c1e [web] web_bridge に gen_id ObsEvents の trace_deriver 注入＋依存 preflight〔doc22 §7〕) / #499(ea2a95f [mode-x-er] `/operator/notice` subscriber runtime node 配線〔doc05 §8.10 item4〕) / #576(1fc6259 [m1] **speed band wiring slice2**＝launch handoff〔①同一 substitution AST pin〕・`reset_period` pin・`speed_bands.*` config keys safe-OFF〔ADR-0012 実装二段目・前回 refresh 後に land〕) / #574(6605462 前回 STATUS refresh＝本ラウンド境界)。**#499〜#503 は 29 日 stale の CI evidence を `gh pr update-branch` で現 main 相手に再実行してから squash merge**（各 17 pass・py3.10 床 AST guard / B4 含む機械ゲート再通過・5 本の編集ファイルはペアワイズ disjoint＝順次 merge で再 update 不要）。**closed-unmerged**: #493（sim verification gate docs）＝①ADR-0006 番号衝突（main に `0006-single-bot-first` 既存）②ADR-0009 部屋スケール転換で 2-bot ジオラマ sim-gate 前提が失効 ③shared/05 未改訂・G5 衝突未消化＋CONFLICTING → 理由コメント付き close・branch/worktree 削除（再スコープは ADR-0009 後提の新 docs PR で）。#575（競合 STATUS refresh 下書き `docs/status-10`）は所有セッションが 2026-08-30 close 済（前 block「破棄推奨」の完了）。**これで前 block next ③「OPEN PR 6 本の merge 判断」は全決着・OPEN PR = 0**。**blocked / next**: ①ADR-0012 残件＝実機実測のみ（帯実値/過渡回帰/runtime 単一 publisher アサート/gesture_detector） ②実機 M0→M1→M2＋`m1_probe`＋G-g 試験→car_type 確定で ADR-0010 contract PR 解錠（不変） ③旧 car_type 二択 stale 2 箇所の別 PR 修正（task chip 化済・不変） ④`mwr-procurement` worktree（branch `main`@`ca088f3` checkout 残）の整理＝ユーザー判断待ち。
- **worktree / branch 実査（2026-08-31・5 PR land＋#493 close＋掃除後）**: `git ls-remote --heads origin` = **1 head = `main` のみ＝stale ゼロ・open PR 0**（merged 5 branch＋`docs/sim-verification-gate`(#493) は remote/local とも削除・対応 lane worktree 6 本〔`mwr-{xer-opnotice,web-trace,wo-kpi,doc21,nav2-preflight,sim-docs}`〕も porcelain クリーン確認のうえ remove 済）。`git worktree list` = main checkout（detached `1fc6259`）＋ `mwr-procurement`（branch `main`@`ca088f3`＝placeholder・要整理で不変）＋ agent worktree 2 本（`pensive-sanderson`＝本ラウンド orchestrator レーン / `epic-borg`=`docs/oq3-footprint-decision`〔据え置き継続〕）＋ 本 refresh レーン（`mwr-status-2026-08-31`=`docs/status-2026-08-31`）。

### 2026-08-30 round 境界 refresh（第2次・#561〜#573+#567）

- main ref = `origin/main`(`1937cb6`) 同期。STATUS の SHA pin（5 箇所）は `scripts/refresh_status.py --check` で fresh（**host `python3`=3.7 では `--fix` が報告行の `zip(strict=)` で TypeError になるが書換自体は完了する**＝本 refresh で実測。py3.12 推奨は不変）。
- **2026-08-30 land（速度帯「設計解→裁定→実装 slice1」＋ jetson 常時運用化＋ py3.10 床実効化＋ G-l docs 閉鎖・`ca088f3`→`c9b66f6`・新しい順）**: #567(c9b66f6 [docs] **G-l/L0' 結線状態の docs 同期＝#550 land 後の stale「未結線」記述を net-zero in-line で閉じる**〔mode-m1/02 Status・doc10 §10-1/§10-2②/§11 G-l 行/§12-1 S-8/§12-1b・doc09 追補③⑤〕＋ **ADR-0009 追補④／ADR-0010 追補を EOF append**〔「結線済み≠実機確認済み」を区別・実機 M0-M2/W-1 brake 実測/G-g は未実施のまま記録・`# TODO(安全レビュー)` は閉じない〕＋ ADR-0010↔0012 双方向化・空行 pin `ADR-0010:21`→`:22` 2 箇所・`warehouse_m1_driver/CLAUDE.md` シリアル層記述の越境訂正〔PR 本文で開示〕。/consistency-audit 完走＝docs↔code ドリフト 0) / #573(d2b0b72 [docs] jetson bring-up の**タグ逸脱宣言＋install-step defer**〔v0.1.0 before enable〕) / #571(876189e [docs] **CI Ruff+pytest を py3.10/3.12 matrix 化**〔`requires-python>=3.10` 床の毎 PR 実走検証〕) / #569(9a6ce40 [m1] **speed band publisher slice1＝`warehouse_perception` 新設**＋R-26 units〔OQ-13 裁定の実体化・ADR-0012 実装一段目〕) / #568(579fb63 [docs] **ADR-0012: 速度帯は L2 非経由の best-effort 制御面**〔OQ-R1〜R7 一括裁定・最小安全方針の docs 初出・hard 床=①起動基準値/凍結契約/L0' 不変〕) / #565(10fcef7 [contract] **py3.10 互換 shim**〔StrEnum/UTC・asyncio timeout・AST floor guard〕) / #564(f36721a [jetson] off-LAN Tailscale route＋board provisioning〔doc02 §9.6/§9.7〕) / #562(5a523f3 [jetson] dev link 常時運用・mDNS 直結・jetson CLI＋guards) / #561(d701612 [docs] **mode-m1/04 新設＝runtime speed limiter（OQ-T3 設計解）**＋ADR 分野別索引) / #560(4ff560a 前回 STATUS refresh＝本ラウンド境界)。**閉じたもの**: OQ-T3 設計解（#561・実値/配線は残）・OQ-R1〜R7（#568）・OQ-13（#569）・doc10 G-l 状態列と ADR-0009/0010 記録の同期（#567）・#560 残件「未 push mode-m1/04」（#561 で land）。**blocked / next**: ①speed band bringup 配線・帯実値・過渡回帰（ADR-0012 残件＝`feat/speed-band-wiring` レーン進行中） ②実機 M0→M1→M2＋`m1_probe`＋G-g 試験→car_type 確定で ADR-0010 contract PR 解錠（不変） ③OPEN PR 6 本（#493/#499-#503）の merge 判断（不変） ④旧 car_type 二択 stale 2 箇所（`docs/README.md`・mode-x-er/11＝#559 の 0x0A→0.7 一択が正）の別 PR 修正（#567 audit で発見・task chip 化済）。
- **worktree / branch 実査（2026-08-30・#567 land＋掃除後）**: `git ls-remote --heads origin` = **7 heads = `main` ＋ open PR 6 本（#493/#499-#503）＝stale ゼロ**（`docs/g-l-l0-wiring-status`(#567)・`docs/jetson-b-plan-sync`(#573)・`feat/runtime-speed-limiter-minimal`(#566 CLOSED-unmerged) はいずれも削除済）。`git worktree list` = main checkout（detached `9a6ce40`）＋ 開発レーン 6 本（#493/#499-#503 対応・merge 判断待ちで不変）＋ `mwr-procurement`（branch `main`@`ca088f3` が checkout されたまま＝placeholder・要整理）＋ agent worktree 4 本（本 refresh レーン `docs/status-2026-08-30` / `epic-borg`=`docs/oq3-footprint-decision`〔据え置き継続〕 / 下記 2 本）。
- **未 push のローカル作業（実査・他セッション所有＝触らない）**: ①`fix/566-review-fixups`(`2173b54`・#566 CLOSED-unmerged の骨格＋レビュー修正。`warehouse_perception` へ翻案予定) ②`feat/speed-band-wiring`(`4fb979d`・locked＝稼働中・speed band 配線 R-26) ③**`docs/status-10`(`f553aa1`)＝競合 STATUS refresh 下書き（pin `876189e`・#573/#567 を含まず stale）→ 本 refresh が supersede。narrative の #561〜#571 部分は本 block に取り込み済・所有セッションは破棄推奨**。

### 2026-08-30 current

- main ref = `origin/main`(`1937cb6`) 同期。STATUS の SHA pin は `scripts/refresh_status.py --check` で fresh（**host `python3` は 3.7 のため `--fix` は `.venv`(py3.12) で実行する**）。
- **2026-08-28〜08-30 land（M1 一次資料の本流化＋L0' の layer 正準化・`93bfc93`→`ca088f3`・新しい順）**: #559(ca088f3 [docs] **doc02 に M1 一次資料の裏取りを land**〔P-5 = 3D プリントマウント前提の撤回・公式直ネジ止め / P-6 = ERF01 V3.0 回路図で**レギュレート 12V レール不存在**を回路図レベル確認・プロトコル V2 で car_type read 不在 / P-7a・P-7c = 公式 STM32 V3.6.5 実読で `CAR_MECANUM_M1=0x0A`・`CAR_M1_MAX_SPEED=700`・**`ENABLE_IWDG=0`（通信途絶停止なし）**を確定＝**L0' クランプと G-g watchdog の工学的根拠** / P-7d = Orin code 7 本を repo 外キャッシュへ取得し別セッションが独立再検証 / **P-7e = Drive 子フォルダ 2 件の恒久 pin＋「ベンダーコードは実装依存でなく仕様の裏取り資料」の裁定＋再 DL 不要の参照手順**〕＋ **§A を「MicroROS ESP32 Car ×2」→ ROSMASTER M1 へ全面改訂**〔ADR-0006/0009 から3週間 stale だった。**43行→43行の net-zero 差し替え**で `02-hardware-design.md:52` 以降の下流 pin（jetson/01・setup/jetson-deploy・mode-x-er/07・`warehouse_m1_driver/clamp.py`）を1つも割らず、旧 ESP32 仕様は §A 末尾に履歴保存〕。`clamp.py`/`m1_driver CLAUDE.md` は**コメント・docstring のみ**（「STM32 はベンダ製バイナリ」の記述訂正＋失効 pin `02:329`→`P-7c` 再 pin。**実行コードは AST 一致で不変**）)/#556(e15ae39 [docs] GLOSSARY §3 に **L0'** 正準エントリ追加)/#555(9d93e8b [docs] box map レイヤ annotation 表に **L0'（`warehouse_m1_driver`）** 行追加)**。
- **worktree / branch 実査（2026-08-30）**: `docs/procurement-2026-08-23` は #559 squash merge で **remote 削除済**（ローカル worktree `mwr-procurement` は掃除対象）。remote heads = `main` ＋ **open PR 6 本のブランチのみ**（#493 / #499 / #500 / #501 / #502 / #503＝いずれも 2026-08-01 ラウンドからの持ち越しで **merge 待ち**）。stale head は 0。
- **未 PR のローカル作業**: worktree `miniature-warehouse-robotics`（branch `docs/m1-runtime-speed-and-adr-index`・`d1b9c2d`）に **mode-m1/04 runtime speed limiter（OQ-T3 設計解）＋ ADR 索引**が commit 済・**未 push**。次ラウンドで PR 化する。

### 2026-08-22 current
- main ref = `origin/main`(`1937cb6`) 同期。STATUS の SHA pin は `scripts/refresh_status.py --check` で fresh。
- **2026-08-22〜08-26 land（Mode M1 新設ラウンド＝実行モード分離＋L0' 実行体化・`e7e13bf`→`93bfc93`・新しい順）**: #550(93bfc93 [m1-driver] **serial driver node＝G-l 実体化**〔`console_scripts` 空を解消・全 dispatch が `clamp_body_velocity` 必経・**W-1** cmd_vel freshness（既定 0.5s=twist_mux 整合・param ハードニング済）・**W-2** 二重停止（`set_car_motion(0,0,0)`＋`FUNC_RESET_STATE 0x0F`・冪等）・read-only `m1_probe`（car_type/版/battery/encoder・motion 送信なし・部分故障縮退）。backend seam=vendor `Rosmaster_Lib` lazy import＝**wire フレーミングを発明しない**。R-26 16関数＋**mutation 4本個別 KILL**〕) / #551(db46aaf [teleop] **`teleop_joy`＋pure `joymap`**〔**ベクトルキャップ=C-8**（軸独立は対角41%超過）・デッドマン必須・非有限→0・Yahboom 公式 joy node 不採用（`/cmd_vel` 直で L0' 素通り・limit 1.0/5.0・gate フラグ不具合）・mutation 2本 KILL〕) / #549(4d2fb3a [docs] **`docs/mode-m1/` 新設**〔オペレーター指示 2026-08-26: Mode M1=traffic_mode 軸（A/B/C）から独立の実行モード・**必須条件=collision_monitor が起動する構成**（`traffic_mode != 'open-rmf'`＝doc10 §11 **G-k の構造的解決**）・02=**watchdog 多層停止 W-1〜W-4**（**STM32 通信 watchdog 不在をソース調査で確定**〔ENABLE_IWDG 0・timeout 機構なし・実機版一致は G-g 実測待ち〕・G-g 5分手順・X3 幾何ハードコード→odom は 0x0D 生カウント方針）・03=joystick bring-up **M0/M1/M2 ゲート**＋実機プローブ6項目〕＋docs ネットワーク配線〔HTML companion 2本・root `.claude/CLAUDE.md` Important Paths・mode-x-er/10／jetson/01／shared/11 の EOF backlink〕) / #545(a22088b [docs] doc19 残 stale 行 pin re-pin=slash form) / #544(f2940e6 前回 STATUS refresh)。**閉じたもの**: doc10 §11 **G-l**（L0' 未結線→実行体 land・状態列の更新はゲート閉鎖 PR 側）・G-k（構造的解決の正本確定）・shared/11 §6 handle 未決（公式 joy 経路は L0' を通らない→自前経路で必経、の裁定）。**blocked / next**: ①stg/prod `traffic_mode: open-rmf` の書き換え裁定（部屋で L1 反射が消える矛盾・prod config=安全レビュー必須 PR・オペレーターゲート） ②jetson/01＋#266 の G0-G7 rescope（約60箇所・`:52-57`/`:97-113` 行数保存制約・調査済） ③ADR-0011（ros2_control=Phase 2 TARGET 案・command 側寄与ゼロ/feedback 側に価値の非対称が確定） ④実機 M0→M1→M2＋`m1_probe`＋G-g 試験（車輪浮かせ）→ car_type 確定で ADR-0010 contract PR と odom スライス解錠。
- **2026-08-19〜08-22 land（速度上限 unfreeze＋調達実績記帳＋docs 整合・`49819ef`→`e7e13bf`・新しい順）**: #542(e7e13bf [docs] **#541 lane の未 push /consistency-audit 修正 `41b9fd9` の cherry-pick 救出 land**〔E-1 実在しない出典引用の訂正・doc09 追補④ OQ の T 系列改番（doc23 OQ-22/23 との衝突回避）・ADR-0010↔追補④双方向リンク成立ほか 1 ERROR/7 WARN/5 INFO〕) / #541(d551497 [docs] **ADR-0010: `MAX_LINEAR_VELOCITY` をプラットフォーム上限へ再定義（accepted・オペレーター裁定 2026-08-19「本フェーズは安全面より速度を優先」）**〔**契約値 `safety.py:18`=0.3 は本 PR では不変**・pin は実機 car_type 確認（0x01→1.0 / 0x02→0.7 m/s）後の後続 contract PR・**L0' クランプ維持＋`m1_driver` 結線と R-26 pin が引き上げの前提条件**（doc10 G-l と接続）・ESP32 firmware（旧2台系）は 0.3 凍結のまま〕＋ doc02【2026-08-19 追記】M1 速度/音声の調査確定事実（Rosmaster_Lib V3.3.9 clamp 無し・int16 溢れ→無音フレーム落ち・STM32 per-wheel clamp=方向破壊・520 モータ公式表・音声=標準 USB オーディオ）＋ doc09 追補④ **ジェスチャ③=指カウント3帯速度セレクタ**（グー=最遅/1〜3本=最速/パー=安定・L4 publish-only・運用値は **S-SPEED** 実測で確定）＋ **新規 doc11 `mode-x-er/11-standby-and-hri-features.md`**（standby 状態機械・persona「はっちゃん」・wake word/拍手・エア描画・ついてこい・tier ロードマップ）＋ GLOSSARY S-SPEED。**⚠️ lane の /consistency-audit 修正 commit `41b9fd9`（E-1 実在しない出典引用ほか 1 ERROR/7 WARN/5 INFO）が未 push のまま PR head 外で merge されていた**＝orchestrator が統合時に発見し **#542 へ cherry-pick 救出→land 済（e7e13bf）**〔#476→#478 と同型の「audit 修正取り込み漏れ」再演〕) / #540(f5fd8d0 [docs] 調達実績 follow-up〔領収書 PDF 4通 ¥149,330 と明細の ¥2,020 未説明乖離を TODO(要再照合) 射程へ・doc02 配線 ASCII 図を採用済み無はんだ WFR-3 構成へ同期・テスター=**通電前ゲート3役**（19.0V 設定/初通電①前の短絡チェック/④前センタープラス確認）・WFR-3 を WAGO 公式レバー式手順＋定格で TODO(到着後) close・doc02 本文行数維持〕) / #539(0f7bcf7 [docs] stale 行ピン `doc19:94`→`doc19:6 / doc19:118` 張り替え〔doc19 §4.2 中段挿入 +24 行ズレが原因＝doc19:125 が「動かさない」と明文化した行自体が後続挿入で壊れた #165 同型事故。**comma 圧縮形ピン（`doc19:6,:118`）は check_consistency B4 の `_DOC_NUM_RE` で 2 本目が死角**＝slash 形を正とする教訓。同型残件 10 箇所（deploy/jetson 4・doc22:341・`doc19:18,:86` 系 5）は PR #539 本文残件に列挙済〕) / #538(e994df7 [docs] shared/11 新設=ROSMASTER M1 説明書転記＋開梱記録) / #537(acd0c09 [docs] 実購入台帳=power-parts ledger＋doc02 status 同期＋三脚 note) / #533(1139f69 前回 STATUS refresh＝本ラウンド境界)。**narrative**: 調達実記帳ペア（#537→#540）と、部屋スケール転換（ADR-0009）後の**速度方針の意思決定 land**（#541=ADR-0010）が主。**安全境界の実体変更はゼロ**（全 docs・契約値 0.3 不変。引き上げは「L0' 結線（G-l）→ S-SPEED 実測 → contract PR」の三段ゲート越し）。#539-#541 はオペレーター指示で orchestrator が squash merge（CI 全緑・#539 は implementer+reviewer の 2 段 agent team で作成/監査）。**次のキーストーン＝①OPEN PR 6 本（#493/#499-#503）の merge 判断 ②OQ-22 裁定（不変）③doc19 stale ピン残件掃除（同型 10 箇所）**。`warehouse_interfaces` 不触・契約変更ゼロ。
- **2026-08-17〜08-18 land（ADR-0009＝M1 部屋スケール転換ラウンド・`5c8e439`→`49819ef`・新しい順）**: #532(49819ef [docs] **部屋スケール安全レビュー doc10 新設**〔`docs/mode-x-er/10-room-scale-safety-review.md`＝[ADR-0009 帰結⑦ :53](adr/0009-m1-room-scale-operation.md) が要求した分析レビューの実体〕。**最重要所見: ADR-0009 が挙げる「残る保護」3 枚のうち現時点で機能するのは ①語彙 gate のみ**（[10:73-81](mode-x-er/10-room-scale-safety-review.md)）——①到達集合を 9 waypoint に限定する語彙 gate（**L3/L2**・`handoff.py` fail-closed）は機能〔ただし選択規則 H-1・座標ゴール seam H-4 の穴あり〕/ ②**L1 collision_monitor は二重に無機能**〔(a) 停止円 `radius: 0.09` < M1 内接 0.1157 ＝車体内部でしか発火しない (b) `traffic_mode: open-rmf` の **stg/prod では node ごと未起動**＝`nav2_bringup.launch.py:126` が node と lifecycle manager の両方を gate・dev のみ `none`〕/ ③**L0' 0.3m/s クランプは紙の上のみ**〔`warehouse_m1_driver` の serial driver node が未 land＝`setup.py` の `console_scripts: []`〕。Emergency Guardian は**人に対して寄与ゼロ**（設計どおり＝ロボット間の pose 距離監視。「残る保護」に足してはならない）。**部屋運用開始 gate G-a〜G-m 13 本を定義**（[10 §11](mode-x-er/10-room-scale-safety-review.md)・**全て未充足**＝未 land / 未裁定 / 未実施 / 未確認）＝**運転許可はオペレーターゲート**) / #531(1492115 [docs] **ADR-0009: M1 は部屋スケールで運用する**〔ジオラマ 1800×900 は **M1 フェーズでは凍結**・ジェスチャ召喚を主役へ・**sim はジオラマのまま回帰環境として維持**〔帰結④＝sim と実機の環境乖離を代償として受容〕・**W3（M1 向け 9 点ジオラマ waypoint）中止**・**F 系列 re-scope**〔F-5-3 の具体値（±14mm 低コスト帯・inflation ≈0.125-0.126）は 280mm 通路由来ゆえ**無効・手法のみ生存**／F-5-4 Spin recovery 抑止は発火条件が「通路内で回転不可」→「人の脚の横で回転しない」へ差し替え〕・**新規 OQ-22**（`nav2_params.yaml` footprint と `warehouse.base.yaml` `locations` の sim/実機 config 二重化・優先度最高）**/ OQ-23**（部屋での nvblox dynamic 層再評価＋部屋体積での TSDF/ESDF メモリ再試算＝S1 の残 RAM ≥500MB 判定に直撃）・帰結を doc23 G 系列（G-1〜G-12）・doc09 追補・doc06/00/01/02/04/05 へ展開〕) / #530(5e8da17 [nav-traffic] **OQ-3 解決＝hybrid 非円形 footprint の採用（F 系列 F-1〜F-7）**〔doc23 ＋ doc02/04/11a ＋ HTML 2 本へ展開。Refs #519〕。**Slice 1（F-5 params 実装）は OQ-22 裁定が着手前提のため blocked**＝[23:765](architecture/23-perception-and-localization.md)「G-7 の config 二重化（OQ-22）が Slice 1 の最初のステップ」) / #528(3487380 [sim] **KNOWN_LOCATIONS を走行目標点へ改訂**〔shelf goal が棚 box 中心＝**障害物内**で Nav2 ABORTED という構造欠陥の修正・sim gate 2026-08-17〕。ADR-0009 により**本値は sim 値として維持**され、**M1 実機値は Phase-1 の部屋 SLAM 後 waypoint 実測へ**〔ADR-0009 Decision 5 / doc10 G-e〕) / #527(b5a0175 [docs] doc23 B-13 に **sim gate PASS を記録**＝A-10 受け入れ close・**OQ-16/17 は残**) / #526(0c7e4b3 前回 STATUS refresh＝本ラウンド境界)。**narrative**: 本ラウンドは **docs のみでプロジェクトの運用前提が変わった**——sim gate PASS 記帳（#527）→ goal 構造欠陥の修正（#528）→ footprint OQ-3 決着（#530）→ **ADR-0009 で M1 をジオラマから部屋へ**（#531）→ その帰結⑦が求めた安全レビュー（#532）で **多層防御 3 枚のうち 2 枚が実体として欠けていた**ことが判明（部屋転換が壊したのではなく、**人が走行面上に立つことで既存の欠落が安全上重大になった**）。**次のキーストーン ＝ ①OQ-22 裁定（#519 Slice 1 の解錠）②doc10 の operator gate 裁定（G 系列・特に G-b 停止円改訂 / G-c 召喚レグ解決規則 / G-d 座標ゴール seam / G-i recovery bypass / G-k env 構成確認 / G-l m1_driver clamp 結線）③Phase-1 部屋 SLAM mapping**。他トラックの **OPEN PR 6 本（#493/#499-#503）は merge 判断待ちで不変**。`warehouse_interfaces` 不触（全 docs 変更）・**安全境界の実体変更はゼロ**（変わったのは記述と可視化＝欠落の明示）。
- **2026-07-26〜08-17 land（M1 単騎構成 docs 確定＋safety-state OQ-11 根治＋dev-tooling 整備・`8c22ff6`→`5c8e439`・新しい順）**: #525(5c8e439 [safety-state] pose gate から speed 項を除去〔出発直後の駐機→発進デッドロック解消・doc23 A-5③ へ code-to-docs〕) / #523(4978cae [dev-tooling] `check_consistency.py` の B4 走査から `.claude/worktrees/`〔agent worktree の repo フルコピー〕を除外＝spurious B4 WARN（25/23/38 本×3回観測）のノイズ根治・pyproject `[tool.ruff] extend-exclude` をミラー・`ws/worktrees/` 等の同名別 dir は走査継続) / #524(9ec5b5e [safety-state] Guardian pose-freshness **変位ゲート**〔doc23 A-5③〕＋999 fail-open 回避策の全撤去＝**OQ-11 駐機 pose_stale 偽陽性の根治**〔1.02s 発火・自己ラッチの sim 実証済み事象〕) / #522(18f6ab7 [dev-tooling] ruff target-version を py310 へ flip〔ADR-0008〕＋PEP 695 構文一掃) / #521(b75c6f3 [skeleton] `camera_link` を FROZEN_LINK_NAMES へ追加〔doc23 §4 contract・additive〕) / #520(5f12436 [docs] html-explainer skill にディレクトリツリー型図解パターン §5) / #518(8258ed6 [docs] **M1 単騎構成の設計確定**＝ADR-0006/0007/0008・doc23・MOLA-LO ＋ [m1-driver] L0' クランプ先行スライス) / #490(c91d3c1 [eval-sdk] Phase-1.5a nav-success + smoothness stats〔doc21 §13.1〕＝Langfuse バッチA #431 完了→**#432 解錠**) / #491(36132cf [docs] ruff <0.16 pin〔0.16.0 format drift の repo-wide 赤対策・のち #522 で range 更新〕) / #481(41c0a86 前回 STATUS refresh＝本ラウンド境界)。**narrative**: M1（ROSMASTER）単騎構成の docs 正本化（#518→契約 #521）→ safety-state OQ-11 偽陽性根治 2 連（#524→#525）→ dev-tooling 衛生（#491/#522/#523）→ eval-sdk KPI 前進（#490）。**merge 待ち OPEN PR 6 本**（#499-#503=round-2026-08-01 5レーン CI 緑・#493=fix-up 待ち）が次の land 候補。契約変更は #521 のみ（additive）。
- **2026-07-14〜18 land（firmware Phase-1 host-first ラウンド＝安全純ロジック実装＋実機前 decision docs・`7b5b32f`→`8c22ff6`・新しい順）**: #480(8c22ff6 [safety-state] GLOSSARY 正準語「L0 voltage brownout floor（over-discharge floor）」追補＝ADR-0005↔GLOSSARY↔doc12:82 双方向 close〔percent 凍結 `battery_is_critical(pct)` と別名・非対称・L0 現行 cutoff 無し〕) / #478(3b6d87e [firmware] #476 の docs-reviewer 修正が未コミットのまま merge された誤引用を是正＝R-43 の `R-45(doc07:255=LLM比較・無関係)`→`T4(doc07:78)`＋D1 時制整合〔#476 follow-up〕) / #477(d489af3 [safety-state] **ADR-0005 L0 battery brownout floor**〔将来 L0 voltage brownout floor は percent policy と別名・別機構・L0 現行 cutoff 無し・percent 3段=L1 所有・cutoff 電圧 Phase-1 実測〕＋doc12 net-zero in-line＋main.cpp comment sync) / #476(782c6ff [firmware] Phase-1 decision docs＝**D1 CMD_TIMEOUT 導出ポリシー**〔上限=0.3×timeout≤許容 residual・robot-length ≤0.2m 目安・plain constexpr placeholder・tighten-only〕＋**D2 R-43 既定=Reliable+MTU-first**〔0.4°保持・fallback=downsample→USB・値は Phase-1 実測〕) / #475(9c29d3c [firmware] **S3 client_key**＝`derive_xrce_client_key(bot_id,mac)` 純ロジックで R-37 第一対策〔弱RNG session 衝突を board identity 決定的導出で排除〕・host R-26 unit〔独立オラクル・mutation-red〕＋CI firmware-safety gate・2段敵対レビュー APPROVE) / #470(dda2867 [llm-bridge] live ER→L3→L2 governance dispatch test〔0 actuation・module-skip gate・3 paid ER call〕＋run_manifest.v1・text-only ER fail-closed) / #469(84466e5 [llm-bridge] offline full-stack E2E ER→L3→0-dispatch→observation incl L0 heartbeat_lost) / #468(be8d840 [firmware] **L0 command-stream watchdog 純ロジック** host-tested R-26〔`command_is_stale`・unsigned rollover-safe・`>` 境界〕) / #467(cf14294 [safety] doc12 Layer 0 に heartbeat/watchdog deadman を additive 反映＝#466 drift 解消) / #466(7d8ee44 [docs] **L0 firmware Phase-1 strategy**＋heartbeat/watchdog cross-doc drift 棚卸し〔phase1-l0-strategy.html・PHASE1_CHECKLIST D1/D2 decision-needed〕) / #464(1e13143 前回 STATUS refresh=本ラウンド境界)。**firmware Phase-1 = host-first 完了**〔#475 client_key merged・安全純ロジック(clamp/watchdog)全緑・decision docs(D1/D2/battery ADR-0005)確定・GLOSSARY 整備〕。**A–D 実機配線は実機到着ゲート**（Yahboom ESP32×2＋ORBBEC MS200 発注済み・未着）。epic **#472 OPEN**（host-first ready / A–D blocked）・S1 proximity_stop は DEFER→S6・S5 follow-up=W1 config.h fail-loud/W2 R-37 close 記帳/W3 main.c:81 引用 reword/CMD_TIMEOUT placeholder。実装/レビューは workflow+agent-teams（LEAD→sub-reviewer 2段・敵対レビュー）。`warehouse_interfaces` 不触・安全境界不変（L0 純ロジック追加のみ・凍結 clamp 不変）。**残キーストーン＝実機到着後 A→B→C→D + #342 G5 sim human gate（別トラック）**。
- **2026-07-13 land 第3ラウンド（Mode X-ER G5 実走準備 完了・`8261403`→`7b5b32f`・新しい順）**: #462(7b5b32f [docs] **layer-annotation ルール**〔`.claude/rules/layer-annotation.md`=回答/plan/実装/PR/レビューで対象 layer(L0–L4/traffic/観測面) を明記・正準表は #461 参照で複製しない・self-modification gate ゆえ apply スクリプト経由でユーザーが land〕) / #461(de07411 [docs] **正準 layer 対応表**〔`docs/productization/01-commercial-box-map.md` 末尾に L0–L4→責務→package/主要ファイル表＋layer≠process/package 注意=同名 `executor.py` が L4/L3 併存の罠・GLOSSARY §3 backlink・全行 code 実査裏取り〕) / #459(575e8cd [mode-x-er] **G5 choreography v2（t1–t5 完了依存 DAG）**〔dev/08 追補3=2台・帰還先=既存 `berth_A`/`berth_B`（KNOWN_LOCATIONS 内・契約変更なし）・**t4 は t2（bot1 帰還）完了で解放**＝「離れたら」の v1 近似・doc08 §10=per-run 実行系切替（config+再起動・per-instruction 非採用）・v2 kit `deploy/dev/xer6/`・offline pin=t4 解放エッジ/fail-closed 負系/duplicate_destination 非誤発火/in-flight≤1〕) / #458(db50693 [docs] **doc02 クロスロボット依存トリガー語彙 裁定**〔到達トリガ=完了依存で今日可能・離脱トリガ=帰還完了依存で近似＋L2 `duplicate_destination`/L1/L0 の安全網・空間述語=将来 ADR 級・早見表。**≥0.15m は traffic 層（`TRAFFIC_MODE=none` で非アクティブ）・L2 Policy Gate に距離チェック無しを明文化**〕) / #457(8fd1cf2 [docs] doc07+HTML re-pin `8261403`＋doc09 tidy＋cross-review touch-up〔HTML `adapter_factory.py:130`・replay 経路の文言反映・GLOSSARY/variants re-pin〕) / #456(4114195 [mode-x-er] **G5 無償 offline-replay 経路**〔`mode_x_er.er_offline_payload`=replay adapter は HTTP sender 無し＝**provider call 構造的に不可・課金ゼロ・`WAREHOUSE_LIVE_ER` 不要**・`deploy/dev/xer6/` 実行キット commit・CI oracle `test_xer6_g5_replay_artifacts.py`〕) / #455(90e7cc9 [mode-x-er] decision_events envelope に **plugin_id 帰属**〔doc05 §8.11 additive・#446 凍結 `operator_notice.v0` wire 不変・#451 fixture の帰属保持を検証・reject 集計を plugin 別区別〕) / #454(c09cf4a [docs] 前回 STATUS refresh〔`8261403`＋#449-#453 land block〕=本ラウンド境界)。**全 PR は PR 毎 LEAD＋3レンズ（BUG-HUNTER/SPEC-AUDITOR/SAFETY-GATE）の敵対的レビュー（mutation probe 実施・#459 は夜間 stall で 2 回走）を経て squash merge・worktree/branch 掃除済**。**結論: #342 G5 の実走に必要な全 artifact／運転手順（dev/08 追補2 無償 replay＋追補3 v2 振り付け）／依存語彙裁定／layer 規律が main に揃った。offline オラクルは全緑（L0 firmware clamp 9・L2 Policy Gate 78・L3 planning core 166＋G5 v2 5＋replay 6）。残る唯一のゲート = #342 RUNNING Gazebo sim（human gate・課金ゼロで実施可）**。`warehouse_interfaces` 不触（config/deploy/docs/bridge-local のみ）・安全境界不変。
- **2026-07-11 land 第2ラウンド（composition 残穴 5 レーン完走・`1fe0747`→`8261403`・新しい順）**: #453(8261403 [mode-x-er] doc09 実装順序 step3/4=**DecisionEventEnvelope 検証 + DuckDB offline join report**〔optional duckdb・pure-python 既定〕) / #452(0c02817 [mode-x-er] **Visual Resolver object-class snap conjunction**〔doc02:150 完了・additive `UnresolvedReason.OBJECT_CLASS_MISMATCH`・`location_classes` は開示済 consume-gap 発明＝absent-class は距離のみ従来 snap で挙動変化ゼロ〕) / #451(219d2c8 [mode-x-er] **repo-root `plugins/` incubator 新設**〔doc09:262-274 layout・l3.zone_policy draft + 実行可能 fixture pair・ruff I001 副作用(spike variants.py)対処＋variants.py:51→:50 re-pin 込み〕) / #450(22f3647 [mode-x-er] **本番 plugin-factory seam**〔doc09 末尾設計節 + `robotics/composition/factory_registry.py`=explicit registry・今日は空・read-only + x_er_bridge 配線・fail-closed 3挙動 R-26 固定。残=lifecycle status gate 未強制・incubator 配線は review gate 後〕) / #449(7e03c74 [docs] doc09 stale RESIDUAL 2箇所＋plugin_manifest docstring を landed 状態へ同期〔net-zero 行編集・下流 :NN 無傷〕) / #448(1fe0747 前回 STATUS refresh)。**全 PR は LEAD+3レンズの敵対的レビュー（mutation probe 実施）を経て squash merge・worktree/branch 掃除済**。composition 層の残穴が閉じ、次は plugin_id envelope 拡張（#453 追補）・doc07 re-pin・G5 準備が並走中（新レーン群＝下記 worktree 実査）。**keystone は依然 #342 G5 sim human gate**。`warehouse_interfaces` 不触・安全境界不変。
- **2026-07-11 land（次実装ラウンド=scout workflow→2 レーン完走＋PR 毎 LEAD→sub-reviewer 2 段敵対レビュー→全 merge・`c838cd2`→`651e4d6`・新しい順）**: #445(651e4d6 [llm-bridge] eval_sdk tracer に disabled-tracing 1 回 WARN＋teardown on-loop 維持〔当初 #282 DoD① の threaded time guard は敵対レビューが **OTEL context detach の thread-affinity 回帰**を最小再現で確定→revert・`Refs #282` 止まり=hard bound 要件化時は別アーキ判断〕・thread-affinity 回帰ガード test=mutation 5/5 RED) / #444(9dd18b8 [llm-bridge] tests/fixtures 新設＋realistic Hermes 応答 fixture→凍結 `Command` end-to-end＋`chat.completions.create` への timeout kwarg assert〔default 5.0+custom・従来完全未ガード〕, Refs #281〔録画 live fixture=#88 gate で OPEN 継続〕) / #443(e578d45 [wo] kpi.py docstring の doc08 ミスピン 3 行 re-pin=:373/375→:395/396/397〔kiiyuu #432 の同一ファイル衝突予防で先行 land・AST 一致で挙動不変を機械証明〕, Refs #296) / #446(587a951 [mode-x-er] **`/operator/notice` 契約凍結（contract）＋publish-only publisher**=#345 step②〔doc05 §8.10 確定値=`operator_notice.v0`・RELIABLE/KEEP_LAST 10・MVP publisher=nav2_bridge(L1)+mcp_server(L2)・emergency は `/emergency/event` 相乗り＝**`WIRE_NOTICE_DECISIONS`(SPEAK−emergency) guard で二重 publish を構造的に封鎖**・v0=reject 級のみ〕＋doc03「Jetson 内部」表へ 1 行 additive＋下流 doc03:NN 参照の全数 1:1 同期〔#165 回避・doc12/19/22/jetson/05/06〕・R-26 publish-only=独立オラクル mutation 5/5 RED・merge 前 3 レンズ最終検証が B2 残存〔doc06 §0 :33/:35 の DEFER/depth=20 矛盾〕を検出→8866ecd で同期・**契約予告 4 通投稿済**〔#5 safety-state/#8 nav-traffic/#6 wo/#298 web〕, Refs #345)。**全 4 PR に PR 毎 LEAD→sub-reviewer 3〜5 レンズの 2 段敵対レビュー＋コメント投稿を実施**（#443/#444/#445=APPROVE・#446=APPROVE-WITH-CHANGES→blocking 2 件修正後 merge）。`warehouse_interfaces` 不触（#446 は `std_msgs/String` JSON・bridge-local）・安全境界は強化方向のみ（emergency wire guard）。**follow-up**: `doc22:95` の base 由来 doc03 参照ドリフト（#446 非起因）／#282 hard bound の別アーキ要否／#283 preflight hint 残（close 不可・小 follow-up 化推奨）。
- **2026-07-10 land（L2 正本化＋open PR 全 4 本完走・`602d96b`→`c9e9ef0`・新しい順）**: #441(c9e9ef0 [llm-bridge] #423 後 stale だった forwarder コメント 3 箇所を 3-way gate〔A/B 注入・C None・X-ER `dispatch.forward_to_nav2`〕へ同期・挙動変更ゼロ) / #427(978b8f3 [safety-state] Policy Gate 鮮度窓 config 化＝**tighten-only 採用（ADR-0004 裁定①）**〔凍結既定 0.5/2.0 が上限・緩い値は起動拒否 fail-closed・`MAX_FRESHNESS_S=10.0` 天井は撤去（0.3m/s×10s=3.0m>ジオラマ1.8m で物理封筒を担保しないため）・境界 mutation kill＋`tools.py` 配線 seam pin テスト・doc11/ADR-0004 の REVIEW POINT close〕) / #429(6cdbdfb [docs] XER6 live-matrix 知見を dev/07・dev/08・retrospectives へ EOF-append 織込〔12/12 sends・ER median 4.68s・鮮度所見は処方せず disposition open 維持〕) / #428(ef899da [mode-x-er] XER6 live-matrix harness=`spike/xer6-live-matrix`〔multi-manifest live e2e＋per-box timing・`WAREHOUSE_LIVE_ER` は operator script のみ set・budget hard cap 12・CI fix=ruff format 2 file＋stray PR_BODY_B 削除〕) / #422(53f9501 [docs] **L2 層の正本 doc11 新設**=Contract†/Governance/Traffic box 詳細＋`layer-l2-detail.html`＋**ADR-0004（L2 restrict-only policy profile=凍結値 floor・data-only・v1 code plugin 不採用）**＋GLOSSARY §10〔二段ゲート/accepted-motion gate/action_map 等 10 語〕＋二段ゲート・coordinate goal 6-gap 補足) / #420(1564e83 前回 STATUS refresh)。**全 4 PR に 2 段構造の敵対レビュー（PR 毎 LEAD→sub-reviewer×3・各 PR へコメント投稿済）を実施し、同日朝の先行レビューが見落とした CI 赤（`ruff format --check`）を根因特定→修正→CI 緑で land**。housekeeping: 日報 Issue の open 放置 15 件を含む全 38 件 close＋daily-dev-report routine に「当日以外を自動 close する step 7」を追加。`warehouse_interfaces` 不触（#427 は mcp_server 局所＋config additive）・安全境界は強化方向のみ。
- **2026-07-07 land（XER6 Slice A＋B・`f0ccd64`→`602d96b`・新しい順）**: #424(602d96b [docs] doc07 impl-status **re-pin 1609515→9ad7ec9**=connectivity verdict の反転を「OFFLINE-WIRED≠RUNNING」の正直な区別で反映〔背骨 land・G5 sim human gate 未実走を明記〕・`check_impl_status` FRESH) / #423(9ad7ec9 [mode-x-er] **XER6 Slice B**=completion 配線〔`/nav2_bridge/goal_result`→純 ROS-free `x_er_completion`→mark_succeeded/failed→retrigger・robot 相関・fail-closed〕＋autonomous red→blue e2e〔手動 mark 無し〕＋forwarder flip〔`resolve_nav2_forwarder`=`dispatch.forward_to_nav2` strict-bool・safe-OFF〕＋llm co-gate〔launch `llm∧mode_x_er.enabled`=Slice A residual 解消〕。敵対自己レビュー〔4 レンズ isolated worktree〕が blocking 1=mid-cycle apply の live-handle 侵害→**enqueue＋cycle 間 drain** で修正・1817 passed/0 fail) / #421(8665e02 [docs] **Slice B key 凍結**=`mode_x_er.dispatch.forward_to_nav2`〔既存 `nav2_bridge.base_url` 再利用・新 endpoint key 発明なし〕・`request_fixture` §3 昇格・`llm:=true`∧`mode_x_er.enabled` co-gate 契約＋doc03:110 X-ER consumer 追記＋audit F2-F5 stale 是正) / #419(949ae7f [mode-x-er] **XER6 Slice A = `x_er_bridge` offline backbone**〔`x_er_composition`=run/plugin manifest 取込＋XER6 委譲の三重突合＋site-profile hash gate＋governed calibration 消費（#416 seam の稼働 caller）＋effective-composition witness／`x_er_cycle`=**plugin gate を compile より先行**（F1 二重 validate・`permits_dispatch` false⇒0 dispatch・store 無接触）→`compile_raw_output`→gen 発番（ER 出力非依存）→dispatch（audit-based 厳密 task 整列）／`x_er_bridge` node shell=起動 fail-closed・Slice A は `nav2_forwarder=None` 固定=0 actuation／launch は `mode_x_er.enabled` gate=`llm_bridge` commander と相互排他・base.yaml 末尾 additive〔#1 予告済〕〕・新 61 tests〔R-26 set＋red→blue ordered e2e〕・**2 段敵対レビュー（PR 毎 LEAD＋5 レンズ並列×2・R-26 は実 mutation 全数赤化を実証）で major 計 3 検出→全修正**) / #418(5ce75b0 [docs] **doc08 `08-x-er-bridge-node-spec` 新設**=x_er_bridge node 契約の設計正本＋doc06 §3 **RESOLVED**=`mode_x_er:` config key 凍結〔traffic_mode 直交・`x-er` 値は発明しない〕＋doc02:354/ADR-0003:71 の stale「未着地」是正) / #416(eda2513 [llm-bridge] governed-calibration production seam=`resolve_governed_calibration(_with_loader)`・fail-closed・11 R-26) / #415(cdb34e0 [docs] 前回 STATUS refresh)。**connectivity hops ⓪(稼働cycle)③④⑤ が offline で閉じ、#409=6/6 完了で CLOSED・#339/#340/#341 も DoD 逐条監査（origin/main 実体裏取り）で CLOSED・#342 は `blocked`→`ready` 反転のうえ Slice B〔`/nav2_bridge/goal_result`→`mark_succeeded` 稼働配線・forwarder key を doc08 §3 追記→sim flip・G5 受入〕へ継続**。`warehouse_interfaces` 不触・安全境界不変。
- **2026-07-05 land（composition residuals・`7379ba6`→`f0ccd64`・新しい順）**: **Mode X-ER A案 composition foundation（#403-#407）land 後の残 wiring を tracking issue #409 配下 4 レーンで消化**: #413(f0ccd64 [docs] #409-D=`compile_raw_output` 行 ref 再pin sweep〔`pipeline.py:89-150`→`90-187` 系の #404 成長による drift を解消〕+ 前回 STATUS refresh〔`7379ba6` まで〕) / #410(3eeddd6 [llm-bridge] #409-A=`emergency_stop` を base `PlanPolicy` の ceiling として narrow-only 許可＋composition preflight を `==` 等値検査へ厳格化) / #412(6cef27e [llm-bridge] #409-C=`site_profile`/`calibration_governance` を `effective_composition.v1` witness に埋め込み〔S3 の profile/calibration を effective-composition 側で観測可能化〕) / #411(461da02 [llm-bridge] #409-B=plugin manifest schema 追加 + two-manifest ingestion loader〔`PluginManifest` + registry〕)。**residual 5/6 消化**（残 = calibration wiring を稼働 Bridge cycle へ通す配線＝**XER6 / #342** 継続）。**#409（tracking issue）OPEN 継続**〔本 refresh で `7379ba6`→`f0ccd64` pin 追随・land block 追記〕。**次段 = XER6 `x_er_bridge`〔稼働 Bridge cycle への composition/calibration 配線・live sim/Langfuse＝#342〕**。`warehouse_interfaces`/config/topic 不触（bridge-local composition）・no-actuation・安全境界不変。
- **2026-07-05 land（6a5ad2e 以降・新しい順）**: **Mode X-ER A案 composition foundation（bridge-local manifest/plugin composition）**: #403(35ad092 [docs] bridge-local manifest/plugin composition〔条件付き A案〕=doc09 standardize + 7 conditions + ADR-0003) / #404(1dd2b74 [mode-x-er] S1: store seam pierce=`compile_raw_output` に keyword-only・相互排他な `executor=`/`store=` 注入〔durable TaskGraphStore 差替口・default-path 後方互換〕。**`compile_raw_output` は 89-150 → 90-187 に成長=本 PR #409 で行 ref 再pin**) / #405(2bb883e [llm-bridge] S2: `run_manifest.v1` + fail-closed composition preflight + effective-composition witness) / #407(beb3d64 [llm-bridge] S4: pluggy `validate_plan`=typed hookspec・namespaced codes・policy clamp・fail-closed registry) / #406(7379ba6 [llm-bridge] S3: site-profile content-hash + calibration governance gate)。**その間の L3/ER land: #399(1609515 ER adapter factory=config→`resolve_audio_transport`→`GeminiErAdapter`〔HERMES/8644・direct fallback〕)/#401(7125751 ER fork gateway 8644 を標準 ER gateway に集約=all-modality + runbook + env-gated audio test)/#400(33820aa [docs] ER-in-Hermes 標準化 ADR + doc06 §5)/#397(b7ccc32 [docs] impl-status refresh〔#389 live-send DONE〕+ tripwire pin-vs-main fix)/#389(ad563de [mode-x-er] Live ER transport=assembly freeze + `propose_plan`〔Hermes + direct fallback〕, Refs #344)/#385(89e5c14 [docs] XER6 live X-lite sim runbook=Docker/Nav2 赤箱/青箱 demo)/#383(79eed41 [mode-x-er] L3 seam-by-seam e2e + live-to-Command runner)/#382(75f6af3 [mode-x-er] HLF: `LangfuseTranscriptTracer` を fail-open span に配線)/#381(208eb76 [mode-x-er] XER6 offline pipeline wiring=`compile_raw_output`)/#380(a4a73f0 [mode-x-er] XER5 Command Compiler offline core)/#379(e355dd1 [mode-x-er] L3 chain tests=1:1 target mirror + draft-equivalence guard)**〔+ docs/skills land #396/#395/#393/#391/#390/#388/#387/#386〕。**#409（[mode-x-er] A案 composition 基盤 land 後の残 wiring / residual）OPEN=本 round の residual を追跡**〔本 PR = #409 Lane D=doc09/ADR-0003 が触れない `compile_raw_output` 行 ref 再pin〔`pipeline.py:89-150`→`90-187` 系〕+ STATUS refresh。稼働 Bridge cycle への composition 配線は XER6 継続〕。
- **2026-07-01 land（782aa37 以降・新しい順）**: #363(6a5ad2e [docs] CI: bridge-leg Option-D pytest を新 `bridge-option-d` job で gate＝default `python-quality` が SDK extras 不在で `importorskip` skip する穴を塞ぐ〔`openai>=1.50,<2`＋`langfuse>=4.9,<5`〕・doc20:48 常時 job 一覧同期・net-zero。**3層敵対 review（1 LEAD→4 sub→16 micro-probe・offline）**: isolated worktree mutation で drift-neuter/header-drop→guarded test RED を実証＋SDK 無しで module skip を実証＝gate は necessary かつ enforcing。review nit（openai pin/未使用 ruff 除去）反映後 merge, Refs #363) / #373(2a1c5fc [mode-x-er] live ER→L3 chain forerunner〔`WAREHOUSE_LIVE_ER=1` env-gate module-skip〕＋再利用 `deploy/dev/run-live-er-smoke.sh`〔`--check` safe・鍵非echo〕, Refs #342) / #374(5bb1731 [mode-x-er] offline ER→handoff→validator→resolver chain e2e test〔direct/hermes 両 transport 同一 destination・rejected plan は resolver skip=R-26 0-dispatch〕, Refs #339) / #375(c9f2939 [docs] agent 自走 live ER を codify〔provider key を `~/.zshenv` 恒久＝再入力不要・cost は batch 毎 operator 確認・standing 無承認 spend 無し〕＋#366/XER3 行ref 再pin・runbook doc07 §4.5, Refs #336) / #376(d509d3e [mode-x-er] XER3 hardening=visual_resolver に w<=0 cheirality 0-dispatch 穴 fail-closed＋NaN(snap_radius/reproj) fail-closed＋malformed polygon guard＋`ResolvedTarget` model_validator〔unresolved→destination None〕・回帰 test〔敵対 review が major=w<0 を摘出〕, Refs #339) / #372(e920829 [mode-x-er] XER4/G3 offline core=`task_graph_executor`〔states/executor・deep review PASS〕, Refs #340) / #371(bd99fd3 [mode-x-er] XER3/G2 offline core=`visual_resolver`〔pixel→map homography＋KNOWN_LOCATIONS snap＋policy・R-26 unresolved→None〕, Refs #339) / #368(b70ae04 [docs] LLM-assisted rule authoring proposal) / #369(2ae8e85 [docs] Mode X-ER live ER→L3 e2e operator runbook=`docs/dev/07`) / #367(fa4a646 [mode-x-er] XER-OF1 offline operator-notice builder, Refs #345) / #366(bf6ca94 [mode-x-er] XER2/G1 L3 Validator＋ValidationReport＋PlanPolicy=決定論 Core〔8 stable code・0 dispatch・PlanningContext/StateStore/calibration/TaskGraphStore seam〕→**#338 CLOSED**, Refs #338)。**Mode X-ER L3 backbone land: XER2 Validator(#366→#338 CLOSED)→XER3 visual_resolver(#371＋hardening #376)→XER4 task_graph_executor(#372)→offline chain e2e T1(#374)＋live forerunner(#373)・agent 自走 live ER 恒久化(#375)＋CI gate(#363)。次段=XER5 Command Compiler〔resolver/executor を `pipeline.py` に配線し凍結 `Command` 生成〕→XER6 live sim/Langfuse 配線(#342)。epic #336 OPEN・XER3 #339/XER4 #340/XER6 #342/OF #345 は Refs 継続で OPEN（#4 は CLOSED）**。
- **2026-06-29 land（7ad0c6c 以降・新しい順）**: #364(782aa37 [docs] ValidationReport `status`/`code`/`severity`/`dispatch_effect` 語彙確定=XER2/G1〔#338 L3 Validator〕prereq〔seam 着手前に値側 semantic 検証の語彙を docs 固定・doc02 末尾追記+doc06 §1 in-place・`dispatch_effect`/派生 `code` は内部派生ラベルと明記〕・契約不変, Refs #338) / #359(705db6c [docs] eval plugin composition manifest=productization に run/plugin/site manifest の責務分離 doc 追加〔`docs/productization/09-run-manifest-and-plugin-composition.md`〕・docs only・契約不変, Refs #269)。**XER2(#338) Validator の語彙が docs 先行確定 → 実装は Lane A `feat/mode-x-er`。eval productization manifest は doc 先行（#269）**。
- **2026-06-27 land（1d0b1e8 以降・新しい順）**: #357(7ad0c6c [mode-x-er] Hermes ER `input_audio` fork productionization=deploy パッケージ〔patch applier + lean gateway launcher・current は direct 恒久 fallback〕 + HLF-G0/Option-D Langfuse trace-owner scaffolding〔opt-in 観測層・shipped default は Bridge-owned 継続〕・deploy/ のみ・契約不変, Refs #356) / #360(08deabc [mode-x-er] Option D=plugin-owned Langfuse trace〔predict-seed `create_trace_id("H::H")`・Langfuse fork ゼロ〕 + Bridge wiring・`WAREHOUSE_LANGFUSE_OWNER=hermes_plugin` で Bridge脚/scorer脚を1 knob flip・**既定 `bridge`=Pattern A 不変**・Bridge脚 live PASS〔trace 着地確認〕・end-to-end score-join は human-gate・`warehouse_interfaces` 不変, Refs #356) / #355(3002fd8 [docs] L4 transport=**default Hermes**〔`input_audio` fork で audio-via-Hermes demonstrated・未 ship ゆえ current=direct〕 + Langfuse Opt C・docs/html 8ファイルのみ・契約/コード不変, Refs #336) / #358(18db682 [docs] productization 観測図整備 + eval-sdk-detail 引用修正)。**Mode X-ER L4 transport/Langfuse クラスタが docs 先行確定 → 実装は #356（input_audio fork productionization + default-Hermes transport flip）で追跡。#336 残 XER2-6 継続**。
- **worktree 状態（2026-08-26・実査/3 PR land＋掃除後）**: `git worktree list` = main worktree(`main`=`93bfc93`) ＋ 開発レーン 7 本（`mwr-doc21`(#502) / `mwr-nav2-preflight`(#503) / `mwr-sim-docs`(#493) / `mwr-web-trace`(#500) / `mwr-wo-kpi`(#501) / `mwr-xer-opnotice`(#499)＝merge 判断待ちで不変 ＋ **`mwr-procurement`=`docs/procurement-2026-08-23`**〔2026-08-23 M1 公式資料実読ラウンド・**ローカルのみ・未 push**〕）＋ agent worktree 1 本（`.claude/worktrees/epic-borg-0c48ae`=`docs/oq3-footprint-decision`＝据え置き継続〔ローカルのみ・未コミット残〕）＋ **本 status refresh レーン**（`mwr-status-2026-08-26`=`docs/status-2026-08-26`）。本ラウンド merged 3 branch（`docs/mode-m1-foundation`(#549)/`hw/rosmaster-m1`(#550)/`feat/teleop`(#551)）は land 直後に worktree remove＋local/remote branch 削除済。stale だった agent worktree 2 本（`modest-lewin`=`docs/repin-doc19-remaining`〔#545 squash 済＝§7.3 `gh pr view --json state`=MERGED 判定〕 / `brave-varahamihira`=detached `e7e13bf`〔前回 orchestrator レーン〕）も本ラウンドで remove＋branch 削除済。`git ls-remote --heads origin` = **7 heads = `main` ＋ open PR 対応 6 本（#493/#499-#503）＝stale ゼロ**。**OPEN PR（`gh pr list` 実査）= 6**（#493/#499-#503・本 status PR 提出で 7）。
- **worktree 状態（2026-08-22・実査/4 PR land＋掃除後）**: `git worktree list` = main worktree（**`main`=`e7e13bf` へ復帰済**。#541 lane が checkout していた `docs/speed-cap-unfreeze` は未 push audit 修正を #542 へ `cherry-pick -x` 救出→land 後にローカル branch 削除）＋ 開発レーン 6 本（`mwr-doc21`(#502) / `mwr-nav2-preflight`(#503) / `mwr-sim-docs`(#493) / `mwr-web-trace`(#500) / `mwr-wo-kpi`(#501) / `mwr-xer-opnotice`(#499)＝merge 判断待ちで不変。`mwr-procurement` は #540 land 後に worktree remove＋branch 削除済）＋ agent worktree 2 本（`.claude/worktrees/brave-varahamihira-ffa2cd`=detached `e7e13bf`＝**本ラウンドの orchestrator レーン**〔#539 branch は掃除済〕 / `.claude/worktrees/epic-borg-0c48ae`=`docs/oq3-footprint-decision`＝据え置き継続〔ローカルのみ〕）＋ scratchpad 一時 worktree 1 本（`docs/status-2026-08-21`=**本 status refresh レーン**）。本ラウンド merged 4 branch（`docs/repin-doc19-git-tag-pins`/`docs/procurement-actuals`/`docs/speed-cap-unfreeze`/`docs/speed-cap-audit-fixes`）の remote+local heads ＋ stale `docs/status-2026-08-18`(#533 squash 済) は orchestrator が §7.3 判定（`gh pr view --json state`=MERGED）で削除済。`git ls-remote --heads origin` = **8 heads = `main` ＋ open PR 対応 6 本（#493/#499-#503）＋ 本 status レーン＝stale ゼロ**。**OPEN PR（`gh pr list` 実査）= 7**（#493/#499-#503 ＋ 本 status PR #544）。
- **worktree 状態（2026-08-18・実査）**: `git worktree list` = main worktree(`main`=`49819ef`) ＋ 開発レーン 6 本（`mwr-doc21`=`docs/doc21-teardown-ruling`(#502) / `mwr-nav2-preflight`=`fix/nav2-bridge-preflight`(#503) / `mwr-sim-docs`=`docs/sim-verification-gate`(#493) / `mwr-web-trace`=`feat/web-trace-deriver`(#500) / `mwr-wo-kpi`=`feat/wo-tier1-kpi`(#501) / `mwr-xer-opnotice`=`feat/xer-operator-notice-node`(#499)＝いずれも merge 判断待ちで稼働中扱い）＋ agent worktree 2 本（`.claude/worktrees/agent-ac544bfe804d27680`=`docs/status-2026-08-18`＝**本 status refresh レーン** / `.claude/worktrees/epic-borg-0c48ae`=`docs/oq3-footprint-decision`(`8258ed6`)＝**#530 に収穫済みの残骸だが未コミット残があるため意図的に据え置き**〔remote head 無し＝ローカルのみ〕）。前回実査で stale だった squash 済 remote heads（前回記載の 5 本を含む計 10 本）とクリーンな agent worktree 3 本は orchestrator が掃除済み。`git ls-remote --heads origin` = **7 heads = `main` ＋ open PR 対応 6 本のみ＝stale ゼロ**（前回 12 heads から解消）。**OPEN PR（`gh pr list` 実査）= 6**（#493/#499/#500/#501/#502/#503・本 status PR 提出で 7）。
- **worktree 状態（2026-08-17・実査）**: `git worktree list` = main worktree(`main`=`5c8e439`) ＋ 開発レーン 6 本（`mwr-doc21`=`docs/doc21-teardown-ruling`(#502) / `mwr-nav2-preflight`=`fix/nav2-bridge-preflight`(#503) / `mwr-sim-docs`=`docs/sim-verification-gate`(#493) / `mwr-web-trace`=`feat/web-trace-deriver`(#500) / `mwr-wo-kpi`=`feat/wo-tier1-kpi`(#501) / `mwr-xer-opnotice`=`feat/xer-operator-notice-node`(#499)）＋ agent worktree（`.claude/worktrees/` 配下 3 本・うち 1 本が**本 status refresh レーン**=`docs/status-2026-08-17`。この agent worktree コピー由来の checker ノイズは #523 で根治済）。**OPEN PR（実査）= 6**（#493/#499/#500/#501/#502/#503・本 status PR 提出で 7）。`git ls-remote --heads origin` = 12 heads = `main` ＋ open PR 対応 6 本 ＋ **stale 5 本**（`chore/ruff-target-py310`(#522) / `docs/skill-html-explainer-tree`(#520) / `feat/contract-camera-link`(#521) / `fix/guardian-displacement-gate`(#524) / `fix/guardian-gate-remove-speed-term`(#525)＝squash-merge 済・§7.3 の `gh pr view --json state` 判定で削除候補）。
- **worktree 状態（2026-07-14〜18・firmware Phase-1 host-first ラウンド・実査/5 PR land 後）**: `git worktree list` = main worktree（別セッションの `docs/open-rmf-traffic-recap`(`1fe0747`) in-flight・触らない）＋ `mwr-sim-docs`(`docs/sim-verification-gate`(`84466e5`)・別セッション in-flight)＋ **本 status refresh レーン**（`mwr-status-r4`=`docs/status-r4`・**本 PR**）。本ラウンドの firmware/safety-state レーン（`mwr-firmware`(#475)/`mwr-fw-decisions`(#476)/`mwr-battery-adr`(#477)/`mwr-fw-fix`(#478)/`mwr-brownout-glossary`(#480)）は **land 後に worktree remove＋local/remote branch 削除済み**。#476 は docs-workflow の revise が未コミットのまま merge され誤引用が残ったため #478 で後追い是正（教訓=merge 前は `gh pr diff` で committed 検証・memory 記録）。2段ネスト敵対レビューの review LEAD は API 落ちから復帰し orchestrator 引き取りで完走。**OPEN PR（実査）= 0**（本 status PR 提出で 1）。`git ls-remote --heads origin` = main + 約 19 本の古い docs/feat/governance 系が残存（20 heads・§7.3 の `gh pr view --json state` 判定で次回 housekeeping 候補）。
- **worktree 状態（2026-07-13 第3ラウンド・実査/8 PR land 後）**: `git worktree list` = main worktree（別セッションの `docs/open-rmf-traffic-recap`(`1fe0747`) in-flight・触らない）＋ **本 status refresh レーン**（`mwr-round-refresh`=`docs/status-2026-07-12`・**本 PR**）。第3ラウンドの 8 レーン（#454-#459/#461/#462）は **land 後に worktree remove＋local/remote branch 削除済み**。中断レーン（`mwr-g5-kit`=`feat/xer-g5-operator-kit`・Fable5 クレジット切れで 0 commit 失敗）も worktree/branch 掃除済み。`.claude/rules/layer-annotation.md`（#462）は **self-modification gate ゆえ apply スクリプト（scratchpad）をユーザーが `! python3` 実行して land**。**OPEN PR（実査）= 0**（本 status PR 提出で 1）。`git ls-remote --heads origin` = main + 約 19 本の古い docs/feat/governance 系が残存（§7.3 の `gh pr view --json state` 判定で次回 housekeeping 候補）。
- **worktree 状態（2026-07-11 第2ラウンド・実査/5 PR land 後）**: `git worktree list` = main worktree（別セッションの `docs/open-rmf-traffic-recap` in-flight・触らない）＋ **本 status refresh レーン**（`mwr-status-refresh`=`docs/status-2026-07-11`・**本 PR**）＋ 並走新レーン 3 本（`mwr-xer-envelope-pluginid`=`feat/xer-envelope-plugin-id` / `mwr-xer-docs-refresh`=`docs/xer-impl-status-refresh` / `mwr-xer-g5-prep`=`feat/xer-g5-offline-replay`・いずれも `8261403` 起点）。第2ラウンドの 5 レーン（#449-#453）は **land 後に worktree remove＋local/remote branch 削除済み**。**OPEN PR（実査）= 0**（本 status PR 提出で 1）。`git ls-remote --heads origin` = main + 19 本の古い docs/feat/governance 系が残存（前回実査から不変・§7.3 の `gh pr view --json state` 判定で次回 housekeeping 候補）。
- **worktree 状態（2026-07-11・実査/4 PR land 後）**: round 掃除完了。`git worktree list` = main worktree(`main`) ＋ **本 status refresh レーン**（`mwr-status-20260711`=`docs/status-20260711`・#448）。本 round の 2 レーン（`mwr-operator-feedback`=feat/operator-feedback・`mwr-quickwins`=docs/296/test/281/feat/282 の 3 branch）は **land 後に worktree remove＋local/remote branch 削除済み**。**OPEN PR（実査）= 0**（本 status PR 提出で 1）。remote heads に古い docs/feat/governance 系 19 本が残存（squash-merge 済み or 停止 spike の可能性・§7.3 の `gh pr view --json state` 判定で次回 housekeeping 候補）。
- **worktree 状態（2026-07-10・実査/4 PR land 後）**: round 掃除完了。`git worktree list` = main worktree(`main`) ＋ status refresh レーン（`mwr-status-20260710`=`docs/status-20260710`・#440）（`mwr-xer-comment-sync` レーンは #441 land 済み・merge 後に掃除）。前回残の 4 レーン（`mwr-l2-docs`/`mwr-policy-freshness`/`mwr-xer6-live-matrix`/`mwr-docs-xer6-findings`）は **land 後に worktree remove＋local/remote branch 削除済み**。**OPEN PR（当時実査）= status PR のみ**（#441 は MERGED）。

### 2026-06-17以前の履歴
- main = `origin/main`(`1937cb6`) 同期・クリーン。**2026-06-27 land（新しい順）: #352(1d0b1e8 [docs] Mode X-ER transport PROBE results + audio-direct probed〔connectivity+plan〕 + Langfuse env fix)/#351(27f6fb4 [mode-x-er] transport + out-of-band STT lane〔ER critical ∥ STT background・fail-open〕+ PROBE-2 measured〔Hermes `input_audio`→HTTP 400 `unsupported_content_type` 実測 2026-06-27〕+ live tests)/#350(7fa0b36 [mode-x-er] XER1/G0: L3 Planning Core handoff〔RawModelOutput→RoboticsPlanDraft・fail-closed L3H-G0/G1 key-gate〕+ L4 ER adapter seam + ErTaskRequest input hygiene)/#347(658e64b [docs] L3 reuse/productization + eval-sdk cross-layer detail)/#335(933291c [docs] Mode X-ER unfrozen-contract resolutions doc06)/#333(9ba0c9b [docs] operator feedback 音声リジェクト応答 spec)/#332(6da2065 [docs] Mode X-ER/VLA L4 diagrams + explainer)/#330(f3c5595 [docs] Mode X-ER-VLA overview HTML)/#329(a9047b3 [docs] L4 box taxonomy 統一)/#328(6275ca1 [docs] html-explainer skill)/#326(f38b607 [docs] ER input modalities/STT necessity/Fusion)/#324(1d992ca [docs] L3 Task Graph DAG cycle semantics)〔+1ec9ede docs: Hermes-first L4 transport clarify〕**。**Mode X-ER XER1/G0 完了 → #344(PROBE-1/2/3 実測) CLOSED・#337(XER1/G0 sub) CLOSED・残 XER2(#336・値側 semantic 検証=L3 Validator/`docs/mode-x-er/02-l3-planning-core.md:78`)**。**2026-06-23 land（新しい順）: #322(07 layer tool decision matrix + 08 navigation/hardware/eval acceptance gate・ROS precheck 追加) / #320(productization governance gate) / #317(11c §3.6 real Open-RMF velocity handling design note) / #316(Mode X-ER / X-ER-VLA / productization 設計提案) / #314(VLA access/runtime spike) / #311(web CI gate) / 2feeb5d(Mode A local negotiation + live launcher) / #310(live layout fixed-height panels) / #309(web/console S3 observability SPA) / #307(warehouse_web_bridge S2 rclpy + FastAPI/WebSocket) / #305(warehouse_web_bridge S1 offline core) / #301(doc08 line ref re-pin) / #302(web track governance) / #299(doc22 web observability design) / #295(前回 STATUS refresh) / #294(eval_sdk canonical registry + B5 guard 緩和) / 2b35def(commander cadence config + time-anchor outage)**。**2026-06-17 land（新しい順）: #290(9650047 [llm-bridge] commander prompts を Langfuse Prompt Management 管理〔code fallback・never-raise fail-open〕＋ `eval_sdk.tracer` additive `extra_tags`/`extra_metadata`〔reserved keys win〕＋自己記述 prompt config + 可読 mode tag + live trace-tag smoke・doc08/13/20 + config `hermes.prompts`, Refs #88/#4)/#293(3538386 [llm-bridge] character-LLM negotiation ROS 配線 + commander hookup=Mode A Slice 2・character_node/negotiation_messages・contract: `Command.start_negotiation` additive・live=#288)/#291(149e46f [docs] env=<v> Langfuse deployment trace tag を taxonomy 追加(doc20 §8/08/13)・code 配線=#292 CLOSED〔2026-06-17・未merge〕)/#276(db60df0 [llm-bridge] offline character-LLM negotiation engine=Mode A core Slice 1)/#286(e466878 [ci] Langfuse SDK API contract guard=`langfuse-api-contract` CI job, Refs #281)/#274(d62b314 [sim] reusable Gazebo+Nav2 sim cockpit=Docker image + run helper)/#278(be8bd2a [llm-bridge] 空文字 command location→None 正規化, Refs #4)/#279(6f6dbda [llm-bridge] dep pins langfuse>=4.9/openai<2/fastapi・uvicorn<1, Refs #88)/#285(26157e6 [docs] AGENTS.md host venv(py3.12) for pytest/ruff)/#273(dd13798 [eval-sdk] domain-free eval core 抽出=doc21 Phase1〔正準 registry 登録は本セッション docs PR=eval_sdk を doc16 §1/§2/§9 + ws/src/README 登録 + B5 guard 全 ws/src/* 緩和〕)/#268(9190530 [latency] collect.py で out/*.json→RESULT §1/§2/§3 導出, Refs #200)/#275(1a521ee [docs] 前回 STATUS refresh=4373c5c boundary)**。**issue 整理（2026-06-17）: #117(R-44 free_fleet eval DoD 充足→**CLOSED**・PoC継続=#180)・#263/#271(古い日次レポート→CLOSED)・状態ラベル16件整備(ready×12 / blocked×2=#266/#284 / #288 blocked→ready)＝open epic 全て ready|blocked を1つ保持**。**2026-06-16 land（新しい順）: #267(4373c5c [docs] #233 collision_monitor: node-isolated ①reflex/④source_timeout = config 層 GO〔実 nav2_collision_monitor 1.3.11 + 実 collision_monitor.yaml〕／②③ full-sim 2台は human gate で PENDING〔Go/No-Go 転記欄 空欄維持〕／Open② 採用候補=forward-biased polygon〔nav-traffic 合意 gate・collision_monitor.yaml 不変〕＋doc16 owner ref :187→:189〔#261 re-pin 追従・stale branch base 由来の誤判定を base merge で解消〕。Refs #233→**#233 OPEN 継続**〔live ②③ + doc12/03/07 採用形反映 残〕・#126 既 CLOSED)/#272(bfbc61f [docs] doc21 eval_sdk extraction proposal 追加=domain-free eval core 抽出設計〔Phase1 実装は #273 OPEN〕)/#270(50f5bfe [docs] deploy/jetson README §4.1 に robot-free/robot-gated ゲート pointer, Refs #127)/#260(a2d0aed [docs] STATUS refresh c08af06 + 2026-06-15 land + worktree 実査)**。**2026-06-15 land（新しい順）: #261(c08af06 [docs] warehouse_rmf_adapter を doc16 §1/§2/§9 + ws/src/README に登録 + `check_package_registry`(B5・ERROR級＝disk 上の warehouse_* pkg が両レジストリ未記載なら fail＝今後の同種 drift を機械防止)追加 + doc16 +2行 shift に伴う `doc16:NNN` 参照を全 repo 再pin, Closes #221)/#262(a7e4b12 [docs] doc-accuracy/hygiene sweep=doc map に setup/jetson 追記・壊れ memory リンク修正・CI 全push現実同期・**08a↔doc12 reconcile**〔Guardian の blocked は recovery 止まりで estop 非格上げ・物理停止は独立の近接/衝突 reflex 責務, doc12:518〕・doc08a:154-161→164-173 行ref 再pin)/#265(1ca32a8 [docs] jetson §4.1 G0-G7 を robot-free/robot-gated 分類=Jetson 単体先着時は G1 メモリを先に回す指針・ゲート/閾値不変, Refs #127)/#264(a9729d5 [docs] langfuse spike RESULT 2026-06-15 live defer + env findings 記録, Refs #88)**。**2026-06-13〜14 land（新しい順）: #257(9b9d479 [de-risk] memory-gate 段階1 live RESULT 転記=**R-38 GO-leaning〔条件付き〕**＋cgroup-peak headroom アーティファクト所見, Refs #187)/#259(0726b3c [de-risk] memory-gate headroom を **working-set〔current−inactive_file=free -h available〕基準**へ修正=cache アーティファクト解消・OOM主判定不変・selftest 16/16・実#187 21サンプル再構成で GO-leaning end-to-end 検証, Refs #187)/#256(3e6c8d8 [nav-traffic] Mode C Fleet Adapter offline core=routing/namespacing/single-writer・rclpy/rmf_* 非import・契約無編集・APPROVE 0-blocking/0-major, Refs #180)/#250(7372f55 [llm-bridge] MCP tool6/7 escalation_response/start_negotiation 直接 dispatch unit + already_resolved 再拒否=additive・凍結契約不触, Refs #4)/#254(87356d7 [docs] STATUS refresh 774ef64 + 2026-06-12〜13 land block + worktree state)**。**2026-06-12〜13 land（新しい順）: #252(774ef64 [wo] offline KPI 算術コアに property-style 不変条件テスト追補=fixed-seed 決定的・`hypothesis` 非依存・mutation 9/9 caught, Refs #88)/#251(117d299 [ci/docs] firmware-safety job に kinematics+host-compile gate 2 step 追加 + doc20 §4 CI job 一覧整合=net-zero 行編集, Refs #242)/#249(eb08899 [firmware] pio native env に test_kinematics 取込=R-26 gate(`run_host_test.sh`)不触, Refs #3)/#248(3dd52d3 [wo] date-stamped Grok 価格表 additive(grok-4.3 $1.25/$2.50) + #88 Phase-3 live checklist 集約, Refs #88)/#246(3dbc32f [firmware] micro-ROS Phase-1 skeleton 肉付け=motor/sensor stubs+kinematics(skid-steer 逆運動学)+PHASE1_CHECKLIST+host-compile gate, Refs #3)/#247(bdc7bb2 [llm-bridge] Mode C stop no-actuation R-26 pin=navigate|stop|charge 完備, Refs #4)/#244(f4710cd [ci/firmware] Layer-0 clamp R-26 host unit を CI ゲート化=5本目 job firmware-safety, Refs #242)/#245(2532eba [llm-bridge] deadlock pattern-3 approach-precursor unit + offline-hardening 裁定記録, Refs #4)/#238(186a2ce [nav-traffic] #126 collision_monitor docs: Open⑤(surfacing defer)/⑥(recovery bypass)確定 + ① defer + 2-bot PoC runbook → **#126 CLOSED**・live PoC/Open②③④ は #233 OPEN 継続)/#237(4e41422 [de-risk] memory-gate operator GO sheet + RESULT 転記 pointer=#187 実走 human gate 準備, Refs #187)/#239(3f453f6 [firmware] Layer-0 clamp 非有限(NaN/±Inf) cmd_vel→stop fail-safe + unity shim 偽GREEN 修正 + host R-26 unit, **Closes #235**)/#241(9d10904 [docs] status-maintenance rule + `scripts/refresh_status.py` helper, Refs #240)/#240(3f9098b [docs] STATUS refresh 387e24e + 2026-06-11 land block)**。**2026-06-11 land（新しい順）: #236(387e24e [docs] doc12a に座標ゴール navigate variant/`INVALID_GOAL` を EOF 追補・#165 回避, Refs #223)/#234(1a28903 [wo] spike/langfuse Phase-3 verify harness, Refs #88)/#219(c17717b [nav-traffic] #180 GATE-前 warehouse_rmf_adapter design scaffold＝**R-38/#187 で BLOCKED**)/#230(0fdaf0e [nav-traffic/llm-bridge] 座標ゴール navigate `goal=(x,y[,yaw])`[XOR・yaw drop・`INVALID_GOAL`] + `HeadOnInjector` route_A 排他直列化, Refs #223/#156)/#231(7cfbef8 [firmware] Layer-0 速度クランプ ≤0.3m/s を host R-26 unit に pin, Refs #3)/#232(bf08384 [docs] STATUS refresh + #229 launch ref re-pin)/#229(bce4853 [nav-traffic] #126 nav2_collision_monitor を Nav2 速度経路に配線=twist_mux prio10 上流・emergency prio100 不変/R-26・controller remap を mode-conditional[A/B=cmd_vel/nav2_raw→monitor→cmd_vel/nav2 / Mode C=直行]・専用 gated lifecycle_manager・**virtual_scan per-source source_timeout 0.0**[>1.0m 無送信の stale-stop 誤発火回避=敵対 review MAJOR 修正・df397e3]。`Refs #126`=wiring のみ・PoC live + Open ②③⑤⑥ は人間 Docker ゲート→follow-up)**。**2026-06-10 land（新しい順）: #218(17b3f8f #156 slice3-live: runbook fix + SCENARIO fail-hard seed + host 24 tests)/#222(a0ce17a warehouse_rmf_adapter を CI cross-import map 登録)/#217(memory-gate doc-fix + verdict FLOOR)/#215(#175 config._validate_safety 非数値型ガード)/#214(post-197 slice3 handoff・05番号gap)/#211(latency fail-closed spend-guards)/#210(memory-gate Hermes liveness)/#206(base prompt battery を doc08a 3段へ reconcile)/#205(parallel-discipline + completion-contract template)/#204(cac5db6 #156 slice3 scenario/rviz_config sim-forward)/#203(Mode C prompt 忠実化 doc08c)/#202(latency verdict branches)/#201(memory-gate Hermes PATH)/#199(#177 session-orchestration §refs pin)/#191(STATUS refresh)**。**2026-06-08 land（新しい順）: #197(slice3 sim startup seeding docs)/#195(Jetson preflight runbook checks)/#196(slice3 live precheck helper)/#194(malformed LLM response cycle ignore + STATUS refresh)**。**2026-06-06〜07 land（新しい順）: #190(Codex context layer=MERGED 06-07)/#184(#156 slice2 commander-cycle wiring harness)/#189(nav-traffic nav2_params safety-pin)/#185(sim head-on spawn preset)/#188(release-path: sim 先行リリース正本化 docs)/#183(llm-bridge #181 Mode-A deadlock prompt+task injection)/#182(R-44 docs follow-up)/#179(doc12/11c cross-ref re-pin)**。**2026-06-05 land（新しい順）: #157(#126 collision_monitor cmd_vel 挿入トポロジ docs)/#154(create-issue skill)/#153(#125 yield ≥0.15m live=CLOSE)/#152(#126 pose-freshness)/#151+#150(#103 fairness guard=CLOSE)/#149(11a §9 yield topology)/#144+#143(#125 traversal/#126 edge-trigger)/#141(#124 真200mm隘路=CLOSE)/#140(.claude Langfuse)/#138-139(#133 wo Grok=CLOSE/#125 vx_max)/#134-136(STATUS整合/docs sweep)**。**2026-06-04 land（新しい順）: #132(L2 #116 R-37 host spike)/#129(L4 #88 比較指標凍結)/#130(L1 nav2_params・11a doc-drift)/#128(L3 #55 deadlock 再基礎付け)**。新規 issue: #124(sim 隘路)/#125(nav Phase2 margin・vx_max config)/#126(R-39 collision_monitor)/#127(jetson fidelity)/#131(#55 波及同期)/#133(wo Grok cost code)。直近 land（新しい順）: #120(STATUS鮮度) / #121(#44 battery scale 単一正規化) / #119(nav2 MPPI footprint #67) / #118(sim odom→base_link TF, closes #112) / #115(短期記憶#102 + #108/#109 cross-lane Langfuse join) / #113(nav #67 E2E gate) / #111(Phase4 公平性 memory/skills OFF docs) / #114(CTRV 横展開 + STATUS 鮮度) / #110(CTRV 実装, closes #101) / #107(doc03 内部トピック表網羅, closes #61) / #106(jetson safety doc Requires→BindsTo) / #105(STATUS 鮮度) / #100(08a CTRV 改訂) / #104(#4 S2-PR2 HALF B 実 dispatch+nav2_bridge REST 転送) / #98(doc12 battery `<=`) / #97(doc09 §7 sim/SLAM 二系統) / #94(sim `/clock`+占有マップ, critical-path) / #96(jetson deploy scaffold) / #92(wo node-wiring) / #93(bringup 合成) / #91(docs-first 実装ゲート) / #90(battery `<=10%`, closes #59)。
- **issue 整理（2026-06-03）**: #73(Langfuse trace 所有=完了 → Phase3 は **#88**) / audit umbrella **#37**(根本=#41確定) / #59(battery `<=10%`、#90 で close) を close。日次レポート #53/#62/#63/#84 を close。残 open の整合 issue は **#44/#55**（#61→#107・#102/#108/#109→#115・#112→#118・**#54→明示 /stop 撤回**(2026-06-05) で close 済）（+ #88 Phase3 Langfuse 検証 / #67 2台 E2E）。
- **worktree 状態（2026-06-27・実査）**: `git worktree list` = `main`worktree(`docs/mode-x-er-unfrozen-resolutions` 作業中) / `mwr-mode-x-er`(`docs/status-0627`・**本 PR**) ＋ docs worktree 多数（`mwr-docs-boxmap`/`mwr-docs-er-input`/`mwr-eval-sdk-detail`/`mwr-hermes-boundary`/`mwr-l3-detail`/`mwr-l4-detail`/`mwr-modex-net`/`mwr-operator-feedback`/`mwr-skill-patterns`/`mwr-code-emergency`）＋ `/private/tmp/mwr-pr*` review 系（cleanup 候補）。`git ls-remote --heads origin` = `main` + active branches（`docs/mode-x-er-diagrams`/`docs/mode-x-er-vla-html`/`docs/operator-feedback-voice`/`docs/er-input-modalities`/`docs/l3-detail-nested`/`docs/hermes-first-modex`/`docs/productization-box-map-layers`/`docs/productization-governance-gate`/`docs/web-observability`/`docs/pr308-mode-a-memory`/`feat/mode-x-er`/`feat/langfuse-env-tag`/`feat/llm-bridge-prompt-langfuse`/`chore/langfuse-api-guard`/`governance/web-track`）。**Mode X-ER XER1/G0 の `feat/mode-x-er-core`・`feat/mode-x-er-transport`・`docs/mode-x-er-transport` は #350/#351/#352 land で削除済**。**OPEN PR（実査）= 0**（#350/#351/#352 MERGED 後・本 status PR 提出で 1）。**継続 OPEN issue**: #336 Mode X-ER（残 XER2-6）/ #4 llm-bridge / #228/#288/#289 Mode C・character live / #88 wo Langfuse Phase3 / #233 nav-traffic live PoC / #3・#116 firmware(実機待ち) / #180・#187(R-38 Jetson/live=人手ゲート) / #266 jetson / #8 nav-traffic / #44 safety-state / #269 eval_sdk。**本セッション CLOSED: #344(PROBE-1/2/3 実測)・#337(XER1/G0 sub)**。

## Mode X-ER 設計状況（2026-06-27・XER1/G0 land 済）
- **XER1/G0 land 済（2026-06-27・#350/#351/#352）・XER2-7 / G1-G6 は未着手**: `ws/src/warehouse_llm_bridge/warehouse_llm_bridge/robotics_planning_core/`（`RawModelOutput`→`RoboticsPlanDraft` の L3 Handoff・fail-closed **L3H-G0/G1 key-gate**＝unknown_schema_version / forbidden endpoint・low-level action・coordinate-goal key を `model_validate` 前に reject）＋ `robotics/`（`ErTaskRequest` input hygiene・`GeminiErAdapter` offline seam〔live transport は #344 系後〕・transport 選択・**out-of-band STT lane**〔ER critical ∥ STT background・fail-open〕・Langfuse tracer skeleton）が実装済。**値側 semantic 検証（target ∈ `detections[].id` | known location 等）は XER2 L3 Validator の責務**（`docs/mode-x-er/02-l3-planning-core.md:78`）で、L3H は KEY 名構造 gate に scope。`ValidationReport`/`VisualResolver`/`TaskGraphExecutor`/`CommandCompiler`/`x_lite`/`x_rmf`/`calibration_id` は未実装。安全境界（MCP/Policy Gate/Emergency/Layer0）不変・no-actuation・`warehouse_interfaces` 不触（bridge-local）。再利用する Mode A/B/C 汎用 L4 配線（`scheduler.py`/`tracing.py`/`situation.py`/`action_map.py`/`executor.py`/`hermes_client.py`/`schemas.py`）は従来どおり。**PROBE-1/2/3 実測完了（#344 CLOSED・PROBE-2 = Hermes `input_audio`→400）**。
- **未凍結7ブロッカーの解決方針を [docs/mode-x-er/06-unfrozen-contract-resolutions.md](mode-x-er/06-unfrozen-contract-resolutions.md) に整理**（FREEZE-NOW / DEFER / NEEDS-PROBE 判定 + 提案形 + contract PR 手順）。要旨: #4 = FREEZE-NOW(MVP=known location 限定・additive ゼロ) ＋ DEFER(coordinate goal 契約) / #5 = NEEDS-PROBE(ER 1.6 audio=公式 YES・Hermes は image 対応/`input_audio` 400 公算→audio は direct 一次・PROBE-1/2/3) / #1#2#3#6#7 = DEFER(gate)。**いま `warehouse_interfaces`/config/topic に凍結すべき項目は実質ゼロ**。`#2` enum は `warehouse_interfaces` に入れず bridge-local(observation-only) / `#7` は **RESOLVED（PR #446・2026-07-11 MERGED）**＝`/operator/notice` 契約凍結（`std_msgs/String` JSON・`operator_notice.v0`・RELIABLE/KEEP_LAST 10・emergency は `/emergency/event` 相乗り＝wire guard で二重 publish 封鎖・v0=reject 級のみ。確定値の正本=doc05 §8.10・doc06 §7。契約予告 4 通投稿済=#5/#8/#6/#298）。
- 機械ゲート: `python3 scripts/check_consistency.py` = **0 ERROR / 0 WARNING**（本 refresh で SHA pin（5箇所）を `1d0b1e8` に再同期・doc04 README:41→:44 行ドリフトは #352 で解消）。
