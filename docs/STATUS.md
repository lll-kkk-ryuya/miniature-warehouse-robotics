# プロジェクト現況（STATUS）

最終更新: 2026-06-23

> 本書は**現況スナップショット**（随時更新）。詳細の正本は各 doc / `.claude/rules/`、リスクは [07-research-notes](shared/07-research-notes.md)。docs 中心主義は [rules/docs-first.md](../.claude/rules/docs-first.md)。

## サマリ
実機未到着のため**ソフト・基盤・ドキュメントを先行整備**中（doc06 方針）。
**doc17 Step 0（リポジトリ骨格＋契約凍結）完了**（2026-05-29, Issue #1 closed）。
**Step 1＝各トラックの並列実装フェーズが進行中**。#4 llm-bridge S1 / #8 nav-traffic / #6 wo / #5 safety-state / #7 sim / #25 gen_id 冪等化 / Mode C 契約(#66) を並列実装し、**Phase 0.5 スライスが main に land 済**（下表）。**S2-PR1 も land 済**: Bridge-owned Langfuse trace(#78) / wo v4 `create_score`(#83) / nav2_bridge REST→BasicNavigator part1(#86)。**Langfuse trace 所有 #73 は完了 → close、Phase3 実トレース検証は #88 に分離**。次ラウンドは4並列: #4 S2-PR2（実 dispatch + MCP→nav2_bridge REST）/ #76 sim `/clock`+占有マップ（critical-path, #67 解錠）/ #6 wo node-wiring / #75 bringup 合成 + #2 jetson。
**【現況 2026-06-03 更新】**上記のうち **#76 sim `/clock`+占有マップ(#94, critical-path) / #6 wo node-wiring(#92) / #75 bringup 合成(#93)** は **3 本ともマージ済**、**#2 jetson deploy scaffold(#96) もマージ済**。**#4 S2-PR2 HALF B（実 dispatch + MCP→nav2_bridge REST）=#104 / docs CTRV(08a)=#100 もマージ済**。派生 issue **#101(CTRV実装・#100 merge で解錠)/#102(history充填+current_task)/#103(Hermes Memory)** を起票。その後 **#105(STATUS鮮度)/#106(jetson safety doc)/#107(doc03 内部トピック表網羅, closes #61)/#110(CTRV 実装, closes #101)/#111(Phase4 比較公平性＝memory/skills 強制 OFF の運用契約を docs 化。#103 本体は未実装 OPEN)/#113(nav-traffic #67 E2E gate 修正)** もマージ済 → さらに **#115（#102 短期記憶 + #108/#109 cross-lane Langfuse join 修正、closes #102/#108/#109）/ #118（sim odom→base_link TF, closes #112）/ #119（nav2 MPPI footprint, #67 E2E）/ #121（#44 battery scale 単一正規化・contract additive）/ #120（STATUS 鮮度）** も land → **main=`5814f08`、open PR 0**。**【2026-06-04 並列ラウンド land】**4レーン: **#128**(L3 #55 deadlock を `status=="blocked"` 依存→凍結 RobotState 信号へ再基礎付け＝option b)/**#130**(L1 #67 follow-up nav2_params・11a doc-drift)/**#129**(L4 #88 subset Phase3-4 比較指標 docs 凍結)/**#132**(L2 #116 R-37 host spike＝根本原因 client_key 衝突を再現＋distinct key で単一 Agent 2台双方向 OK 実証＝**R-37 de-risk**)。**#67 は close**（コア 2台 E2E 達成・Phase2 残=#125 margin/#124 隘路）。新 follow-up: #124/#125/#126(R-39)/#127(jetson fidelity)/#131(#55 波及同期)/#133(wo Grok cost code)。稼働は H（#103）。R-37/R-44 tracking=#116(✅#132)/#117。#91 で **docs-first 実装ゲート**（[parallel-workflow.md §1.1](../.claude/rules/parallel-workflow.md)：着手前 file:line 引用 → 実装中 CLAUDE.md 記録 → 完了前 `check_consistency.py` 0ERROR + `/consistency-audit` + 未決列挙）が**完了条件として必須化**された。

## main の状態（CI 常時 green, origin/main = `261a6d8`）
- **契約ハブ `warehouse_interfaces`（凍結）**: pydantic `Situation`/`Command`/`Proposal`(+ `gen_id`、`CommandItem.idempotency_key`)、`StateSnapshot`/`RobotSnapshot`、`KNOWN_LOCATIONS` 9キー、共有パス（doc16 §4）、`StateStore`/`GenStore`/`IdempotencyStore` IF + file 実装、`safety`（速度/battery しきい値の単一ソース）、`config`。
- **実装済みノード（main 入り）**:
  - `warehouse_mcp_server` — 7ツール + Policy Gate + gen_id B-3 + **per-call idempotency 強制**（#35/#41。偽 store でユニット検証、MCP SDK は遅延 import の pip extra）。
  - `warehouse_state` / `warehouse_safety` — State Cache 100ms（`StateSnapshot` 形で `state.json` を書く producer）+ Emergency Guardian 50ms reflex（#39。偽入力でユニット検証）。
  - `warehouse_sim` / `warehouse_description` — 1.8×0.9 world 単一定数生成 + minicar URDF（凍結フレーム `bot{n}/base_link→{lidar_link,imu_link}`）+ `sim.launch.py`（bot1/bot2 spawn）（#43）。`/clock`/`sim_time`/占有マップは #94 で main 入り、2台 Gazebo Nav2 E2E のコアゲート #67 は close 済み。
  - `warehouse_llm_bridge` — `llm_client`(ABC) / `action_map`（Command→ToolCall 変換、`gen_id` は注入・**per-call UUID `idempotency_key` を mint**）（#27/#41）。
- **S1/scaffold が main 入り（#68/#69/#70）**: `warehouse_llm_bridge`（`scheduler`/`hermes_client`/`situation`/`executor`/`llm_bridge` = 司令官サイクル S1、tool dispatch は log stub＝**#104 で実 dispatch に置換済**）、`warehouse_traffic`（TrafficManager None/Simple + VirtualScanNode `/bot{n}/virtual_scan`）、`warehouse_orchestrator`（audit reader + result/task_completion_time KPI + Langfuse seam NO-OP）。
- **`warehouse_nav2_bridge` part1 main 入り（#86）**: REST→BasicNavigator パッケージ（`core`/`backend`/`errors`/`app`/`nav2_bridge`、23 unit ケース）。**MCP→nav2_bridge REST 配線は #104（S2-PR2 HALF B）で完了**。
- **残り `main()` スタブ / 未実装**: なし（`warehouse_teleop` は #158 で keyboard teleop 実装済み）。`warehouse_bringup` の per-bot launch 合成は **#75=#93 で main 入り**。
- **firmware 雛形**（ESP32 micro-ROS、**Layer-0 速度クランプ ≤0.3 m/s**、#23）。実機実装は Phase 1。
- **開発基盤**: Ruff + pytest（安全契約テスト, R-26）+ pre-commit + GitHub Actions CI + governance CI / main-worktree edit guard / docs-first ルール（#45）+ Playwright 雛形（Phase 4 までゲート）。

## 開発体制（正本: parallel-workflow.md / merge-and-communication.md / implementation-and-dependencies.md / docs-first.md / doc17）
- **トランクベース**: 全コードは PR 経由で `main` へ（squash）。**マージ後はブランチ削除**（stale 防止）。**直 main 編集/push 禁止・同一ターン self-merge 禁止**（#31）。
- **docs 中心主義**: docs を正本とし、コードは検証・具現側。docs に無い契約/トピック/スキーマ/しきい値を発明しない（#45, rules/docs-first.md）。**完了条件＝docs-first 実装ゲート**（#91, parallel-workflow.md §1.1）: 完了前に docs 再照合 → `check_consistency.py` 0ERROR → `/consistency-audit` → 残る未決を PR 本文に列挙、を満たして初めて「完了」。
- **dev/stg/prod は実行環境**（`config/<env>` + `WAREHOUSE_ENV`）。Git ブランチではない。昇格はデプロイ。
- **並列 git worktree**。連絡は GitHub Issue/PR に **`[worktree | branch | track]` タグ**必須。
- **依存の扱い**: 各トラックは凍結契約 `warehouse_interfaces` のみに依存し、他トラック内部を import しない。新依存は契約PR(`contract` ラベル)+予告+凍結してから実装。

## トラック状況（epic Issue #1〜#8 + #25）
| # | track | 状態 |
|---|-------|------|
| 1 | skeleton | ✅ **CLOSED**（契約凍結 #22 + 12pkg #24） |
| 2 | jetson | ready（実機不要で着手可） |
| 3 | firmware | 雛形 main 入り（#23）。実機実装は Phase 1 |
| 4 | llm-bridge | 🟢 **S1 + S2-PR1 + S2-PR2 HALF B main 入り（#70/#78/#86/#104）**: 司令官サイクル(Scheduler+HermesClient+Situation+action_map+排他 A/B-3+C) / **Bridge-owned Langfuse trace `tracing.py`(#78)** / **nav2_bridge part1(#86)** / **実 in-process tool dispatch（`WarehouseTools().dispatch` 注入・同一トラック #81）+ MCP→nav2_bridge REST 転送（`nav2_client`・受理時のみ・R-26）(#104)**。MCP 層(#35/#41) 済。**残**: Mode C 忠実化 / Open-RMF 転送経路 / Phase4 起動ハーネスでの実 Hermes memory/skills OFF 配線。**✅完了: 短期記憶 history+current_task=#102(#115) / 予測 CTRV 化=#101(#110) / cross-lane Langfuse live-join=#108・#109(#115) / R-35A=#54(明示 /stop 撤回・Layer A client-side のみ・主担保 B-3+C) / #103 fairness guard(intent-only) / #181 Mode-A prompt+task seed / #192 malformed response cycle ignore**。Langfuse Phase3 検証は #88 |
| 5 | safety-state | 🟢 **実装 main 入り（#39）**: Emergency Guardian 50ms + State Cache 100ms（`StateSnapshot` producer, atomic `state.json`）。残: 実機統合・twist_mux 連携（Phase 2+） |
| 6 | wo | 🟢 **KPI scaffold + Langfuse v4 配線 main 入り（#69/#83）**: audit.jsonl reader + result/task_completion_time + **v4 `create_score` 実配線 + trace_id 導出 + efficiency(#83)**。**node-wiring（provider+gen_id metadata + collector test）= #92 main 入り**（`score_send.py` pure helper 抽出 + `provider` param[`WAREHOUSE_PROVIDER` env] + score metadata に provider/gen_id 追加 + blank-run_id ゲート修正）。**残**: 他 score 名（docs-PR 前提）/ Grok cost(=#88) / Metrics+Experiments。trace_id は seed 導出で契約不要 |
| 7 | sim | 🟢 **環境スパイク GO + 実装 + `/clock`/`sim_time`/占有マップ main 入り（#43/#46/#94）**: world/URDF/`sim.launch.py` + `/clock` bridge + use_sim_time + 占有マップ生成（#76=#94, critical-path 解錠済）。2台 Gazebo Nav2 E2E コアゲート #67 は close 済み。 |
| 8 | nav-traffic | 🟢 **TrafficManager/VirtualScan/nav2_params(#68) + E2E enablement(#82) main 入り**: None/Simple + `/bot{n}/virtual_scan` + DWB→MPPI + twist_mux 移設(#40) + AMCL initialpose seeding + container nav2 provisioning(#82)。2台 Gazebo E2E ゲート **#67 は close 済み**。残は Phase2/実機寄りの collision_monitor 配線・nav_status gating など（#126 系）と、live provider 比較 run の確認。`bringup.launch.py` per-bot 合成 **#75 = #93 main 入り**。 |
| 25 | gen_id UUID冪等化 | ✅ **CLOSED/merged**: 契約(#36 `IdempotencyStore`/`FileIdempotencyStore` + `CommandItem.idempotency_key` + `idempotency_store_path()`) + enforcement(#41) が main 入り。`action_map` が tool-call 毎に per-call UUID を mint（LLM は echo しない＝信頼の非対称性）、`GenChecker.check` が gen→idempotency を強制（replay=`duplicate_command`）、MCP 7ツール全てに `idempotency_key`（per-call UUID）貫通 |

## ⚠️ 要決着事項（latent / 監視）
- **（解決済・参考）R-35(A)** は **Issue #54 で解決（2026-06-05）**＝明示 `POST /v1/runs/{id}/stop` を撤回。採用トランスポートは同期 `chat/completions`（ステートレス・run_id 無し）かつ tool dispatch は Bridge 仲介の in-process（doc08 採用実装📌・#104）でサーバー側 tool 実行が無いため、止めるべき run／tool 実行が存在しない。Layer A はクライアント側キャンセル（`asyncio.wait_for`）のみ、安全主担保は B-3（古い世代 reject）+ C（冪等キー）。#41 は R-35(B) 冪等化を解決済（同一世代 replay を `duplicate_command` で拒否）。unit: `test_stale_call_rejected_when_stop_noop_54` / `test_end_to_end_stale_generation_rejected` / `test_older_generation_rejected`（R-26）。
- **（解決済・参考）実行モデル**（Bridge mint vs LLM echo）の未確定は #41 で確定済: **`action_map` が per-call UUID を mint・LLM は触らない**（信頼の非対称性。doc08 §C / doc15 §競合状態の防止）。
- **（決定・実装済）Langfuse trace 所有 + provider access**（#72 docs / #78 Bridge seam / #83 wo wiring。**#73 は close、Phase3 実トレース検証は #88**）: trace 所有は **Bridge**（`langfuse.openai` + `base_url`=Hermes、Hermes 内蔵 Langfuse は無効化）。score は v4 `create_score`（v2 `langfuse.score` 廃止）。`trace_id`=32hex no-dash、#4↔#6 は `create_trace_id(seed)` 導出で契約不要、audit 突合は `gen_id`+timestamp。**live join は従来 no-op だった**（#108: Bridge が `WAREHOUSE_RUN_ID` 未読＝#4↔#6 trace_id 不一致 / #109: MCP executed audit 行に `gen_id` 欠落＝WO score join 不成立）が **#115 で修正・MERGED（#108/#109 close）＝Phase3/4 比較検証の前提が成立済**（`resolve_run_id`=WAREHOUSE_RUN_ID 起点 + executed 行 `detail.gen_id`、cross-leg test 済）。**Vertex AI SDK は不採用・Hermes 単一経路維持**（doc13 §7.5/§7.6）。

## 次の山（kickoff brief: `~/Developer/mwr-handoff/round-2026-06-04/`）
**直近マージ済（2026-06-03）**: #94 sim `/clock`+`sim_time`+占有マップ（critical-path 解錠） / #92 wo node-wiring / #93 bringup.launch.py 合成 / #96 jetson deploy scaffold / **#104 #4 S2-PR2 HALF B（実 dispatch+MCP→nav2_bridge REST 転送）** / **#100 docs CTRV（08a linear→CTRV）** / **#105 STATUS 鮮度** / **#106 jetson safety doc（Requires→BindsTo）** / **#107 doc03 内部トピック表網羅（closes #61）** / **#110 CTRV 実装（closes #101）** ＋ **#111(Phase4公平性docs)/#113(nav #67 E2E gate)/#114(CTRV横展開)/#115(短期記憶#102+#108/#109 cross-lane join)/#118(sim odom→base_link TF, closes #112)/#119(nav2 MPPI footprint #67)/#121(#44 battery scale 単一正規化)/#120(STATUS鮮度)** も land 済（→ `83eec5b`）。**2026-06-04 並列ラウンド: #128(L3 deadlock)/#130(L1 doc-drift)/#129(L4 比較指標凍結)/#132(L2 R-37 spike) land → 当時 main=`5814f08`**。 **【2026-06-05 round2/3 land】** #134-136/#138-144/#149-154/#157 land（#103 Hermes Memory fairness guard / #124 真200mm隘路 / #125 yield ≥0.15m を **live 0.2317m** で実証 / #126 edge-trigger+pose-freshness+collision_monitor cmd_vel 挿入トポロジ docs(#157) / #131 deadlock 波及同期 / #133 wo Grok cost / #140 .claude Langfuse / #154 create-issue skill）→ **当時 main=`7185955`**。**CLOSED: #103/#124/#125**。**OPEN: #126**（edge-trigger+pose-freshness+cmd_vel 挿入トポロジ docs 確定（#157, doc12:522 §collision_monitor 委譲）land・collision_monitor 配線 impl は nav-traffic hand-off で defer・nav_status gating Phase2）。**LLM-in-Gazebo 統合 capstone の部品が全て揃った**（Nav2 2台E2E#67 ＋ 真隘路#124 ＋ yield ≥0.15m 機械実証#153 ＋ Hermes Memory fairness#103）＝次の大物。

**残り（着手待ち / 実測待ち）**:
- **#156 / slice3 live** — **全 slice land 済（origin/main=`261a6d8`）**: slice1 統合 launch 合成（#162）＋ slice2 commander-cycle wiring（#181 task injection + Mode-A prompt / #192 malformed cycle ignore、両 CLOSED）＋ **slice3-live = #218 MERGED（2026-06-10）**: SCENARIO 整合 seed（`head_on`=sim `head_on_spawn_poses` 導出・導出失敗時は **fail-hard exit 2** で berth 誤 seed を拒否）＋ runbook demo-breaker 2件修正（Nav2 Bridge :8645 は in-process 合成＝外部起動禁止 / ≥0.15m は当時 座標ゴール injector 未実装）＋ host 24 tests。**PENDING（人間/secret ゲート・live のみ）**: 実 Hermes/4-provider の yield 判断＋RViz/noVNC mp4 録画＋L6 サイクル長。**≥0.15m 座標ゴール経路は実装・文書化済（DEFERRED 解消）**: #230 MERGED（`Nav2BridgeCore.navigate` に additive 座標 `goal=(x,y[,yaw])`＝XOR・yaw drop・`INVALID_GOAL`、`HeadOnInjector` が route_A 排他で head-on swap を直列化）＋ #236 MERGED（doc12a に座標ゴール variant/`INVALID_GOAL` を EOF 追補）。**残るゲートは LIVE ≥0.15m 計測のみ**（user-docker・`WAREHOUSE_MINSEP_STREAMS` でゲート＝Gazebo 物理が要る）。**#223 / #156 は OPEN 継続**（live 録画/計測＝人間ゲート）。
- **#193 jetson** — deploy scaffold #96 main 入り。G0-G7 runbook + `deploy/jetson/bin/preflight.sh` を整備済み（到着前 `--offline` / 到着後 `--arrival --gates G0,G1,G7`）。実測値は Jetson 到着後に記録する（#2 本体は close 済み）。
- 派生 issue: **#101**(CTRV ✅#110) / **#102**(history充填+current_task ✅#115) / **#103**(fairness guard intent-only ✅。実 Hermes memory/skills OFF 配線は Phase4 起動ハーネス責務) / **#192**(malformed response cycle ignore ✅本修正)。
- **latent 整合（✅解消済）**: #108/#109（Langfuse live-join 二重故障）は **#115 で修正・close**（`resolve_run_id`=WAREHOUSE_RUN_ID 起点 + executed 行 `gen_id`、cross-leg test 済）＝Phase3/4 比較検証の前提が成立。

- マージ順（doc17 §6）: skeleton(#75/#93) → 独立トラック(#92 wo / #4) 随時 → **sim(#76/#94, critical-path)** → nav-traffic(#67 E2E) → 統合。
- 既知の設計リスクは [07-research-notes](shared/07-research-notes.md) R-35〜R-52。

## git 衛生（現況, 2026-06-23）

### 2026-06-23 current
- main ref = `origin/main`(`261a6d8`) 同期。STATUS の SHA pin は `scripts/refresh_status.py --check` で fresh。
- **2026-06-23 land（2026-06-17 refresh 以降・新しい順）**: #322(07 layer tool decision matrix + 08 navigation/hardware/eval acceptance gate・ROS precheck 追加) / #320(productization governance gate) / #317(11c §3.6 real Open-RMF velocity handling design note) / #316(Mode X-ER / X-ER-VLA / productization 設計提案) / #314(VLA access/runtime spike) / #311(web CI gate) / 2feeb5d(Mode A local negotiation + live launcher) / #310(live layout fixed-height panels) / #309(web/console S3 observability SPA) / #307(warehouse_web_bridge S2 rclpy + FastAPI/WebSocket) / #305(warehouse_web_bridge S1 offline core) / #301(doc08 line ref re-pin) / #302(web track governance) / #299(doc22 web observability design) / #295(前回 STATUS refresh) / #294(eval_sdk canonical registry + B5 guard 緩和) / 2b35def(commander cadence config + time-anchor outage)。
- **worktree 状態（2026-06-23・実査）**: `git worktree list` = main worktree(`docs/status-refresh-2026-06-23`, base `261a6d8`) / `/private/tmp/mwr-gemini-er-spike`(`spike/gemini-robotics-er`) / `mwr-code-emergency`(`fix/situation-emergency-slice3-precheck`) / `mwr-docs-boxmap`(`docs/productization-box-map-layers`) / `mwr-docs-er-input`(`docs/er-input-modalities`) / `mwr-hermes-boundary`(`docs/hermes-boundary-modex`)。`git ls-remote --heads origin` = `main` + active/stale branches（`docs/l3-task-graph-dag-semantics`, `docs/hermes-boundary-modex`, `fix/situation-emergency-slice3-precheck`, `docs/productization-box-map-layers`, `docs/productization-box-taxonomy` ほか）。**OPEN PR（実査）**: #324(L3 Task Graph DAG semantics) / #323(Hermes 利用境界) / #321(situation emergency forward + slice3 precheck ROS source) / #319(productization box-map layer 明示)。

### 2026-06-17以前の履歴
- main = `origin/main`(`261a6d8`) 同期・クリーン。**2026-06-17 land（新しい順）: #290(9650047 [llm-bridge] commander prompts を Langfuse Prompt Management 管理〔code fallback・never-raise fail-open〕＋ `eval_sdk.tracer` additive `extra_tags`/`extra_metadata`〔reserved keys win〕＋自己記述 prompt config + 可読 mode tag + live trace-tag smoke・doc08/13/20 + config `hermes.prompts`, Refs #88/#4)/#293(3538386 [llm-bridge] character-LLM negotiation ROS 配線 + commander hookup=Mode A Slice 2・character_node/negotiation_messages・contract: `Command.start_negotiation` additive・live=#288)/#291(149e46f [docs] env=<v> Langfuse deployment trace tag を taxonomy 追加(doc20 §8/08/13)・code 配線=#292 CLOSED〔2026-06-17・未merge〕)/#276(db60df0 [llm-bridge] offline character-LLM negotiation engine=Mode A core Slice 1)/#286(e466878 [ci] Langfuse SDK API contract guard=`langfuse-api-contract` CI job, Refs #281)/#274(d62b314 [sim] reusable Gazebo+Nav2 sim cockpit=Docker image + run helper)/#278(be8bd2a [llm-bridge] 空文字 command location→None 正規化, Refs #4)/#279(6f6dbda [llm-bridge] dep pins langfuse>=4.9/openai<2/fastapi・uvicorn<1, Refs #88)/#285(26157e6 [docs] AGENTS.md host venv(py3.12) for pytest/ruff)/#273(dd13798 [eval-sdk] domain-free eval core 抽出=doc21 Phase1〔正準 registry 登録は本セッション docs PR=eval_sdk を doc16 §1/§2/§9 + ws/src/README 登録 + B5 guard 全 ws/src/* 緩和〕)/#268(9190530 [latency] collect.py で out/*.json→RESULT §1/§2/§3 導出, Refs #200)/#275(1a521ee [docs] 前回 STATUS refresh=4373c5c boundary)**。**issue 整理（2026-06-17）: #117(R-44 free_fleet eval DoD 充足→**CLOSED**・PoC継続=#180)・#263/#271(古い日次レポート→CLOSED)・状態ラベル16件整備(ready×12 / blocked×2=#266/#284 / #288 blocked→ready)＝open epic 全て ready|blocked を1つ保持**。**2026-06-16 land（新しい順）: #267(4373c5c [docs] #233 collision_monitor: node-isolated ①reflex/④source_timeout = config 層 GO〔実 nav2_collision_monitor 1.3.11 + 実 collision_monitor.yaml〕／②③ full-sim 2台は human gate で PENDING〔Go/No-Go 転記欄 空欄維持〕／Open② 採用候補=forward-biased polygon〔nav-traffic 合意 gate・collision_monitor.yaml 不変〕＋doc16 owner ref :187→:189〔#261 re-pin 追従・stale branch base 由来の誤判定を base merge で解消〕。Refs #233→**#233 OPEN 継続**〔live ②③ + doc12/03/07 採用形反映 残〕・#126 既 CLOSED)/#272(bfbc61f [docs] doc21 eval_sdk extraction proposal 追加=domain-free eval core 抽出設計〔Phase1 実装は #273 OPEN〕)/#270(50f5bfe [docs] deploy/jetson README §4.1 に robot-free/robot-gated ゲート pointer, Refs #127)/#260(a2d0aed [docs] STATUS refresh c08af06 + 2026-06-15 land + worktree 実査)**。**2026-06-15 land（新しい順）: #261(c08af06 [docs] warehouse_rmf_adapter を doc16 §1/§2/§9 + ws/src/README に登録 + `check_package_registry`(B5・ERROR級＝disk 上の warehouse_* pkg が両レジストリ未記載なら fail＝今後の同種 drift を機械防止)追加 + doc16 +2行 shift に伴う `doc16:NNN` 参照を全 repo 再pin, Closes #221)/#262(a7e4b12 [docs] doc-accuracy/hygiene sweep=doc map に setup/jetson 追記・壊れ memory リンク修正・CI 全push現実同期・**08a↔doc12 reconcile**〔Guardian の blocked は recovery 止まりで estop 非格上げ・物理停止は独立の近接/衝突 reflex 責務, doc12:518〕・doc08a:154-161→164-173 行ref 再pin)/#265(1ca32a8 [docs] jetson §4.1 G0-G7 を robot-free/robot-gated 分類=Jetson 単体先着時は G1 メモリを先に回す指針・ゲート/閾値不変, Refs #127)/#264(a9729d5 [docs] langfuse spike RESULT 2026-06-15 live defer + env findings 記録, Refs #88)**。**2026-06-13〜14 land（新しい順）: #257(9b9d479 [de-risk] memory-gate 段階1 live RESULT 転記=**R-38 GO-leaning〔条件付き〕**＋cgroup-peak headroom アーティファクト所見, Refs #187)/#259(0726b3c [de-risk] memory-gate headroom を **working-set〔current−inactive_file=free -h available〕基準**へ修正=cache アーティファクト解消・OOM主判定不変・selftest 16/16・実#187 21サンプル再構成で GO-leaning end-to-end 検証, Refs #187)/#256(3e6c8d8 [nav-traffic] Mode C Fleet Adapter offline core=routing/namespacing/single-writer・rclpy/rmf_* 非import・契約無編集・APPROVE 0-blocking/0-major, Refs #180)/#250(7372f55 [llm-bridge] MCP tool6/7 escalation_response/start_negotiation 直接 dispatch unit + already_resolved 再拒否=additive・凍結契約不触, Refs #4)/#254(87356d7 [docs] STATUS refresh 774ef64 + 2026-06-12〜13 land block + worktree state)**。**2026-06-12〜13 land（新しい順）: #252(774ef64 [wo] offline KPI 算術コアに property-style 不変条件テスト追補=fixed-seed 決定的・`hypothesis` 非依存・mutation 9/9 caught, Refs #88)/#251(117d299 [ci/docs] firmware-safety job に kinematics+host-compile gate 2 step 追加 + doc20 §4 CI job 一覧整合=net-zero 行編集, Refs #242)/#249(eb08899 [firmware] pio native env に test_kinematics 取込=R-26 gate(`run_host_test.sh`)不触, Refs #3)/#248(3dd52d3 [wo] date-stamped Grok 価格表 additive(grok-4.3 $1.25/$2.50) + #88 Phase-3 live checklist 集約, Refs #88)/#246(3dbc32f [firmware] micro-ROS Phase-1 skeleton 肉付け=motor/sensor stubs+kinematics(skid-steer 逆運動学)+PHASE1_CHECKLIST+host-compile gate, Refs #3)/#247(bdc7bb2 [llm-bridge] Mode C stop no-actuation R-26 pin=navigate|stop|charge 完備, Refs #4)/#244(f4710cd [ci/firmware] Layer-0 clamp R-26 host unit を CI ゲート化=5本目 job firmware-safety, Refs #242)/#245(2532eba [llm-bridge] deadlock pattern-3 approach-precursor unit + offline-hardening 裁定記録, Refs #4)/#238(186a2ce [nav-traffic] #126 collision_monitor docs: Open⑤(surfacing defer)/⑥(recovery bypass)確定 + ① defer + 2-bot PoC runbook → **#126 CLOSED**・live PoC/Open②③④ は #233 OPEN 継続)/#237(4e41422 [de-risk] memory-gate operator GO sheet + RESULT 転記 pointer=#187 実走 human gate 準備, Refs #187)/#239(3f453f6 [firmware] Layer-0 clamp 非有限(NaN/±Inf) cmd_vel→stop fail-safe + unity shim 偽GREEN 修正 + host R-26 unit, **Closes #235**)/#241(9d10904 [docs] status-maintenance rule + `scripts/refresh_status.py` helper, Refs #240)/#240(3f9098b [docs] STATUS refresh 387e24e + 2026-06-11 land block)**。**2026-06-11 land（新しい順）: #236(387e24e [docs] doc12a に座標ゴール navigate variant/`INVALID_GOAL` を EOF 追補・#165 回避, Refs #223)/#234(1a28903 [wo] spike/langfuse Phase-3 verify harness, Refs #88)/#219(c17717b [nav-traffic] #180 GATE-前 warehouse_rmf_adapter design scaffold＝**R-38/#187 で BLOCKED**)/#230(0fdaf0e [nav-traffic/llm-bridge] 座標ゴール navigate `goal=(x,y[,yaw])`[XOR・yaw drop・`INVALID_GOAL`] + `HeadOnInjector` route_A 排他直列化, Refs #223/#156)/#231(7cfbef8 [firmware] Layer-0 速度クランプ ≤0.3m/s を host R-26 unit に pin, Refs #3)/#232(bf08384 [docs] STATUS refresh + #229 launch ref re-pin)/#229(bce4853 [nav-traffic] #126 nav2_collision_monitor を Nav2 速度経路に配線=twist_mux prio10 上流・emergency prio100 不変/R-26・controller remap を mode-conditional[A/B=cmd_vel/nav2_raw→monitor→cmd_vel/nav2 / Mode C=直行]・専用 gated lifecycle_manager・**virtual_scan per-source source_timeout 0.0**[>1.0m 無送信の stale-stop 誤発火回避=敵対 review MAJOR 修正・df397e3]。`Refs #126`=wiring のみ・PoC live + Open ②③⑤⑥ は人間 Docker ゲート→follow-up)**。**2026-06-10 land（新しい順）: #218(17b3f8f #156 slice3-live: runbook fix + SCENARIO fail-hard seed + host 24 tests)/#222(a0ce17a warehouse_rmf_adapter を CI cross-import map 登録)/#217(memory-gate doc-fix + verdict FLOOR)/#215(#175 config._validate_safety 非数値型ガード)/#214(post-197 slice3 handoff・05番号gap)/#211(latency fail-closed spend-guards)/#210(memory-gate Hermes liveness)/#206(base prompt battery を doc08a 3段へ reconcile)/#205(parallel-discipline + completion-contract template)/#204(cac5db6 #156 slice3 scenario/rviz_config sim-forward)/#203(Mode C prompt 忠実化 doc08c)/#202(latency verdict branches)/#201(memory-gate Hermes PATH)/#199(#177 session-orchestration §refs pin)/#191(STATUS refresh)**。**2026-06-08 land（新しい順）: #197(slice3 sim startup seeding docs)/#195(Jetson preflight runbook checks)/#196(slice3 live precheck helper)/#194(malformed LLM response cycle ignore + STATUS refresh)**。**2026-06-06〜07 land（新しい順）: #190(Codex context layer=MERGED 06-07)/#184(#156 slice2 commander-cycle wiring harness)/#189(nav-traffic nav2_params safety-pin)/#185(sim head-on spawn preset)/#188(release-path: sim 先行リリース正本化 docs)/#183(llm-bridge #181 Mode-A deadlock prompt+task injection)/#182(R-44 docs follow-up)/#179(doc12/11c cross-ref re-pin)**。**2026-06-05 land（新しい順）: #157(#126 collision_monitor cmd_vel 挿入トポロジ docs)/#154(create-issue skill)/#153(#125 yield ≥0.15m live=CLOSE)/#152(#126 pose-freshness)/#151+#150(#103 fairness guard=CLOSE)/#149(11a §9 yield topology)/#144+#143(#125 traversal/#126 edge-trigger)/#141(#124 真200mm隘路=CLOSE)/#140(.claude Langfuse)/#138-139(#133 wo Grok=CLOSE/#125 vx_max)/#134-136(STATUS整合/docs sweep)**。**2026-06-04 land（新しい順）: #132(L2 #116 R-37 host spike)/#129(L4 #88 比較指標凍結)/#130(L1 nav2_params・11a doc-drift)/#128(L3 #55 deadlock 再基礎付け)**。新規 issue: #124(sim 隘路)/#125(nav Phase2 margin・vx_max config)/#126(R-39 collision_monitor)/#127(jetson fidelity)/#131(#55 波及同期)/#133(wo Grok cost code)。直近 land（新しい順）: #120(STATUS鮮度) / #121(#44 battery scale 単一正規化) / #119(nav2 MPPI footprint #67) / #118(sim odom→base_link TF, closes #112) / #115(短期記憶#102 + #108/#109 cross-lane Langfuse join) / #113(nav #67 E2E gate) / #111(Phase4 公平性 memory/skills OFF docs) / #114(CTRV 横展開 + STATUS 鮮度) / #110(CTRV 実装, closes #101) / #107(doc03 内部トピック表網羅, closes #61) / #106(jetson safety doc Requires→BindsTo) / #105(STATUS 鮮度) / #100(08a CTRV 改訂) / #104(#4 S2-PR2 HALF B 実 dispatch+nav2_bridge REST 転送) / #98(doc12 battery `<=`) / #97(doc09 §7 sim/SLAM 二系統) / #94(sim `/clock`+占有マップ, critical-path) / #96(jetson deploy scaffold) / #92(wo node-wiring) / #93(bringup 合成) / #91(docs-first 実装ゲート) / #90(battery `<=10%`, closes #59)。
- **issue 整理（2026-06-03）**: #73(Langfuse trace 所有=完了 → Phase3 は **#88**) / audit umbrella **#37**(根本=#41確定) / #59(battery `<=10%`、#90 で close) を close。日次レポート #53/#62/#63/#84 を close。残 open の整合 issue は **#44/#55**（#61→#107・#102/#108/#109→#115・#112→#118・**#54→明示 /stop 撤回**(2026-06-05) で close 済）（+ #88 Phase3 Langfuse 検証 / #67 2台 E2E）。
- **worktree 状態（2026-06-17・実査）**: `git worktree list` = `main`(origin/main=`261a6d8`) / `mwr-eval-sdk-registry`(`docs/eval-sdk-registry`・**本セッション docs PR**=eval_sdk 正準 registry 登録 + B5 guard 緩和) / `mwr-status-0617`(`docs/status-0617`・**本 PR**) ＋ マージ済 PR 由来の stale worktree 多数（`/tmp/mwr-pr*` review 系・`mwr-character-llm`/`mwr-llm-bridge`/`mwr-prompt-langfuse` 等＝cleanup 候補）。`git ls-remote --heads origin` = `main` / `docs/eval-sdk-registry`(本セッション) / `docs/status-0617`(本 PR) / `feat/commander-cadence-config`(#297 OPEN) / `feat/langfuse-env-tag`(**stale**=PR#292 CLOSED・未merge) / `chore/langfuse-api-guard`(**stale**=PR#286 `e466878` MERGED 後) / `feat/llm-bridge-prompt-langfuse`(**stale**=PR#290 `9650047` MERGED 後)。**OPEN PR（実査）**: #297([llm-bridge] make commander cadence config-driven + time-anchor outage) ＋ 本セッション docs 2本(`docs/eval-sdk-registry` / `docs/status-0617`)。**#292 は CLOSED（未merge）・#290/#273/#274/#268 は MERGED**（旧 OPEN 一覧から除去）。**継続 OPEN**: #4 llm-bridge ＋ #228/#288/#289 Mode C/character live / #88 wo Langfuse Phase3 ＋ #281/#282/#283/#284 follow-up / #233 nav-traffic live PoC〔②③ full-sim human gate〕 / #3・#116 firmware(実機待ち) / #180・#187(R-38 段階2 Jetson/live=人手ゲート) / #266 jetson(実機投入前ゲート G0-G7) / #8 nav-traffic / #44 safety-state / #269 eval_sdk roadmap。**#117/#263/#271 は本セッションで CLOSED・#221 は PR#261 で CLOSED**。

## Mode X-ER 設計状況（2026-06-25・docs-only）
- **L4 実装 = XER0(docs 設計)段階のみ・Mode X-ER 固有コードはゼロ**（`*.py`/`*.yaml` 全体に `RoboticsPlan`/`ValidationReport`/`ErTaskRequest`/`provider_type`/`VisualResolver`/`TaskGraphExecutor`/`CommandCompiler`/`x_lite`/`x_rmf`/`calibration_id` の実体なし＝grep 裏取り）。実装済みは Mode A/B/C 汎用の L4 配線（`scheduler.py` commander cycle / `tracing.py` / `situation.py` / `action_map.py` idempotency mint / `executor.py` / `hermes_client.py` / `schemas.py` `CommandAction`/`KNOWN_LOCATIONS`）で、Mode X-ER は将来これを再利用する。XER1-7 と G0-G6 は全未着手。
- **未凍結7ブロッカーの解決方針を [docs/mode-x-er/06-unfrozen-contract-resolutions.md](mode-x-er/06-unfrozen-contract-resolutions.md) に整理**（FREEZE-NOW / DEFER / NEEDS-PROBE 判定 + 提案形 + contract PR 手順）。要旨: #4 = FREEZE-NOW(MVP=known location 限定・additive ゼロ) ＋ DEFER(coordinate goal 契約) / #5 = NEEDS-PROBE(ER 1.6 audio=公式 YES・Hermes は image 対応/`input_audio` 400 公算→audio は direct 一次・PROBE-1/2/3) / #1#2#3#6#7 = DEFER(gate)。**いま `warehouse_interfaces`/config/topic に凍結すべき項目は実質ゼロ**。`#2` enum は `warehouse_interfaces` に入れず bridge-local(observation-only) / `#7` は**案A（専用 `/operator/notice`）の採用方針のみ確定**（`std_msgs/String` JSON・`operator_notice.v0`・QoS は doc05 §8 の contract draft＝**未凍結**[§8.8 に topic名/depth/publisher/emergency/schema_version の未決]・凍結は XER-OF1 + 別 contract PR 後）。
- 機械ゲート: `python3 scripts/check_consistency.py` = **0 ERROR**（残 WARN は既存の STATUS SHA 陳腐化 + doc04 README:41 行ドリフトのみ・doc06 由来ゼロ）。**次回 STATUS refresh で SHA pin 再同期が必要**（pin `261a6d8` → 現 origin/main）。
