# 07 Mode X-ER 実装ステータス（想定 vs 現状・ER→L3）

> **目的**: Mode X-ER の「ER（Gemini Robotics-ER）→ L3 → frozen Command → L2/L1/L0」パイプラインが、**設計 docs が想定する完成形**に対して **`origin/main`（`4114195`）で今どこまで実装・検証されているか**を、実コードの file:line で裏取りして可視化する。図解版は [implementation-status.html](implementation-status.html)（[mode-x-er-explainer.html](mode-x-er-explainer.html) と同スタイル）。
> **正本の切り分け**: 「想定」= 設計 docs（[01](01-architecture-and-flow.md)/[02](02-l3-planning-core.md)/[03](03-er-adapter-skeleton.md)/[04](04-er-input-modalities-and-stt.md)/[06](06-unfrozen-contract-resolutions.md)）。「現状」= main の実コード＋テスト。両者がズレたら**コードが真**（本 doc は現状の記録であり、設計正本を上書きしない）。
> 生成 2026-07-02（re-pin 2026-07-11・#449-#453 land 反映＝ER-in-Hermes 標準整合 / object-class snap / composition factory seam / plugins incubator / eval join。re-pin2 同日・#454-#456 land 反映＝STATUS refresh / plugin_id envelope / G5 無償 replay 経路）・対象 `origin/main 4114195`。live 手順は [dev/07 runbook](../dev/07-mode-x-er-live-e2e-runbook.md)。

<!-- impl-status-pin: 411419587612 -->
<!-- 鮮度ゲート: `python3 scripts/check_impl_status.py`（開発が進んで pin より main が動いたら drift を報告）。更新規律は末尾「鮮度と更新」節。 -->

## 総括（どこまで終えているか）

- **offline の ER→L3 チェーンは「frozen Command まで」完成・テスト済**。手組みの `RawModelOutput` が Handoff → Validator（R-26 0-dispatch）→ Visual Resolver → Task Graph Executor → Command Compiler を通り、**本物の `warehouse_interfaces.schemas.Command`** に着地する（`pipeline.compile_raw_output`＝[pipeline.py:90-187](../../ws/src/warehouse_llm_bridge/warehouse_llm_bridge/robotics_planning_core/pipeline.py)）。core chain（validator/pipeline/handoff/adapter/transport の 5 file）の offline unit **63 本が緑**（L3 各 stage の個別 suite はさらに多数＝下記マトリクスの per-stage 本数参照。要 python3.12。host `python3`=3.7 は `frozenset[str]` で落ちる）。
- **live-send は #389 で main に着地**（2026-07-02 merge）＝`build_provider_request` + `HttpErTransportSender` + `_live_send` + HERMES→DIRECT fallback + `WAREHOUSE_LIVE_ER` gate。**XER6 Slice A/B（#419/#423）で稼働 `x_er_bridge` cycle がこの `propose_plan` を毎 cycle 呼ぶ配線が着地**（offline=fixture 即答・live-send は gate 越し）。
- **背骨（X-ER commander node `x_er_bridge`）は land**（#419 Slice A + #423 Slice B・main `9ad7ec9`）＝startup composition→cycle（ER→plugin gate→L3→gen→dispatch）→completion（`goal_result`→`mark_succeeded`→再駆動）を鎖化し、**hops ⓪(稼働 cycle 部分)②③④⑤ が OFFLINE-WIRED**（offline で red→blue を node 単独完走）。ただし**実 ER→live Command→L2/Nav2 作動を稼働 rclpy node が sim で回す RUNNING 実走はなお未達**（`forward_to_nav2=true`・2-bot Gazebo＝G5 human gate・該当 sim テストなし。**無償 replay 経路＝`mode_x_er.er_offline_payload`（#456・[dev/08 追補2](../dev/08-xer6-live-sim-x-lite-runbook.md)）が land 済＝G5 実走は課金なしで可能**）。
- 要するに: **offline L3 全チェーン＋ live-send 機構＋ `x_er_bridge` 背骨（composition・cycle・completion 配線・config forwarder flip）＝実装済み（offline 決定論）**。残るは **live rclpy spin + sim 作動（G5 human gate）** と実マイク capture（後続 slice）。

## 想定 vs 現状マトリクス（file:line 裏取り）

| ステージ | 想定（設計 doc） | 現状 | 主な根拠（file:line） | ギャップ |
|---|---|---|---|---|
| **L4 ER-in-Hermes 境界**（標準=fork 8644 一本・direct=fail-safe） | **ADR-0002 決定**: fork gateway **8644**（`input_audio`＋text/image）**1 本で全 modality を Hermes 経由**が標準（TARGET）。8643（lean text/image）は 8644 へ**統合し retire**。`direct` は緊急 fail-safe / 恒久 fallback（[ADR-0002](../adr/0002-er-in-hermes-standard.md):25-26 / doc06 §5） | **PARTIAL**（標準決定＋live 実証済／default 配備は未） | `deploy/hermes/er-audio-fork/run-er-gateway.sh:3-4,30-36`（8644 LIVE 200）・XER6 live-matrix で **12/12 approved sends が全て 8644 fork 経由**（[dev/07:211](../dev/07-mode-x-er-live-e2e-runbook.md)・#428/#429）・PROBE-2 400=`doc06:159`（unforked に fork が必須の根拠） | **shipped default は依然 audio=direct**（config `er_gateway` 既定 OFF→`resolve_audio_transport`→DIRECT＝ADR-0002:52）。残=8644 の default 配備＋8643 retire 完了（#357） |
| **L4 adapter: transport 選択 + offline seam** | `propose_plan(ErTaskRequest)→RawModelOutput`、`resolve_audio_transport`（fail-safe DIRECT）、observation-only enums | **DONE** | `transport.py:29-58`・`adapters/enums.py:23-40`・`er_task.py:31-73`・`gemini_er.py:39-75`（offline seam）。25 offline tests | なし（選択面のみ。実行ではない） |
| **L4 adapter: LIVE-SEND 実行** | frozen per-transport 組立 + HTTP sender + HERMES→DIRECT fail-safe（doc06 §5 tail:277-279） | **DONE**（機構）／ 呼び出し配線も **DONE**（#419/#423） | **#389 で main 着地**（2026-07-02）＝`gemini_er.py` の `build_provider_request`/`ErTransportSender`/`HttpErTransportSender`/`_live_send`・HERMES→DIRECT fallback・`WAREHOUSE_LIVE_ER` gate（未設定で RuntimeError）。**XER6: `x_er_bridge` cycle が毎 cycle `adapter.propose_plan` を呼ぶ**（`x_er_bridge.py`・#419）。offline HTTP unit で URL/header/gate/missing-base_url を検証 | 実 HTTP live-send は gate 越し（有料・operator gate）。稼働 cycle の呼び出し自体は着地（offline=fixture・live=gate） |
| **知覚レーン + STT + observability** | ER critical lane ∥ out-of-band STT（レイテンシ非追加・fail-open）、Bridge 所有 Langfuse trace（deterministic seed） | **DONE** | `perception_lanes.py:48-125`・`observability.py:88-147`（`LangfuseTranscriptTracer` は実 fail-open span・既定 `enabled=False` no-op＝#382/`75f6af3` で旧 no-op を置換）。11 offline tests | `HermesTranscriber` の実 HTTP path・`JsonlTranscriptSink` の書込 path は offline unit なし（live-gated のみ）。実 Langfuse 着地は #88 human gate |
| **L3 Validator（0-dispatch + ValidationReport 語彙）** | `validate(raw,ctx)→ValidationReport`、status≠accepted→0 dispatch、正確に 9 code、注入式 PlanPolicy、反復サイクル検出 | **DONE** | `validator.py:90`・`report.py:69-87`（**厳密に 9 code**）・`report.py:163-181`（3層 0-dispatch double-guard・frozen）・`validator.py:55-84`（反復 Kahn）。53 tests | `normalized_plan` は意図的 defer stub（doc02:346）＝下流の責務 |
| **L3 Visual Resolver（pixel→map→snap）** | `resolve(plan,calibration)→ResolutionResult`、3×3 homography・valid-polygon・最近傍 KNOWN_LOCATION snap（注入半径・**距離 かつ object class**＝doc02:150）・座標ゴール禁止 | **DONE**（機構。site class 供給は defer） | `resolver.py:60-76`(homography・w≤0 fail-closed)・`:79-104`(polygon)・`:194-199`(距離 gate 4a)・`:201-217`(**object-class 一致 gate 4b＝#452**・不一致→`object_class_mismatch` 0-dispatch・redirect 無し)。26 tests・`pipeline.py:180` で wire | doc02:150 conjunction は **#452 で完全実装**（旧 `# TODO(object-class)` resolver.py:192-194〔pre-#452 実査で位置確認済〕は解消）。残: `VisualPolicy.location_classes` の本番供給は未配線（`x_er_composition.py:239-241` は coords+radius のみ注入＝既定空で距離のみ挙動）・`Detection.color`（class proxy）の live 実在性未実証 |
| **L3 Task Graph Executor** | 6-state・自作 runtime state（NetworkX なし）・after 順 ready_tasks・二重/重複/サイクル guard・swappable store・zero actuation | **DONE** | `states.py:30-38`・`executor.py:84-146`(ready_tasks)・`:115-127`(dedup)。17 tests・`pipeline.py:185-186` で wire | doc-drift のみ（`executor.py:9-11` の「pipeline に wire しない」注記が XER6 後 stale） |
| **L3 Command Compiler（XER5）** | `WarehouseNavCompiler` が resolved task を frozen `Command` へ・0-dispatch skip・1:1 audit・gen_id/速度/座標なし・`ExecutionProfile` x_lite/x_rmf | **PARTIAL** | `compiler.py:95-134`（実 `warehouse_interfaces.schemas.Command/CommandItem` を構築）・`:113-130`(0-dispatch skip)・`:79-83`(x_rmf は NotImplementedError)。`pipeline.py:187` で wire。42+ tests | **x_rmf 半分が defer**（#346・`RmfTaskCompiler` は設計/stub のみ）。doc-drift（`__init__.py:6`/`CLAUDE.md:6` の「wire しない」注記が stale） |
| **L3 pipeline/handoff 配線** | 常在 Handoff seam が L4 raw を決定論的 Validator 入力へ正規化（fail-closed KEY-NAME gate）・full L3 chain to frozen Command・transport 非依存 | **DONE**（稼働 cycle へ **OFFLINE-WIRED**） | `handoff.py:114-164`（endpoints/velocity/coords/unknown を reject）・`pipeline.py:58-87`(validate_raw_output)・`pipeline.py:90-187`(compile_raw_output・R-26 short-circuit)。41 tests・transport 等価性検証 | **`x_er_bridge` cycle（`x_er_cycle.run_x_er_cycle`・#419）が `RawModelOutput`→`compile_raw_output` を稼働配線**（offline pytest で red→blue 完走）。**live rclpy spin での実走は G5 human gate** |
| **テスト + live インフラ** | offline XER1-5 suite・env-gated live 前駆 probe・lean gateway + safe `--check`・XER6 sanctioned live e2e（sim） | **PARTIAL**（offline autonomous e2e land・sim は gate） | offline: `test_l3_pipeline.py:197-317`（chain to Command）・**`test_x_er_offline_e2e.py` / `test_x_er_autonomous_e2e.py`（#419/#423・`goal_result` 単独で red→blue・手動 mark 無し）**。live: `tests/live/{test_er_handoff_live,test_xer3_chain_live,test_xer_full_chain_live}.py`（`WAREHOUSE_LIVE_ER=1`+key）・`run-er-hermes.sh`(8643) | offline autonomous e2e は land。**live-matrix harness＝`spike/xer6-live-matrix`（#428）が live ER→L3→dispatch 記帳→赤青順序を 12/12 sends で実証**（harness 駆動＝RUNNING node ではない・[dev/07:211](../dev/07-mode-x-er-live-e2e-runbook.md)）。**XER6 の sim closure（Command→L2/Nav2 を稼働 node が作動）は G5 human gate＝未実走**（`tests/e2e/*` は Mode A nav） |
| **L3 composition 層（run manifest + plugin）** | run_manifest.v1 + startup fail-closed preflight + effective-composition record + typed validate_plan hookspec + namespaced plugin code + policy clamp + safety-critical profile hash gate（[productization/09](../productization/09-run-manifest-and-plugin-composition.md) / [ADR-0003](../adr/0003-bridge-local-manifest-composition.md)） | **DONE**（稼働 node に **WIRED**・#419） | `robotics/composition/`（15 module）＋**`x_er_bridge` 起動時に `x_er_composition.build_x_er_runtime` が §4 steps1-7 を fail-closed 実行**（manifest→preflight→triple cross-check→governed calibration→effective-composition witness `out/runs/<run_id>/`）。**production factory seam＝#450**（`factory_registry.py` explicit registry・node が `production_plugin_factories()` を渡す・registry は今日空・missing factory は従来どおり起動拒否）。**eval 側 offline tooling＝#453**（`decision_event_envelope.py`＋`offline_join_report.py`/`duckdb_join.py`＝doc09 実装順序 3/4） | 起動 composition は稼働 node に配線済（offline）。**sim/live 実走は G5 gate**。残: incubator plugin（`plugins/l3_zone_policy`＝#451・status draft）の registry 実配線は review gate 後・**lifecycle status gate（doc10:152-153）は機械未強制**（doc09 §RESIDUAL）・per-cycle stage 記録は follow-up |

## 一気通貫（e2e）の現状 — 2 tier・どちらも作動には届かない

- **OFFLINE（無料・通常 CI/`pytest`・network/provider/ROS 不要）**: 手組み `RawModelOutput` が L3 全チェーンを通り frozen Command に着地。正確な範囲=[`tests/unit/test_l3_pipeline.py:197-317`](../../tests/unit/test_l3_pipeline.py) が `compile_raw_output`（handoff→validator→visual_resolver→task_graph_executor→command_compiler）を両 transport で駆動し、R-26 0-dispatch short-circuit を `RaisingCompiler` で保護。**core chain 5 file で 63 unit が緑**（要 python3.12。host `python3`=3.7 は `frozenset[str]` で失敗。L3 各 stage の個別 suite〔validator/resolver/executor/compiler〕は別途より多数）。
- **LIVE（有料 Gemini Robotics-ER・human gate・`WAREHOUSE_LIVE_ER=1`+`GEMINI_API_KEY`/`GOOGLE_API_KEY`）**: [`tests/live/test_xer_full_chain_live.py`](../../tests/live/test_xer_full_chain_live.py) が**実課金 generateContent**（`_er_live_client.py:call_er_direct`）→ `RawModelOutput(direct)` → `compile_raw_output` → `Command` かつ全 item.destination が frozen KNOWN_LOCATIONS ∧ action==NAVIGATE を assert。runner=[`deploy/dev/run-live-er-chain.sh`](../../deploy/dev/run-live-er-chain.sh)（安全 `--check` あり）。text/image は lean Hermes gateway(8643)経由も可、audio は direct。**これらは forerunner（不変条件の assert であり acceptance ではない）で、チェーンは Command で終端＝L2 MCP/Policy Gate/L1/L0 へ続かない**。
  - **2026-07-02 live 実走（operator 承認・課金）**: `test_xer_full_chain_live.py` **PASS**。実 `gemini-robotics-er-1.6-preview` 呼び出し（**607 tokens**）→ `compile_raw_output` → **valid frozen Command（`command_items=0`・`destinations=[]`）**。この fixture では live ER 出力が KNOWN_LOCATIONS へ解決せず **R-26 0-dispatch**（安全に空 Command）＝**チェーンが live で end-to-end 動作し安全に終端すること**は実証。ただし**非空 live Command（ER 出力が KNOWN_LOCATIONS に解決するケース）は未実証**。この test は adapter の `propose_plan` live-send（**#389 で main 着地したが本 test は経由しない**）ではなく `_er_live_client.call_er_direct`（test 側直呼び）を使う。gateway は `run-er-hermes.sh`(8643) を起動済で health OK（`/v1/models`=`hermes-agent`）。
- **どの run もカバーしないもの**（2026-07-07 更新）: (1) の「稼働 cycle が `propose_plan` を呼ぶ」配線は **#419/#423 で着地**（`x_er_bridge`・offline=fixture／live=gate）。残るは (2) **稼働 rclpy node を sim で spin し Command を L2/Nav2 へ実作動させる XER6 sim closure**（`forward_to_nav2=true`・2-bot Gazebo・[dev/07:188](../dev/07-mode-x-er-live-e2e-runbook.md)・[README:91](README.md)）＝**G5 human gate・該当 sim テストなし**（offline autonomous e2e までは land）。

## トップダウン連結（running で上から届くか）

STATUS（実装済みか）とは**直交する第 2 軸＝CONNECTIVITY**（稼働している rclpy node から、上＝operator 入力 → 下＝motor へ各 seam に到達できるか）。file:line で裏取りした結論（2026-07-07・#419/#423 land 後）: **背骨＝X-ER commander node `x_er_bridge` が land し、各 seam を鎖化する caller が存在する＝hops ⓪(稼働 cycle 部分)②③④⑤ は「node に配線済」に前進**。ただし正直な区別を導入する: **OFFLINE-WIRED（node 実装・offline pytest で cycle が走る）≠ RUNNING（実 rclpy node を sim で spin し L2/Nav2 まで実作動）**。後者は **G5 human gate ＝ まだ実走していない**（launch は `mode_x_er.enabled`∧`llm` gate・base は既定 OFF）。図解版は [implementation-status.html](implementation-status.html) の「つながり(connectivity)」帯（node の**枠＝ring**で表現。塗り＝STATUS とは別軸）。

| hop | seam | 到達状態（connectivity） | 根拠（file:line） |
|---|---|---|---|
| ⓪ | mic → audio capture / operator input lane を driving | **PARTIAL**: 稼働 cycle 部分は `x_er_bridge` が driving（#419・入力源=`request_fixture` v0）／**実マイク capture 部分は NOT-IMPLEMENTED**（後続 slice） | `x_er_bridge.py`（`_run_cycles`・`request_fixture`）／`perception_lanes.py:48-125`（mic lane は caller なし） |
| ① | text/image → lean Hermes ER gateway(8643) の「200 OK」 | **CONSTRUCTIBLE**（node が起動時 `build_er_adapter` で adapter を構築・#419／offline=fixture で gateway 非経由・live gateway 200 は gate） | `x_er_bridge.py`（`build_er_adapter`）・`run-er-hermes.sh:2-8`・live は `tests/live/*` human gate |
| ② | adapter LIVE-SEND（`propose_plan`/`HttpErTransportSender`） | **CALLED-BY-CYCLE**（`x_er_cycle` が毎 cycle `adapter.propose_plan` を呼ぶ・#419／offline=fixture 即答・live-send は gate） | `x_er_cycle.py`（`run_x_er_cycle` step1）・`gemini_er.py:39-75`（`WAREHOUSE_LIVE_ER` gate） |
| ③ | adapter → `pipeline.compile_raw_output`（RawModelOutput 受け渡し） | **OFFLINE-WIRED**（`x_er_cycle` が cycle 内で `compile_raw_output` を呼ぶ・#419／sim spin は G5 gate） | `x_er_cycle.py`（step3）・`pipeline.py:90-187`・`test_x_er_offline_e2e.py` |
| ④ | full L3 chain → 凍結 `Command`（handoff→validator→resolver→executor→compiler） | **OFFLINE-WIRED**（稼働 cycle が鎖化・offline 完走／実 rclpy spin は未） | `x_er_cycle.py`・`handoff.py:114-164`・`validator.py:90`・`compiler.py:95-134`・`pipeline.py:187` |
| ⑤ | 凍結 `Command` → L2 action_map/MCP/Policy Gate → L1 Nav2 → L0 | **OFFLINE-WIRED**（`x_er_cycle`→`command_to_tool_calls`→`DispatchToolExecutor`→`WarehouseTools.dispatch`・#419／既定 `nav2_forwarder=None`=0 actuation・`forward_to_nav2=true` で `Nav2RestForwarder`=#423／completion `goal_result`→`mark_succeeded`=#423 で red→blue 完走）。**sim 実作動は G5 gate** | X-ER: `x_er_cycle.py`（step5）・`x_er_completion.py`。Mode A 同型: `scheduler.py:368-374`・`bringup.launch.py:240-243` |

**verdict**（2026-07-07・#419/#423 land 後）: **背骨＝X-ER commander node `x_er_bridge` は land**し、hops ⓪(稼働 cycle 部分)②③④⑤ を鎖化する caller が揃った＝**「X-ER に欠けていた配線」は埋まった**（offline で red→blue を node 単独完走）。残る 2 軸差分は正直に: **(a) OFFLINE-WIRED であって RUNNING ではない**（実 rclpy node を sim で spin し L2/Nav2 まで実作動させる XER6 sim closure は G5 human gate・未実走）、**(b) hop⓪ の実マイク capture は未実装**（入力源は v0 `request_fixture`）。Mode A の同型 seam（`llm_bridge.py:308`→`scheduler.py:368-374`→`bringup.launch.py:240-243`）は既に sim/実機で稼働実績があり、X-ER も同じ launch 経路（`bringup.launch.py` の x_er_bridge Node）に載った。

**next_wire（背骨＝DONE・#419/#423）**: `warehouse_llm_bridge/x_er_bridge.py`（`main()` + `rclpy.Node`）は **land**（Slice A #419 + Slice B #423）＝`setup.py` `console_scripts`（`llm_bridge` と同型）登録・`bringup.launch.py` に x_er_bridge Node 追加（launch gate は `llm=='true' and mode_x_er.enabled`＝#423 で `llm` co-gate を追加）。cycle が `build_er_adapter` → `propose_plan` → `compile_raw_output` → dispatch →（`goal_result`）`mark_succeeded` を鎖化し、**hops ⓪(稼働 cycle 部分)②③④⑤ を OFFLINE-WIRED 化**（L3 帯の OFFLINE-ONLY が「node に配線済」へ変わった）。**残る next_wire = live rclpy spin + sim 実作動（`forward_to_nav2=true`・2-bot Gazebo・G5 human gate）と実マイク capture**。※ 起動 gate と node 契約の正本は [08-x-er-bridge-node-spec](08-x-er-bridge-node-spec.md)＝launch 条件は `traffic_mode=='x-er'`（本行の旧スケッチ・不採用）ではなく **`mode_x_er.enabled`**（[06 §3 追補](06-unfrozen-contract-resolutions.md)・traffic_mode に `x-er` 値は発明しない）。

## ER inside Hermes（図の要）

「ER が Hermes 内にいる」の標準は **fork gateway 8644 一本で全 modality（text+image+`input_audio`）**（[ADR-0002](../adr/0002-er-in-hermes-standard.md) 決定。8643 lean gateway は 8644 へ統合し **retire 中**の CURRENT 実体）。XER6 live-matrix は **12/12 approved sends を全て 8644 fork 経由で live 実証**（[dev/07:211](../dev/07-mode-x-er-live-e2e-runbook.md)・#428/#429）＝8644 は「probe のみ」ではなく **live 実績のある標準 wire**。ただし **shipped default の audio は依然 direct**（config `er_gateway` 既定 OFF→`resolve_audio_transport`→DIRECT。`direct`＝緊急 fail-safe / 恒久 fallback・ADR-0002:52）。個人 `~/.hermes` は触らない（`HERMES_HOME` 隔離）。

## 残作業（完成形へ）

- ✅ **PR #389 merge 完了（2026-07-02）**＝live-send 機構（build_provider_request + HttpErTransportSender + `_live_send` + HERMES→DIRECT fallback + `WAREHOUSE_LIVE_ER` gate）が main に着地。残るは次項の「稼働 cycle 配線」（[doc06:277-279](06-unfrozen-contract-resolutions.md) / [dev/07:189,202](../dev/07-mode-x-er-live-e2e-runbook.md)・#344/#389）。
- ✅ **L4 アダプタ出力を稼働 Bridge サイクルへ配線＝完了（#419/#423）**＝`x_er_bridge` cycle が `RawModelOutput`→`compile_raw_output`→dispatch→`goal_result`→`mark_succeeded` を鎖化（offline で red→blue 完走）。残るは実 rclpy spin（次項）。
- **XER6 sanctioned live/sim e2e**＝稼働 rclpy node を sim で spin し frozen Command を L2 MCP/Policy Gate/L1 Nav2 Bridge へ実作動（`forward_to_nav2=true`・2-bot Gazebo・X-lite）。**G5 human gate・main に sim テストなし**（offline autonomous e2e までは land。owner: [README:91](README.md) / [dev/07:188](../dev/07-mode-x-er-live-e2e-runbook.md)）。
- **ER-in-Hermes 標準（fork 8644）の default 配備**: 標準決定（[ADR-0002](../adr/0002-er-in-hermes-standard.md)）＋live-matrix 12/12 sends 実証（[dev/07:211](../dev/07-mode-x-er-live-e2e-runbook.md)）は済。残=config `er_gateway` 既定 OFF の flip（shipped audio=direct の解消）＋8643 の retire 完了（owner: [environments.md:26](../../.claude/rules/environments.md)・#357）。
- ✅ **Visual Resolver の object-class snap＝#452 で実装完了**（doc02:150「距離 かつ object class」conjunction・gate 4b `resolver.py:201-217`・不一致→`object_class_mismatch` 0-dispatch）。残: `VisualPolicy.location_classes` の本番供給（`x_er_composition.py:239-241` は coords+radius のみ注入）・`Detection.color` の live 実在性確認。
- **x_rmf ExecutionProfile**＝`RmfTaskCompiler` plugin 実装（Mode X-rmf backend。owner: [02:234,240](02-l3-planning-core.md)・#346）。
- **実 Langfuse trace の着地**（ER/STT 観測レッグ・既定 OFF・main に未着地。human gate #88/HLF-G0..G5。owner: [productization/02:177-199](../productization/02-l4-robotics-bridge-box.md) / `observability.py:16`）。
- **offline カバレッジ追補**: `HermesTranscriber` 実 HTTP/fail-soft・`JsonlTranscriptSink` 書込 path（現状 live-gated のみ。owner: [04](04-er-input-modalities-and-stt.md) / doc06:164）。
- ✅ **doc-hygiene（#419 で sweep 済）**: 「pipeline に wire しない」stale 注記 5 箇所（`visual_resolver/resolver.py:9-10`・`visual_resolver/__init__.py`・`task_graph_executor/executor.py:9-11`・`command_compiler/__init__.py:6`・`command_compiler/CLAUDE.md:6`）を稼働 caller（x_er_bridge）着地に合わせ [docs-first](../../.claude/rules/docs-first.md) 同期で是正済。
- ✅ **plugins/ incubator 着地（#451）**: repo-root `plugins/l3_zone_policy`（[doc09:262-271](../productization/09-run-manifest-and-plugin-composition.md) の形・status **draft**＝offline replay＋review 専用・実行可能 fixture pair＝`tests/unit/test_plugins_incubator_zone_policy.py`）。残: registry（#450 seam）への実配線は review gate 後。
- ✅ **eval 側 offline tooling（doc09 実装順序 3/4）＝#453 land**: `decision_event_envelope.py`（fail-closed envelope 検証）＋`offline_join_report.py`/`duckdb_join.py`（`(run_id, gen_id)` join・join_gap/artifact_missing 検出・engine 等価 unit）。
- **composition factory seam の残（#450 後）**: incubator plugin の registry 実配線（review gate 後）と **lifecycle status gate（doc10:152-153「`approved` 以上のみ有効化」）の機械強制**（現状は運用規約のみ。owner: [doc09 §RESIDUAL](../productization/09-run-manifest-and-plugin-composition.md)）。

## 鮮度と更新（freshness・開発が進んだら更新する仕組み）

本 doc + [図](implementation-status.html) は **main の 1 時点スナップショット**（pin=`4114195`）。`STATUS.md` と同じく放置すると開発で腐るので、鮮度ゲートを持つ:

- **pin**: 冒頭の `<!-- impl-status-pin: SHA -->` が assess 対象の main。
- **checker**: **`python3 scripts/check_impl_status.py`**（read-only・stdlib）が (1) pin vs `origin/main` の staleness、(2) pin 以降で **ER/L3 パイプラインを触った commit 数**（0 なら snapshot はほぼ有効／>0 なら要再確認）、(3) tripwire（`build_provider_request` 等の presence が **pin と `origin/main` で異なれば** flag＝assess 後に flip した claim を検出。re-pin で自己解消）を報告し、drift/stale で exit 1。
- **更新規律**: XER ステージが land した／checker が drift を出したら **該当行 + 図 + pin を更新**する（大きく変われば 9-stage assessment を再実行）。`STATUS.md` と同じ **round 境界 refresh**（[status-maintenance.md](../../.claude/rules/status-maintenance.md)）。checker の CI 配線は governance follow-up（人間が `.github`/pre-commit に）。

## References

- 図解: [implementation-status.html](implementation-status.html)（本 doc の視覚版）/ [mode-x-er-explainer.html](mode-x-er-explainer.html)（全体像）
- 設計正本: [01-architecture-and-flow](01-architecture-and-flow.md) / [02-l3-planning-core](02-l3-planning-core.md) / [03-er-adapter-skeleton](03-er-adapter-skeleton.md) / [04-er-input-modalities-and-stt](04-er-input-modalities-and-stt.md) / [06-unfrozen-contract-resolutions](06-unfrozen-contract-resolutions.md)
- live 手順: [dev/07-mode-x-er-live-e2e-runbook](../dev/07-mode-x-er-live-e2e-runbook.md) / gateway: `deploy/dev/run-er-hermes.sh`(8643) / `deploy/hermes/er-audio-fork/`(8644)
- 実装: `ws/src/warehouse_llm_bridge/warehouse_llm_bridge/{robotics,robotics_planning_core}/` / tests: `tests/unit/test_l3_*` `tests/live/test_xer_*`
